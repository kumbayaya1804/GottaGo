---
gsd_state_version: 1.0
milestone: v1.0
milestone_name: milestone
status: planning
last_updated: "2026-07-30T11:00:00Z"
last_activity: 2026-07-30
progress:
  total_phases: 12
  completed_phases: 5
  total_plans: 25
  completed_plans: 19
  percent: 42
---

## Roadmap Evolution

- 2026-07-15 - Shared Artifact QA Gate routing COMMITTED as `1c84934`: `.claude/skills/artifact_qa_gate.md` defines the common evidence ladder and separate Codex/Antigravity overlays; startup/router, harness, role guides, reviewer commands, and packet generation route through it. Antigravity's installed global skill also routes Gotta Go into the project contract and composes the installed Superpowers bootstrap and verification skills. Verification passed 20/20 gate tests, skill validation, and the Superpowers skills/hooks import check; Codex approved the exact staged scope. The user accepted routing verification instead of a live Antigravity verdict for this infrastructure commit, so the hook exception is recorded without asserting Antigravity approval. The local review queue was cleared after commit.
- 2026-07-01 — Phase 3: added `family_mode`/`access_sensitivity` RPC-layer filter requirement (closes a gap between Phase 1.5's UI spec and Phase 3's success criteria). Source: `.planning/roadmap-app-store-audit-2026-07-01.md`.
- 2026-07-01 — Phase 4: added `access_sensitivity` field to `submit_location` scope. Source: same audit.
- 2026-07-01 — Phase 5: added "contribution verified" push notification success criterion. Source: same audit.
- 2026-07-01 — Phase 7: added `report_user` RPC and "report fixed" push notification. Closes the Apple 1.2 / Play UGC report/block-user gap identified as LAUNCH-BLOCKING in the audit. Source: same audit.
- 2026-07-01 — Phase 8: added save/favorite-location requirement and plan 08-04. Source: same audit.
- 2026-07-04 — Phase 3: added Nearby list-view tab (accessible alt to map, designed in Phase 1.5 but never scheduled into any phase) and a `family_mode` Settings toggle (the RPC-layer filter Phase 3 builds had no UI to ever activate it) — both folded into new plan 03-04. Source: `03-CONTEXT.md` discussion, two systematic cross-reference passes.
- 2026-07-07 — Phase 3 executed (all 5 plans, 3 waves fully autonomous + 1 wave with deferred device checkpoints). Cross-AI review (Antigravity + Codex, 7 findings) resolved pre-execution; internal code review found 2 more criticals (CR-01 null-viewport crash, CR-02 D-08 chill_spot violation) post-execution, both fixed and pushed live. VERIFICATION.md: 12/13 must-haves, pgTAP-execution gap accepted as a tracked override (no Docker in this environment) — see pending todo. 7 device-UAT items deferred by design, not gaps.
- 2026-07-07 — Phase 4 discuss-phase started. 1 of 4 selected gray areas complete (Non-building locations — closes the 2026-07-05/06 open PROJECT.md design question: no new location_type field, free-text description replaces street address when none exists, GPS fix is always the canonical coordinate source). Checkpoint saved: `.planning/phases/04-gps-service-submission/04-DISCUSS-CHECKPOINT.json`. Superseded by the 2026-07-09 Phase 4 closure update in `.beads/context/execution-state.md`.
- 2026-07-09 - Phase 4 code/review gate closed after final review-fix commit `bf93a37` (`fix phase 4 review findings`). Antigravity and Codex both APPROVE the 32-file queue; `.claude/review-queue.txt` is empty. GSD `progress` still reports Phase 04 as `Needs Review` only because `04-VERIFICATION.md` honestly remains `status: human_needed` for two deferred device-UAT walkthroughs.
- 2026-07-09 - Phase 5 readiness audit created at `.planning/phases/05-trust-engine-verification/05-READINESS.md`. It reconciles Phase 4's staging model with the trust-engine roadmap and identifies eight discussion gates: pending-candidate discovery, pre-publication event modeling, the unmeasurable 48-hour no-flag route, numeric confidence authority, trust/decay formulas, missing push infrastructure, the personal-impact metric definition, and discarded accessibility selections.
- 2026-07-09 - Codex contingency orchestration documented in `docs/codex-model-routing.md`: Sol/high is the default orchestrator while Claude is rate-limited; Terra/Luna are bounded delegates; max/ultra are task-specific; the independent Antigravity + Codex review gate remains mandatory.
- 2026-07-09 - Whole-project pre-Phase-5 audit saved at `.planning/project-audit-2026-07-09.md` and stale-info scan refreshed. At audit time, Phase 5 executable planning waited on the Phase 3 PostGIS fix, verification-event write hardening, clean lint/Jest exit, schema/source-document reconciliation, and the Phase 5 decision worksheet. This dated condition is historical and superseded by the 2026-07-10 entry below.
- 2026-07-09 - Claude recovery handoff saved at `.planning/CLAUDE-HANDOFF-2026-07-09.md` with the authoritative read order, exact P0 remediation sequence, verification evidence, review state, worktree safety notes, and Phase 5 decisions that must not be guessed.
- 2026-07-09 - Global `artifact-qa-gate` Codex skill upgraded for GPT-5.6 Sol and Gotta Go: risk-scaled evidence ladder, scope/persistence controls, delegated-work verification, failed/non-exiting command handling, completion contract, and a progressively loaded Gotta Go QA profile. Skill validation passed; the global skill is outside this repo's review queue.
- 2026-07-10 - Pre-Phase-5 P0 remediation batch (49 files) COMMITTED as `c5f4f7b` after fresh Round 8 Antigravity + Codex APPROVE, both bound to an independently-verified `scope_hash` (`sha256:87b008d0...`). Covers: PostGIS schema-qualification fix + full legacy-RPC retirement (live); verification_events direct-insert policy removal (live) + client-role ACL lockdown migration `20260710121534` (**confirmed LIVE 2026-07-10**: `information_schema.role_table_grants` shows `anon` has zero privileges and `authenticated` has SELECT only; `supabase_migrations.schema_migrations` records it applied); clean verification baseline (46/46 suites, 389/389 tests, 100% coverage); GPS-provider/location-detail recovery UX fixes; removal of the orphaned `useDeniedLocationState` hook; and a new `scope_hash` fingerprinting mechanism in the review-gate hook binding reviewer approvals to exact staged bytes. `.claude/review-queue.txt` cleared. All pre-Phase-5 P0 remediation (items 1-3) is now committed and live-verified. Next action: item 4 authority-doc refresh, then Phase 5 discussion gates.
- 2026-07-11 - Item 4 authority-doc refresh (7 files: `.planning/PROJECT.md`, `.planning/config.json`, `docs/SYSTEM_MAP.md`, `docs/schema-contract.md`, and 3 design docs) COMMITTED as `3be687a` after 4 review rounds. Codex caught 7 real accuracy defects across rounds 1-3 (not-yet-built Phase 5/7/8 decisions mislabeled "Implemented" incl. the 2-verification publish threshold; an overclaimed `submissions` read path vs. the still-live `submissions_select_published` policy; `suppressed_at`'s shipped-vs-future split; `submissions.status`'s real CHECK-constrained-text type vs. claimed enum; `search_locations_bbox`'s actual no-`distance_m` return shape; `access_code_confirmed_at`'s real nullable-no-default column vs. claimed insert default) plus a round-3 staging-hygiene issue (a stray reviewer-verdict file in the staged index alongside the queue). All fixed and independently re-verified against migrations/generated types before restaging each round. Final scope_hash `sha256:b0839309...`, both reviewers APPROVE. All pre-Phase-5 P0 remediation (items 1-4) is now complete. Next action: Phase 5 discussion gates in `05-DISCUSSION-DRAFT.md` (8 gates, per `05-READINESS.md`).
- 2026-07-11 - Phase 5 discussion completed via `/gsd-discuss-phase 5`. `05-CONTEXT.md` and `05-DISCUSSION-LOG.md` COMMITTED as `f7a49b0`, locking 34 decisions (D-35 through D-68) across candidate discovery, the pre-publication event model, presence-assurance/fraud controls, trust scoring, confidence scoring, publication/lifecycle rules, accessibility staging, and notifications — resolving all 8 architecture gaps from `05-READINESS.md`. Notable finding surfaced mid-discussion: `trust_score` (default 9) and `trust_multiplier` (default 0.5) are independent axes, not one derived from the other (D-48) — a live-schema inconsistency caught before being locked in wrong. Next action: `/gsd:plan-phase 5` (research + executable plans 05-01 through 05-06 per the recommended split in `05-READINESS.md`), then Antigravity + Codex plan review before implementation.
- 2026-07-11 - Phase 5 executable plans re-verified and materially hardened before external review. Corrections include: valid pending-only GiST predicate (no `now()` in a partial index); private server cooldown state with reason-free `accepted=false` domain results so rejected-attempt timestamps commit; creator implicit claim + one independent verifier threshold semantics for grandfathered/future rows; server enforcement against own/non-pending/expired targets; actual accessibility-tag forwarding; durable owner-scoped Published fallback; operational raw-GPS purge and submission-expiry maintenance; current Expo/Supabase Edge Function secret, ticket, receipt, backoff, and delivery-guarantee contracts; and blocking inherited + Phase 5 pgTAP execution before any Phase 5 live push. Plans remain uncommitted and require the normal independent Antigravity + fresh Codex plan review before implementation. The Task 05-02 constants/delta table decision checkpoint also remains open.
- 2026-07-12 - Phase 5 plans (05-01 through 05-06, 6 plans/5 waves) COMMITTED as `43a08eb` after research + UI-SPEC (`98c7a4a`, approved 5/6 dimensions) + pattern-mapping + internal plan-checker (1 blocker + several warnings found and fixed pre-commit: missing server-side `verify_radius_m` proximity gate was the blocker, independently verified against `docs/schema-contract.md:179`). Then began iterative EXTERNAL plan review (Antigravity + Codex) — a distinct step from the internal plan-checker gate. Antigravity APPROVED on essentially every round without further findings after round 1. Codex ran **8 full review rounds**, each finding real, independently-verified defects — this was NOT rubber-stamped; every Codex finding was checked against actual migration/schema/API source before being dispatched as a fix, and every round's fix was itself re-verified before the next review cycle. Commits, in order: `becb3a1` (round 1: 11 issues incl. plans citing a stale pre-code-review-fix migration that would have regressed 2 already-fixed Phase 4 bugs, a `submit_location` signature change that would have created a duplicate RPC overload, a discovery result-cap null-bypass), `1cd9574` (round 2: 6 more incl. a D-52 shadowban-recheck gap and a NOT-NULL-violation execution-breaking bug in the creator_claim event insert), `575f4d9` + `9a8a850` (rounds 3-4: RESEARCH.md worked examples contradicting the actual corrected plan logic — invalid non-immutable index predicate, missing FOUND-check on a lock statement, Expo channel/permission ordering, elevated-key/Vault guidance mixing, discovery-cooldown misattribution), `6085a26` (round 5: **real new plan-logic gap** — a shadowbanned creator's implicit publish claim was never checked against their current shadowban status; user decision made — inherit `shadowban_status=true` onto the published location, reusing Phase 3's existing search-suppression mechanism, rather than blocking publication or requiring extra verifiers), `49fe987` (round 6: the shadowban-status read wasn't actually lock-protected — real MVCC race — fixed with `FOR SHARE`; user decision — withhold `published_contribution` trust credit from a shadowbanned creator's suppressed publish; recorded as new locked decision **D-69** in `05-CONTEXT.md`), `13cc591` (round 7: the exact same unprotected-read bug was still present on the VERIFIER side of the eligibility count — fixed with a single unified deterministic lock order across creator+all verifiers in ascending `users.id` order to stay deadlock-safe; also structurally fixed `05-PATTERNS.md`'s Pattern 5, which had drifted out of sync with the plan across 3 separate rounds, by removing its duplicated SQL and replacing it with an invariants-only pointer to the plan as sole source of truth). **Round 8 packets sent to user, awaiting both verdicts** as of last activity — this is the current in-progress step, not yet resolved. `docs/schema-contract.md` has one small uncommitted fix (stale `user_id NOT NULL` claim) sitting in the working tree by design — it collides with `.claude/review-queue.txt`, which is actively owned by a **separate concurrent Claude Code session** doing unrelated "Shared Artifact QA Gate" work (see 2026-07-11 entry above); do not commit it without first checking whether that queue conflict has cleared, and do not touch `.claude/review-queue.txt`, `.claude/antigravity-prompt-latest.md`, `.claude/codex-prompt-latest.md`, `.claude/antigravity-review-latest.md`, or `.claude/codex-review-latest.md` — those belong to that other session, not this workstream. This workstream's own review packets/verdicts use distinct filenames: `.claude/antigravity-phase5-plan-review-prompt.md`, `.claude/codex-phase5-plan-review-prompt.md`, `.claude/antigravity-phase5-plan-review.md`, `.claude/codex-phase5-plan-review.md`. **Next action on resume:** check whether round-8 verdicts have landed in those two `-phase5-plan-review.md` files; if both APPROVE, proceed to `/gsd:execute-phase 5`; if Codex has more findings, continue the same verify-then-fix-then-recommit-then-regenerate-packets loop (grep `docs/schema-contract.md` claims against actual `supabase/migrations/*.sql` before trusting any reviewer assertion — this reviewer's hit rate has been 100% across ~30+ independently spot-checked claims this session, so treat new findings as credible pending verification, not skepticism).
- 2026-07-15 - Round 8 verdicts landed: Antigravity APPROVE, Codex REQUEST CHANGES with 3 MAJOR + 1 MINOR findings. All independently verified against the actual `05-02-PLAN.md` text before fixing (Codex's 100% hit-rate held): (1) the caller's own shadowban-dependent weight/trust effects ran before any user-row lock, (2) step 6b's trust_multiplier UPDATE took a lock before the documented global-order pass — a real reciprocal-deadlock path between two different submissions, (3) `05-RESEARCH.md`'s Pattern 5 still carried a stale copyable SQL example encoding both bugs. Fixed and COMMITTED as `db5e3bc`: the user-row lock pass (renumbered step 5) now runs before weight is computed and includes the current caller's own row alongside the creator and historical verifiers; lock mode for the whole set changed from `FOR SHARE` to `FOR NO KEY UPDATE` (strong enough for the later trust_multiplier UPDATE, never upgraded); `05-RESEARCH.md` Pattern 5 reduced to an invariants-only pointer (same treatment `05-PATTERNS.md` got in round 7); added `CURRENT-CALLER-SHADOWBAN-RACE` and a reciprocal-user lock-order deadlock pgTAP test requirement; sharpened the creator-race assertion from "either/or" to a hard block-then-resolve sequence per Codex's minor finding; propagated the lock-mode/caller-inclusion change through `must_haves`, `acceptance_criteria`, `key_links`, the Task 3 verify grep (now also asserts `for share` is ABSENT from the eventual migration), threat model T-05-13/T-05-14, `success_criteria`, `05-PATTERNS.md`, `05-CONTEXT.md`'s D-69 entry, and `05-VALIDATION.md`'s SC3/SC4-5/Wave-0 rows. Round 9 packets written to `.claude/antigravity-phase5-plan-review-prompt.md` and `.claude/codex-phase5-plan-review-prompt.md`, sent to user. **Next action on resume:** check whether round-9 verdicts have landed in the two `-phase5-plan-review.md` files; if both APPROVE, proceed to `/gsd:execute-phase 5`; if either has more findings, continue the same verify-then-fix-then-recommit-then-regenerate-packets loop.
- 2026-07-15 - Round 9 verdicts landed: Antigravity APPROVE (confirmed the round-9 lock repair is architecturally sound and complete). Codex REQUEST CHANGES with 3 MAJOR + 1 MINOR — all independently verified real (100% hit-rate held), and NOT overlapping round 8's findings (Codex explicitly confirmed "the Round 9 lock repair itself is correct"): (1) `trust_events` appends (`verification_given_nonzero`, `published_contribution`) never applied their delta to `users.trust_score` anywhere in the codebase — confirmed via migration grep, no trigger/helper exists — despite ROADMAP SC6 requiring the score to actually move; (2) `05-RESEARCH.md`'s ASCII architecture diagram still showed the pre-fix step order (weight computed immediately after distance, no lock pass at all) — a SECOND drifted executor-facing sequence the Pattern 5 fix alone didn't catch, confirmed by direct inspection; (3) the `DECIDING-VERIFIER-SHADOWBAN-RACE` pgTAP fixture described an unreachable state under the locked `submission_publish_threshold=2` — any single genuine verifier already IS the deciding call, so a "still-pending submission with an existing historical verifier" never occurs, confirmed by reasoning through the threshold math; (4) `05-01-PLAN.md` misstated `confirmation_count`'s column default as 1 vs. the live default of 0, confirmed via migration/schema-contract grep. Fixed and COMMITTED as `0d9dcd4` (scoped commit, `git commit -- <4 paths>`, to avoid sweeping up the concurrent session's separately-staged files in the shared index): Task 3 steps 7b/9 now atomically apply each trust_events delta to the affected user's `trust_score` (clamped 0-9) on the already-locked row; the architecture diagram now matches Task 3's actual step order including the lock pass; the race test was renamed `HISTORICAL-VERIFIER-SHADOWBAN-RACE` and redefined with an isolated threshold-3 fixture and three distinct identities (creator, historical verifier, current caller) to make it reachable; `05-01-PLAN.md`'s default corrected. Propagated through `must_haves`, `acceptance_criteria`, the verify grep, `05-RESEARCH.md` Pattern 6, and `05-VALIDATION.md`'s SC3/SC6/Wave-0 rows. Round 10 packets written to the same distinct-named prompt files, sent to user. **Next action on resume:** check whether round-10 verdicts have landed; if both APPROVE, proceed to `/gsd:execute-phase 5`; if either has more findings, continue the same verify-then-fix-then-recommit-then-regenerate-packets loop. Note: the concurrent session's packet-generator template now adds a "Required Skills" / "Skills Applied" section to packets (observed via an external file-modification note on `.claude/antigravity-phase5-plan-review-prompt.md`) — match that format when generating future rounds for this workstream too.
- 2026-07-15 - Round 10 verdicts landed: Antigravity APPROVE again (explicitly confirmed the trust_score sync, corrected diagram, and unchanged lock design). Codex REQUEST CHANGES with 3 MAJOR + 1 MINOR (4 sub-items) — Codex explicitly stated "the Round 9 repairs themselves are materially correct," so none of these overlap prior rounds. All independently verified real before fixing: (1) the round-10 `least(9, greatest(0, trust_score + delta))` clamp mishandles the live NULLABLE `trust_score` column — confirmed via migration (`integer default 9`, no `NOT NULL`) — PostgreSQL's GREATEST/LEAST ignore NULL, so a NULL row would silently zero instead of receiving the delta; (2) the round-9 `HISTORICAL-VERIFIER-SHADOWBAN-RACE` fixture's threshold-3 "isolated/savepoint" override cannot actually work — a transaction-local change is invisible to the separate DB session the race needs, and `app_config.key` is a primary key so no row can shadow it; (3) Phase 5 recreates `delete_account`, `withdraw_submission`, and `submit_location` from Phase 4 bodies using `search_path=public`(`,auth`) without requiring the rewrite to also harden them to Phase 5's own fixed-empty-search-path contract — confirmed via migration grep on all three inherited bodies; (4) RESEARCH.md's diagram still showed GPS/mock validation before the submission lock, two stale `deciding-verifier-shadowban` references survived the round-9 rename, RESEARCH.md's older Wave-0 list under-described the suite, and all six Phase 5 plan files had a stray duplicate `</output>` tag with no matching opener. Fixed and COMMITTED as `73fcee2`: every trust_score clamp now anchors on `coalesce(trust_score, 9)`; the historical-verifier fixture now uses a real COMMITTED config change (serialized/isolated, with guaranteed restore) instead of a savepoint; `delete_account`/`withdraw_submission`/`submit_location` now explicitly require `set search_path=''` + full qualification with pgTAP catalog assertions; the diagram order, stale test-name references, Wave-0 pointer, and all 6 stray `</output>` tags are fixed. **`docs/schema-contract.md`'s companion fix (trust_events.user_id nullability, matching the same class of fix already sitting there for verification_events.user_id) was made but is intentionally EXCLUDED from the commit** — a newly-installed concurrent-session pre-commit hook (`.claude/hooks/check-review-artifacts.js`) blocks it because it's not in their `.claude/review-queue.txt`; this workstream does not bypass hooks without explicit user approval, so it remains uncommitted in the working tree pending either their queue process or explicit user instruction. Round 11 packets written to the same distinct-named prompt files, sent to user. **Next action on resume:** check whether round-11 verdicts have landed; if both APPROVE, proceed to `/gsd:execute-phase 5`; if either has more findings, continue the same loop.
- 2026-07-16 - Round 11 verdicts landed: Antigravity APPROVE again (explicitly praised the search-path hardening, coalesced clamp, committed-config race fixture, and stray-tag cleanup). Codex REQUEST CHANGES narrowed to just ONE MAJOR finding, with the other three items marked CONFIRMED (verified correct/complete, not new defects) — a real convergence signal: (1) MAJOR — the round-10 fix (a real COMMITTED `app_config` mutation, "serialized/isolated" within one pgTAP process) is still unsafe if two SEPARATE suite invocations ever share the same database: run A commits threshold=3, run B commits threshold=3, run A finishes and restores threshold=2, run B's still-in-flight steps silently observe the wrong value — "ordering within one process" is advisory, not enforceable isolation; confirmed via inspection that `.github/workflows/ci.yml` doesn't even run the DB suite, so no existing CI isolation contract exists to lean on. (2)-(4) CONFIRMED: the nullable-trust_score coalesce fix covers every Phase 5 writer (no third writer found); the search-path hardening covers all three inherited privileged RPCs (get_profile_stats in 05-04 was already hardened; no other unsafe inherited body found) — plus a non-blocking precision note that 05-06-PLAN.md's "fixed empty/private search_path" phrasing could be misread; the round-11 structural cleanup (diagram order, stale test names, Wave-0 pointer, stray tags) is complete. Fixed and COMMITTED as `2014bb0`: the historical-verifier fixture now explicitly requires running against a disposable, single-use local Supabase/Postgres instance (never persistent/shared), with a fail-fast precondition check (assert threshold=2 before starting) as defense-in-depth; referenced from Task 5's live-push checkpoint; 05-06-PLAN.md's search_path wording clarified. `docs/verification.md` got the same isolation note added but is INTENTIONALLY EXCLUDED from the commit for the same reason as `docs/schema-contract.md` (both fall under the concurrent session's `^docs/` review-gate hook pattern and aren't in their queue) — left as uncommitted working-tree changes, not bypassed. Round 12 packets written to the same distinct-named prompt files, sent to user. **Next action on resume:** check whether round-12 verdicts have landed; if both APPROVE, proceed to `/gsd:execute-phase 5`; if either has more findings, continue the same loop. If/when the concurrent session's queue conflict clears, both `docs/schema-contract.md` (trust_events.user_id nullability) and `docs/verification.md` (disposable-instance isolation note) have pending uncommitted fixes from this workstream worth including in whatever commit resolves that queue.
- 2026-07-16 - Round 12 verdicts landed: Antigravity APPROVE again (no new findings; praised the disposable-instance fixture, coalesced clamp, lock ordering, search-path hardening). Codex REQUEST CHANGES with 1 MAJOR + 1 MINOR: (1) MAJOR, independently verified real — round 11's disposable-instance requirement was prose-only. `05-02-PLAN.md`/`05-VALIDATION.md` still required plain `supabase test db` against this repo's fixed `Gotta_Go` project/ports (`supabase/config.toml`), so two concurrent invocations could still interleave the fixture's committed threshold mutation and its restore; `05-VALIDATION.md`'s 4 gate locations still permitted any generic "Docker-capable or isolated non-production environment." (2) MINOR — the round-12 packet pointed reviewers at a nonexistent `05-06-PLAN.md` Task 3 (the plan only has Task 1/Task 2). **Fixed but NOT yet committed** (new blocker, see below): added `supabase/scripts/run-isolated-db-suite.js` — copies migrations/tests/seed + a rewritten `config.toml` (unique `project_id`, generic per-key port-offset regex, not hardcoded to today's port list) into a fresh temp workdir, preflight-checks port availability, drives it via `supabase --workdir`, and guarantees `supabase stop --no-backup` teardown in a `finally` block; refuses `--linked`/`--db-url`. Verified against the installed CLI (2.107.0, confirmed present in this environment): `--workdir` resolves `<tmp>/supabase/config.toml` correctly, the rewritten `project_id` propagates into the actual Docker container name Supabase attempts to inspect, and a real dry run (no Docker here) fails loudly with the temp dir actually deleted afterward. Caught and fixed a real bug in the runner itself during that verification: the first version called `process.exit()` inside the `try` block on a start failure, which skips `finally` in Node and leaked the temp directory — confirmed leaked, then confirmed fixed by re-running. `05-02-PLAN.md` and `05-VALIDATION.md` (all 4 locations) now require this runner specifically for any invocation including `phase5_verify_publish.test.sql`; other suite files keep plain `supabase test db --local`. **New blocker surfaced attempting to commit:** this script is the first actual executable code this workstream has produced (everything before was `.md` prose) — this repo's commit-time code-review gate (`.claude/hooks/check-review-artifacts.js`) correctly flagged it as a review-required path needing `.claude/review-queue.txt` coverage, which is actively owned by the concurrent "Shared Artifact QA Gate" session and must not be touched. **User decision (asked directly, not assumed):** fold the script's review into this workstream's own round-13 packets instead of using the shared queue; once both reviewers APPROVE, commit via `REVIEW_GATE_ALLOW_UNREVIEWED=1` citing that APPROVE as the recorded justification (same precedent as commit `1c84934`). The 3 files (`supabase/scripts/run-isolated-db-suite.js`, `05-02-PLAN.md`, `05-VALIDATION.md`) are currently `git add`-staged but uncommitted, scoped correctly (verified via `git diff --cached --name-only` before staging — did not sweep up the concurrent session's dirty `docs/schema-contract.md`, `docs/verification.md`, `.beads/context/execution-state.md`, `ANTIGRAVITY.md`, `CODEX.md`, or any `.claude/*-latest.md` file). Round 13 packets written to the same distinct-named prompt files (now also listing `supabase/scripts/run-isolated-db-suite.js` in their queue block and asking both reviewers to review it as new code, with Codex specifically asked about the `spawnSync(..., { shell: true })` call), sent to user. **Next action on resume:** check whether round-13 verdicts have landed; if both APPROVE, commit the 3 staged files with `REVIEW_GATE_ALLOW_UNREVIEWED=1` (cite the round-13 APPROVE verdicts as justification in the commit message), regenerate packets referencing the new commit hash for one final confirmation round if warranted, then proceed to `/gsd:execute-phase 5`; if either has more findings, continue the same verify-fix-recommit-repackage loop. If the staged files are ever unstaged or the working tree changes for other reasons before round-13 verdicts return, re-verify `git diff --cached --name-only` matches exactly these 3 paths before regenerating further packets.
- 2026-07-16 - Round 13 verdicts landed: Antigravity APPROVE (no new findings; called the isolation architecture "bulletproof," praised the guaranteed-cleanup fix and structural enforcement in the plan docs). Codex REQUEST CHANGES with 3 MAJOR + 1 MINOR (2 more CONFIRMED intact), all independently verified real: (1) MAJOR — teardown was gated on `started` being true, which was only set AFTER a successful `supabase start`; a start that failed after already creating containers/volumes skipped `stop` entirely. A second false-success path: if the suite passed but `stop --no-backup` itself failed, the runner only warned and still exited 0, discarding the only record of what needed manual cleanup. (2) MAJOR — `rawArgs`/`targets` were appended to `spawnSync('supabase', args, {shell:true})`; only the exact strings `--linked` and `--db-url*` were denylisted, leaving a general shell-injection surface for any other argument (confirmed empirically: a metacharacter payload executed as a live shell command under this pattern against the actual installed CLI shim). Also flagged as a deprecated Node pattern per official docs. (3) MAJOR — several EXECUTABLE plan/validation fields still named plain `supabase test db` for suites containing `phase5_verify_publish.test.sql`, despite the checkpoint prose being fixed: `05-02-PLAN.md:227`'s `<db-suite>` tag, all 4 `05-VALIDATION.md` per-task-map rows, and `05-06-PLAN.md:90`'s full-suite tag — exactly the task-local fields an execution agent would actually run. (4) MINOR — the port-rewrite regex only matched `port`/`shadow_port`, silently skipping the active `edge_runtime.inspector_port = 8083`, confirmed via a direct rewrite simulation. **Fixed but STILL NOT committed** (same blocker as round 12, not yet resolved): rewrote `run-isolated-db-suite.js` — teardown now runs whenever `supabase start` was ever invoked (tracked via `startAttempted`, set before the call, not after success), a teardown failure now forces exit 1 and PRESERVES the temp workdir (not deleted) as a manual-recovery reference instead of being downgraded to a warning; replaced `shell:true` entirely with a strict `validateTargets()` path-allowlist (every arg must resolve via `fs.realpathSync` to inside `supabase/tests/`, anything starting with `-` rejected outright) plus a `cmd.exe /d /s /c` + manual MSVCRT-style argv-quoting invocation (verified empirically: the same injection payload that executed live under `shell:true` now passes through as an inert literal argument against the same CLI shim); `PORT_KEYS` now includes `inspector_port`, confirmed via a test asserting a real `config.toml` rewrite shifts all three key types. **A second real bug was caught by the new test suite itself during this repair**, worth remembering as a pattern: an intermediate refactor used `return` inside a `try` block to short-circuit on error — this correctly ran `finally` (so cleanup happened) but then exited the ENCLOSING function entirely, skipping exit-code handling that lived after that function's own try/finally, so a failed teardown was silently exiting 0 despite `runError` being set. Fixed by extracting the whole lifecycle into its own function (`runLifecycle`) so nothing follows its try/finally that an early return could skip. Fixed every remaining plain-`supabase test db` reference Codex found (05-02/05-06/05-VALIDATION) PLUS one more via a full grep of the Phase 5 directory (05-RESEARCH.md's duplicate copy of the same test-map table). **New this round:** `supabase/scripts/run-isolated-db-suite.test.js` (new file, 18 tests via `node --test`, all passing) extracts the lifecycle into an injectable `runLifecycle()` and deterministically covers exactly the failure modes Codex asked for (partial-start-then-teardown, suite-failure-then-teardown, teardown-failure-must-not-report-success, pre-start setup failure) plus port-rewrite/quoting/path-allowlist unit tests; the teardown-gating bug above was deliberately reintroduced into a scratch copy and confirmed caught by these tests (one failed with a clear diff) before restoring the fix, so the suite is verified to actually catch regressions, not just decorative. 6 files now staged (`run-isolated-db-suite.js`, `run-isolated-db-suite.test.js`, `05-02-PLAN.md`, `05-06-PLAN.md`, `05-RESEARCH.md`, `05-VALIDATION.md`), verified scoped correctly via `git diff --cached --name-only` (did not sweep up the concurrent session's dirty files). Round 14 packets written to the same distinct-named prompt files with a computed `staged_scope_hash` in the manifest, sent to user. **Next action on resume:** check whether round-14 verdicts have landed; if both APPROVE, commit the 6 staged files with `REVIEW_GATE_ALLOW_UNREVIEWED=1` citing the round-14 verdicts, then proceed to `/gsd:execute-phase 5`; if either has more findings, continue the same verify-fix-recommit-repackage loop. Re-verify `git diff --cached --name-only` matches exactly these 6 paths before regenerating any further packets.
- 2026-07-17 - Round 14 verdicts: Antigravity APPROVE landed 2026-07-17 00:21 (called the isolation architecture "bulletproof," praised the guaranteed-cleanup fix, injection safeguards, port coverage, and test count — again no `### Verification` section, no commands shown, no file:line evidence). Codex's file on disk was still the stale round-13 leftover when checked (mtime predates the round-14 prompt) — no fresh round-14 Codex verdict was ever actually captured. **User raised a real process question: why does Antigravity keep approving while Codex keeps finding real, independently-verified defects almost every round on the same bytes?** Investigation found the root cause is NOT a difference in the two AIs' actual capability or diligence — it's a packet-generation defect in THIS workstream. `ANTIGRAVITY.md`'s own canonical "Review Output" format (section, read directly from disk) requires a mandatory `### Verification` section ("Commands run and results, or why verification was not run"), `### Runtime Boundary Check`, `### Claim And State Audit`, and a required `scope_hash` echo, plus an explicit pre-APPROVE checklist ("Every material packet claim was checked against repository evidence," "Mocked tests are not presented as proof..."). The simplified ad-hoc packet template this workstream had been sending Antigravity for many rounds (Reviewed Plans/Skills Applied/Strengths/Concerns/Wave-Dependency Check/Requirement Coverage/Risk Assessment) **dropped every one of those evidence-forcing sections** — there was no section asking Antigravity to show a single command it ran. Rereading the round-14 Antigravity verdict against `ANTIGRAVITY.md`'s own rules confirms it is exactly the failure mode the guide warns against: assertions like "the script eliminates `shell: true`" and "prevents port collisions" with zero commands, zero output, zero file:line citations — narration, not verified review. Codex's ad-hoc template had, by chance, kept an approximate `### Verification` section, which is why Codex's rounds show real evidence (blob hashes, CLI version checks, an actual executed injection-payload test) while Antigravity's did not — this was never a difference in genuine rigor between the two models, it was an artifact of which packet template each reviewer happened to receive. **Fix (packet-generation only, zero code changes — same 6 files, same `scope_hash` as round 14):** rewrote both prompt packets to use each reviewer's own canonical verdict format read directly from `ANTIGRAVITY.md`/`CODEX.md` instead of the workstream's simplified version — Antigravity's packet now requires Issues/Verification/Runtime Boundary Check/Claim And State Audit/Approved with the scope_hash echo; Codex's packet now restores the `### Open Questions` and `### Approved` sections its own canonical format has but the ad-hoc template had dropped. Both packets explicitly instruct the reviewer to re-verify the round-13→14 fix with real evidence (run `node --test`, actually grep for stale `supabase test db` references, actually try to construct an argument-validation bypass) rather than accepting the packet's own narrative. Antigravity's round-14 APPROVE is NOT accepted as sufficient under its own guide's rules and must be re-earned under the corrected format. Round 15 packets sent to user. **Next action on resume:** check whether round-15 verdicts have landed under the corrected format; if both APPROVE WITH real evidence shown in `### Verification`, commit the 6 staged files with `REVIEW_GATE_ALLOW_UNREVIEWED=1` citing the round-15 verdicts, then proceed to `/gsd:execute-phase 5`. If either verdict still lacks real evidence (commands/output/line citations) in its `### Verification` section despite the corrected template, that is itself a process finding to raise with the user, not something to silently accept. If either finds new real issues, continue the same verify-fix-recommit-repackage loop. **Going forward, all future review packets for this workstream (and ideally project-wide) should use each reviewer's canonical `ANTIGRAVITY.md`/`CODEX.md` Review Output format by default, not simplified ad-hoc templates** — the simplification was well-intentioned (shorter prompts) but silently discarded the evidence-forcing structure that makes these reviews actually useful rather than confidence theater.
- 2026-07-17 - Round 15 verdicts landed within ~4 minutes of the corrected-format packet — **user flagged the speed as suspicious given the ask (read ANTIGRAVITY.md, all 6 plans, full script, run node --test, cross-check STATE.md)**. Antigravity: APPROVE, but this time with real evidence — independently spot-checked and CONFIRMED accurate: all 3 cited line numbers (05-04-PLAN.md:92, 05-05-PLAN.md:151, 05-RESEARCH.md:796) matched real file content exactly; its claimed `node --test` run (18/18 passing) was independently re-run and matched; its code-trace line citations (225, 227, 244, 257-258 in run-isolated-db-suite.js) were checked against the actual file and were accurate — the corrected packet format genuinely worked for grounding claims in real evidence. BUT its "Manual Code Trace — Shell Injection & Allowlist" section concluded the cmd.exe/caret-escaping approach was safe purely by READING the code and reasoning about it — it never built or ran an actual attack. Codex's simultaneous round-14 verdict (finally captured this round, file was stale until now) found a REAL, independently-reproduced vulnerability in that exact code: `cmd.exe` performs `%VAR%` environment-variable expansion as a raw text pass over the WHOLE command line before it even tokenizes quotes, so no caret-escaping of the constructed string can prevent it — Codex built a real `.cmd` argv-echoing helper, drove it through the production `windowsCmdQuote()`+`cmd.exe /d /s /c` path with a crafted environment variable, and got a live `echo INJECTED` to actually execute. **This workstream independently reproduced Codex's exact finding before touching any code** (same method: real `.cmd` fixture, real production code path, `%VAR%`-payload) — confirmed real. Codex also found: `05-RESEARCH.md:796`'s per-wave sampling instruction still said plain `supabase test db (when Docker available)`, contradicting the exception directly above it (MAJOR, confirmed via direct read); and the plans overstated teardown as "guaranteed" when the implementation correctly treats a failed `supabase stop` as a hard failure with the workdir preserved, not an unconditional guarantee (MINOR, confirmed — `docs/verification.md`'s existing "attempts teardown" wording was already accurate and didn't need changing). **This is the answer to "why does Antigravity approve so fast and keep missing things even with the corrected format":** the packet-format fix (round 15) closed the evidence-*citation* gap — Antigravity now shows real commands and real line numbers — but did NOT close the evidence-*type* gap: Antigravity is still reasoning about code correctness by reading it, not by attacking it, even though `ANTIGRAVITY.md`'s own "Adversarial Review Discipline" section already says not to equate successful-looking reasoning with proof. Codex defaults to building real fixtures and running real exploits; Antigravity defaults to tracing control flow and asserting conclusions. **Real fix (code, not process, this time):** removed the `cmd.exe` command-string boundary entirely rather than trying to out-escape it. `resolveCliJsEntry()` now resolves the installed `supabase` npm package's REAL underlying JavaScript entry point (its `.cmd`/`.exe` shim, confirmed by reading the actual shim file, is nothing but `"<node.exe>" "<dp0>\node_modules\supabase\dist\supabase.js" %*`) and `makeRunSupabase()` invokes it directly via `spawnSync(process.execPath, [jsEntry, ...args], {shell:false})` — no shell, no cmd.exe, no command-line re-parsing, on any platform (the old Windows-specific branch and `windowsCmdQuote()` are gone entirely). Verified: re-ran both payloads that broke the old pattern against the new one — both arrive as inert literal arguments (`UnknownSubcommand "%ROUND14_PAYLOAD%"` from the real CLI, no expansion, no second command). `run-isolated-db-suite.test.js` replaced its old shape-only string-inspection tests with REAL round-trip tests: two against a synthetic argv-echoing fixture asserting exact `process.argv` equality (not substring checks — the first draft of one such assertion was itself buggy, asserting `!includes('INJECTED')` against a payload that legitimately contains that substring as inert text; caught and fixed to assert exact single-line JSON argv equality instead), and one genuine round-trip against the ACTUAL installed Supabase CLI on this host. 19/19 tests pass. Also fixed the two remaining Codex findings: `05-RESEARCH.md:796` now correctly splits `phase5_verify_publish.test.sql` (isolated runner) from other suites (`--local`); `05-VALIDATION.md`/`05-02-PLAN.md` wording changed from "guarantees teardown" to "always attempts teardown... not an unconditional guarantee." 6 files staged (same set as before), new `scope_hash` computed and verified via `git diff --cached --name-only`. Round 16 packets sent, EXPLICITLY instructing Antigravity to attack the fix rather than read it, and explicitly naming the round-15 miss so it understands why. **Next action on resume:** check whether round-16 verdicts have landed; specifically check whether Antigravity's `### Verification` section this time contains an actual attempted attack (fixture built, payload tried, output shown) rather than more code-reading — if it still doesn't, that's a deeper limitation worth surfacing to the user rather than re-patching the packet a third time. If both APPROVE with real adversarial evidence, commit the 6 staged files with `REVIEW_GATE_ALLOW_UNREVIEWED=1` citing the round-16 verdicts, then proceed to `/gsd:execute-phase 5`. If either finds more real issues, continue the loop.
- 2026-07-17 - Round 16 verdicts landed. Antigravity: APPROVE, and this time genuinely earned it — built and ran a real attack script (`attack-test.js`, saved to its own scratch dir with a real timestamp matching the verdict) testing the exact same %VAR%-expansion and metacharacter-chaining payloads Codex/this workstream used, rather than tracing code and asserting safety. **Independently re-ran a self-reviewed copy of that same script and reproduced its output byte-for-byte** — confirmed genuine, not fabricated. This is the adversarial-testing behavior round 16's packet explicitly demanded, and Antigravity delivered it. Its 3 MINOR findings (05-04-PLAN.md:92, 05-05-PLAN.md:151, 05-RESEARCH.md:796 needing `--local`) were unchanged carryovers from round 15, still valid, not yet fixed (low priority, no urgency). Codex: REQUEST CHANGES, 1 MAJOR + 1 MINOR (3 more CONFIRMED, including — importantly — independently re-confirming the injection fix itself works: "the Round 14 Windows command-injection defect is fixed in the production path," its own fresh test with `%SUPABASE_ROUNDTRIP_PAYLOAD%` against the real CLI reached Supabase as a literal unknown subcommand, no execution). New findings: (1) MAJOR, independently reproduced by this workstream before fixing anything — the two real-CLI round-trip tests in `run-isolated-db-suite.test.js` used a bare `return` when `resolveCliJsEntry()` returned `null`; in `node:test` a bare return is an ordinary PASS, not a skip. Codex proved this by restricting PATH so the CLI was undiscoverable and observing `pass 19`/`skipped 0` regardless. Reproduced independently (PATH restricted to node's own dir + core Windows dirs, no npm global bin) — confirmed same false-positive behavior with the pre-fix code. (2) MINOR — `05-02-PLAN.md:275` still said "guaranteed `supabase stop --no-backup` teardown," missed when the same wording was corrected at line 223 in the same file two rounds ago; confirmed via direct read. **Fixed:** both real-CLI tests now call `t.skip('supabase CLI is not discoverable on PATH...')` explicitly before returning. Extracted the shim-text-parsing half of `resolveCliJsEntry()` into its own exported pure function, `resolveJsEntryFromShimPath()`, and added 5 new deterministic tests against a synthetic on-disk npm-shim fixture (byte-accurate to the real npm template) that run unconditionally regardless of CLI installation state — covering successful resolution, fail-closed-to-null on a missing JS-entry target, fail-closed-to-null on an unrecognized shim format, direct `.exe` pass-through, and null-in/null-out. **Re-verified the fix itself, not just trusted the description:** re-ran the exact restricted-PATH scenario against the FIXED code and confirmed `pass 22`/`skipped 2` with the two real-CLI tests explicitly named as skips (`﹣` symbol, clear reason text) — this is the precise before/after Codex's finding demanded. 24 tests total (up from 19), all pass in the normal environment. Also fixed `05-02-PLAN.md:275`'s teardown wording to match the accurate contract used elsewhere, and `docs/verification.md:42`'s CLI-invocation description (previously implied every platform resolves through a JS entry; now correctly distinguishes the JS-entry branch from the direct-executable/.exe branch, matching `makeRunSupabase()`'s actual `isJsEntry` conditional). 6 files staged (same set), new `scope_hash` computed and verified via `git diff --cached --name-only`. Round 17 packets sent — lighter-weight than round 16 since no new security-relevant code path was added this round (test-infrastructure + wording fixes only), but still asking both reviewers to independently confirm the skip-vs-pass fix by running the suite themselves. **Next action on resume:** check whether round-17 verdicts have landed; if both APPROVE, commit the 6 staged files with `REVIEW_GATE_ALLOW_UNREVIEWED=1` citing the round-17 verdicts, then proceed to `/gsd:execute-phase 5`. If either finds more real issues, continue the loop. Given the trajectory (round 16 closed the last MAJOR security finding, round 17 is cleanup-tier only), this workstream may be nearing convergence — but do not assume completion without both fresh verdicts actually landing clean.
- 2026-07-17 - **Round 17 verdicts landed and CONVERGED — external plan review CLOSED, commit `4914945` on `master`.** Antigravity: APPROVE. Independently re-verified both its central claims before accepting: re-ran `node --test` normally (24/24 pass, matched exactly) and re-ran its cited restricted-PATH scenario (`pass 22`/`skipped 2`, matched exactly) — genuine, reproduced evidence, not narration. Only the same 3 low-priority MINOR carryovers from rounds 15-16 (unfixed `--local` suggestions), no new findings. Codex: APPROVE WITH CONCERNS. Independently re-confirmed everything from round 16 (skip-vs-pass fix complete — searched the whole file for other bare-`return`-in-test patterns, found none; parser extraction preserves old safe behavior; real-CLI round trip still shell-free and passes; teardown wording now accurate and consistent; zero production trust/RLS/PostGIS/concurrency regression). One new MINOR/non-blocking finding, verified real: the synthetic npm-shim test fixture was NOT byte-for-byte identical to the real installed shim (missing the real shim's unrelated `SET PATHEXT=%PATHEXT:;.JS;=;%` ELSE-branch line) and two comments overstated the fixture ("byte-accurate," "REAL .cmd helper" for what's actually a JS argv-echo file) — confirmed by direct diff against the real installed shim at `C:\Users\mrsai\AppData\Roaming\npm\supabase.cmd`. Codex explicitly stated this needs no re-review since no executable bytes change, only comment/assertion-message text. **Fixed:** rewrote the inaccurate comments in `run-isolated-db-suite.test.js` to say "matches the parser-relevant npm launch line" instead of "byte-for-byte"/"byte-accurate," and to correctly describe the three actual coverage layers (synthetic JS argv-echo fixture / synthetic .cmd shim exercising the real exported parser / genuine real-CLI round trip) instead of claiming a "REAL .cmd helper" for the JS fixture. Re-ran full suite after the comment-only change: still 24/24 pass. Re-staged same 6 files. **Given user's explicit sign-off, committed as `4914945`** on `master` with `REVIEW_GATE_ALLOW_UNREVIEWED=1`, citing both round-17 verdicts as the recorded justification (per the standing authorization from round 12 — this script/test pair was never eligible for the shared `.claude/review-queue.txt`, which is owned by a separate concurrent session). Commit stat: 6 files changed, 818 insertions, 26 deletions. **17 total external review rounds** on this one script+test pair, closing 4 real, independently-verified defect classes across the process: (round 13) teardown not guaranteed after partial start + false-success on teardown failure; (round 14) a live, reproduced Windows `cmd.exe` `%VAR%`-expansion command-injection vulnerability, closed by removing the shell boundary entirely rather than better-escaping it; (round 16) a test-integrity bug where CLI-absent environments silently reported full pass coverage with zero real assertions executed; (round 17) cosmetic-only test-comment accuracy. **This workstream's job is done — Phase 5 EXTERNAL PLAN REVIEW IS FULLY CLOSED.** **Next action:** run `/gsd:execute-phase 5` to begin Phase 5 implementation. Do not re-open this review loop unless new Phase 5 plan/validation content changes in a way that would alter the isolation runner's staged bytes.
- 2026-07-17 - **Phase 5 EXECUTION started (`/gsd:execute-phase 5`), Plan 05-01 CHECKPOINTED at Task 5's live-push gate — real structural finding, not a bug.** The Wave 1 executor (worktree-isolated) correctly identified that GSD's default execute-phase flow (subagents implement → commit → continue) conflicts with this project's own `docs/agent-harness.md`, which mandates both Antigravity and Codex APPROVE before any commit touching `supabase/**`/`app/**`/`docs/**` — enforced by `.claude/hooks/check-review-artifacts.js`, which the executor's own commit attempt correctly tripped. First dispatch attempt was killed by an unrelated API stream error mid-reasoning (no commits made, no work lost — confirmed via `git log`/`git status` in the worktree, consistent with this project's known session-limit recovery pattern). **User decision:** per-task review loop — executor implements, halts before any commit of review-gated files, orchestrator drives the Antigravity+Codex review centrally, exactly mirroring the discipline already used throughout the Phase 5 plan-review workstream (rounds 1-17 above), just applied to real implementation instead of plan-doc revisions. Resumed executor with corrected instructions; it completed Tasks 1-3 of 05-01 (RED pgTAP suites: `phase5_event_model.test.sql` 25 assertions, `phase5_discovery.test.sql` 16 assertions; GREEN migrations: `20260717120000_phase5_event_model.sql` — polymorphic verification_events + exactly-one CHECK (D-39), per-user-per-submission uniqueness (D-43), submissions.status gains 'cancelled' (D-58), new submission_tags table (D-62/63), delete_account() hardened to purge raw GPS while preserving derived evidence (D-41/D-40) + search_path='' hardened, withdraw_submission() rewritten event-aware with WR-04 preserved + search_path='' hardened, new private.verification_rate_limits (D-36); `20260717120100_phase5_discovery_rpc.sql` — search_pending_submissions_nearby, 500m radius, D-37 result cap, atomic cooldown claim, partial GiST index), self-caught and fixed 2 real bugs during self-review (a literal `gps_lat` string in a comment tripping its own verify grep; a bare `<->` KNN operator that would not resolve under `set search_path=''`, fixed to `OPERATOR(extensions.<->)` matching the proven Phase 3 remediation pattern), and correctly halted at Task 5 (BLOCKING live-push checkpoint) without attempting any `supabase db push` or commit of the 4 review-gated files. Wrote `05-01-SUMMARY.md` (committed `74e7575` — not review-gated, `.planning/**` is excluded from the hook's path patterns) documenting exactly this. **Discovered mid-session:** `.claude/review-queue.txt` (gitignored, empty, last touched 2026-07-15) and the tracked `.claude/*-prompt-latest.md`/`*-review-latest.md` files (last actually COMMITTED at `c5f4f7b` 2026-07-10; their 2026-07-15 working-tree edits were themselves uncommitted scratch state from a session that finished its real commit `1c84934` without them) are the project's genuine SHARED intended mechanism per `docs/agent-harness.md`/`/antigravity-review`/`/codex-prompt` — not still-active concurrent-session ownership the way this session had been treating them throughout the plan-review workstream (that caution was correct for the isolation-runner script rounds, since real edits WERE in flight then; it does not apply here). **Asked user directly before touching them** (not assumed) — confirmed safe to use, then used the project's actual `/antigravity-review`+`/codex-prompt` packet format for the first time this session rather than inventing another distinct-filename workaround. Merged the worktree (SUMMARY.md only), copied the 4 uncommitted review-gated files into the main checkout, removed the worktree/branch. Independently re-verified several of the implementation's own key claims before trusting them (matching this session's established discipline): `submissions.status`'s prior CHECK confirmed against `20260519010000_remote_schema.sql:192`; `verify_radius_m`'s live distinctness confirmed against `20260519020000_fix_schema.sql:23`; `delete_account`'s prior body confirmed against `20260627000004_profile_rpcs.sql:47-73`; `withdraw_submission`'s prior WR-04 body confirmed against the LATEST `20260708000000_phase4_code_review_fixes.sql:171-193` (not the older stale body); both pgTAP files' assertion counts independently regrep'd and matched their declared `plan(N)` exactly; both rewritten RPCs' client callers (`withdrawSubmission.ts`, `deleteAccount.ts`) read directly and confirmed signature-unchanged. Staged exactly the 4 intended files (verified via `git diff --cached --name-only`), computed scope_hash `sha256:d2d0912a79e5759cbcfdd6e9323a91f6e26e4a5f40d293c4c6b96ba13d2f165b`, wrote real `.claude/antigravity-prompt-latest.md` + `.claude/codex-prompt-latest.md` packets (Tier 1 context, full queued file contents, Runtime Boundary And Mock Audit with actual client-wrapper source quoted, required skills including `postgis_optimizer`/`rls_security_guard`/`trust_engine_validator`), sent to user. Corrected an inflated `completed_plans` count (20→19; `state.begin-phase` had miscounted the SUMMARY.md-only commit as a completed plan — 05-01 is checkpointed, not complete). **Next action on resume:** check whether `.claude/antigravity-review-latest.md`/`.claude/codex-review-latest.md` have fresh verdicts (mtime newer than the prompt files, generated 2026-07-17T14:29:55Z); if both APPROVE, commit the 4 files (citing both verdicts), then the plan still needs a Docker-capable `supabase test db` run (BLOCKING, no override) and explicit separate human authorization before Task 5's actual live push can proceed — do not conflate "review APPROVE" with "authorized to push." If either reviewer finds real issues, fix and re-review before committing, same loop as the plan-review workstream. `.claude/review-queue.txt` stays populated with these 4 paths until commit, per `/review-gate`'s rule not to clear it early.
- 2026-07-17 - **Round-1 verdicts for the 05-01 implementation review landed split, and Codex's finding was real.** Antigravity: APPROVE, but the `.claude/codex-review-latest.md` on disk at first glance was a stale leftover from an unrelated 2026-07-15 "Shared Artifact QA Gate" workflow batch (`scope_hash sha256:fa71714f...`, not this round's `sha256:d2d0912a...`) — a packet/verdict mismatch of exactly the kind this project's context-router warns about. The user then confirmed Codex had actually finished; the real fresh verdict (mtime after the round-1 prompt) was **BLOCK**: (1) MAJOR, independently verified real — `search_pending_submissions_nearby`'s discovery-cooldown claim in `20260717120100_phase5_discovery_rpc.sql` was a plain select-then-insert (read `last_discovery_at`, decide, then upsert): two concurrent calls for the same user could both read the same stale/null value and both pass, bypassing the abuse/privacy throttle; the denied branch also skipped the upsert entirely, so a denied call never advanced the cooldown, contradicting the function's own header comment. (2) MAJOR — the cooldown pgTAP tests only exercised serial calls with manual backdating; nothing proved a denied call still advances the claim, and nothing proved the claim safe under real concurrency — "all 16 assertions can pass while the required atomic claim remains bypassable." (3) MINOR — `withdrawSubmission.ts`'s doc comment still claimed the RPC always deletes, contradicting the already-implemented D-58 cancel-vs-delete behavior. **Fixed:** the claim is now a single locked read-modify-write — `insert ... on conflict (user_id) do nothing` to ensure the row exists, then `with locked as (select last_discovery_at ... for update) update ... set last_discovery_at = clock_timestamp() from locked ... returning locked.last_discovery_at into v_last_call` — a `for update` clause inside a CTE is never inlined by the planner, so the lock genuinely serializes concurrent callers; the elapsed-time comparison now runs unconditionally after the claim, so every call (allowed or denied) advances the timestamp. Switched from `now()` (frozen for the calling transaction's lifetime) to `clock_timestamp()` (real elapsed time, re-evaluated per call) specifically so the "denied call still advances the claim" behavior is provable inside a single wrapped pgTAP transaction, not only across separate real transactions. Added two same-session assertions to `phase5_discovery.test.sql` (now plan(18), was plan(16)) proving the denied-call-advances-claim behavior deterministically. Added a NEW file, `supabase/tests/phase5_discovery_cooldown_race.test.sql` (plan(3)), using `dblink` to open two genuinely separate Postgres sessions and fire two truly concurrent discovery calls for the same caller, asserting exactly one returns candidates — this file intentionally does NOT use `begin;...rollback;` (a dblink session can't see this script's own uncommitted fixtures) and REQUIRES the isolated runner (`node supabase/scripts/run-isolated-db-suite.js supabase/tests/phase5_discovery_cooldown_race.test.sql`), matching the same class of technique planned for 05-02's HISTORICAL-VERIFIER-SHADOWBAN-RACE fixture but applied here first. **Honestly flagged as unverified in the round-2 packets** (no Docker in this environment): both the CTE/lock-inlining reasoning and the dblink self-connection mechanism are new to this codebase and have never been executed — round-2 packets explicitly ask both reviewers to attack/attempt-to-run this rather than just trace it. Fixed the stale `withdrawSubmission.ts` comment to describe the real cancel-vs-delete behavior. Updated `.planning/phases/05-trust-engine-verification/05-01-PLAN.md` (Task 1/3 actions, verify commands, must_haves, artifacts) and `05-VALIDATION.md` (D-36 row, Wave 0 requirements, Test Infrastructure isolated-runner exception list) to describe the fixed mechanism and the new race file's isolated-runner requirement. Re-staged exactly 6 files (verified via `git diff --cached --name-only`): the 2 original migrations/tests (event_model unchanged, discovery_rpc + discovery.test.sql modified), the new race test file, and `withdrawSubmission.ts`. New `scope_hash sha256:436414eee6433b27f25ebd5b6113f1d23e46c2de026ca332a51ee3086cd2ca4a`, computed via `node .claude/hooks/check-review-artifacts.js --print-staged-scope-hash`. Round-2 packets written to `.claude/antigravity-prompt-latest.md`/`.claude/codex-prompt-latest.md` (both explicitly quote Codex's round-1 finding and ask each reviewer to verify the fix directly, not accept the packet's description), sent to user. **Next action on resume:** check whether round-2 verdicts have landed in `.claude/antigravity-review-latest.md`/`.claude/codex-review-latest.md` with mtimes after this round's prompt generation (2026-07-18T00:08:16Z) AND scope_hash `sha256:436414ee...` (do not trust a verdict bound to the round-1 hash `sha256:d2d0912a...`) — if both APPROVE, commit the 6 staged files (citing both round-2 verdicts), then the plan still needs a Docker-capable `supabase test db` run (BLOCKING, no override) for `phase5_event_model`/`phase5_discovery` PLUS the isolated-runner run of `phase5_discovery_cooldown_race.test.sql` (also BLOCKING, also no override) and explicit separate human authorization before Task 5's live push. If either reviewer finds more real issues, continue the same verify-fix-recommit-repackage loop.
- 2026-07-18 - **Round-2 verdicts landed split again, and Codex's finding was real again — this time entirely confined to the NEW test file.** Antigravity: APPROVE, genuinely engaged with the actual fix this round (traced the CTE/lock code directly, confirmed the 18 and 3 assertion counts, correctly flagged as a non-blocking concern that the dblink race file still needs a real Docker run before the live push). Codex: **REQUEST CHANGES**, 3 MAJOR + 1 MINOR, ALL in `phase5_discovery_cooldown_race.test.sql` (the migration fix itself was explicitly confirmed correct and not touched again): (1) MAJOR — the three fixture UUIDs (`...race0000a01`/`...race0000c01`/`...race000s001`) were not valid hex (`r`/`s` are not hex digits) and one had the wrong digit count, confirmed via PowerShell `[guid]::TryParse` returning `False` for all three — fixture setup would abort before either dblink session was even created. (2) MAJOR — `dblink_exec` was called with a row-returning `SELECT set_config(...)`; `dblink_exec` only accepts non-row-returning commands per its own documentation, so the session setup would error. (3) MAJOR — firing both async discovery calls back-to-back only established probable overlap, not proof: the "exactly one winner" assertion could pass even against the OLD buggy read-then-write code if one call simply finished before the other started, so the harness proved nothing about genuine concurrency. (4) MINOR — `05-01-PLAN.md:65,118,121` still said "two pgTAP suites" / "Both test files" / required every suite to end in `rollback`, contradicting the plan's own three-file, one-non-transactional design. **Fixed:** replaced the fixture ids with valid hex (`facade`-themed, independently re-verified with `[guid]::TryParse` this time before regenerating packets — the exact verification step skipped before round 2, which is how the invalid-hex bug got through); replaced `SELECT set_config(...)` with a plain `SET request.jwt.claims TO '...'` command for `dblink_exec`. **Substantially redesigned the concurrency proof** rather than patching the timing: connection A now manually acquires and HOLDS the exact row lock the RPC's own claim takes (via an explicit uncommitted transaction + an async 2-second hold), connection B dispatches the REAL production RPC call while A is confirmed still holding it, and the script polls `pg_stat_activity` for B's own backend pid in a bounded loop until B is actually observed in `wait_event_type = 'Lock'` — a genuine, deterministic proof of contention — before letting A commit (with a fresh, known `clock_timestamp()` claim) and draining B's now-unblocked result, which must show 0 rows (proving B read A's just-committed value, not a stale pre-lock one — the exact property the round-1 bug lacked). Worked around two further subtleties discovered while designing this fix (not previously flagged by either reviewer, self-caught): `pg_sleep()` returns `void`, which cannot be reliably typed through `dblink_get_result`'s `AS t(col type)` clause, so the hold is dispatched as `select 1 as ignored where pg_sleep(2) is null` instead of a bare `select pg_sleep(2)`; and the blocked-state result is recorded via `UPDATE` on a pre-created temp table rather than `CREATE TABLE AS SELECT` inside the polling `DO` block, since PL/pgSQL's direct local-variable substitution is unambiguous for `UPDATE` but not confidently so for `CREATE TABLE AS`. Fixed the stale `05-01-PLAN.md` wording to describe all three test files and the race file's deliberate non-transactional design. New `scope_hash sha256:0a51cc4e6625b7a1b9337a67b39ea1e7d258c34564fa737c01b3c0a25d047300` (only the race test file's content and the `.planning`-excluded plan doc changed; the other 5 queued files are byte-identical to round 2). Round-3 packets written to `.claude/antigravity-prompt-latest.md`/`.claude/codex-prompt-latest.md`, both explicitly quoting Codex's round-2 findings and asking each reviewer to re-verify (not trust) the fixes given the last two rounds' pattern of a plausible-looking claim turning out to have a concrete, checkable defect. **Next action on resume:** check whether round-3 verdicts have landed with mtimes after this round's prompt generation (2026-07-18T09:29:46Z) AND scope_hash `sha256:0a51cc4e...` — if both APPROVE, commit the 6 staged files (citing both round-3 verdicts) plus the along-for-the-ride `.planning/phases/05-trust-engine-verification/05-01-PLAN.md` doc fix, then the plan still needs a Docker-capable `supabase test db` run for `phase5_event_model`/`phase5_discovery` PLUS the isolated-runner run of `phase5_discovery_cooldown_race.test.sql` (both BLOCKING, no override) and explicit separate human authorization before Task 5's live push — the race file's dblink mechanism has never been executed and should be treated as unproven until it actually runs once for real. If either reviewer finds more real issues, continue the same verify-fix-recommit-repackage loop — this specific test file has now needed fixes in 2 of its first 2 review rounds, so do not assume round 3 converges without both fresh verdicts actually landing clean.
- 2026-07-18 - **Round-3 verdicts landed: both reviewers independently converged on the SAME real defect, plus Codex found 2 more wording issues.** Antigravity: REQUEST CHANGES, 1 MAJOR — `dblink_get_result` was called only once per async connection on lines 148/160, but PostgreSQL's dblink protocol requires an additional terminating call before the connection can be reused; reusing connection A via `dblink_exec` immediately after would fail with a busy-connection error, aborting the proof before A could commit and release the lock. Codex: REQUEST CHANGES, the SAME MAJOR finding (independently, citing PostgreSQL 17's dblink/monitoring docs specifically — confirmed the repo is pinned to PG17), plus 2 MINOR: (1) the test file's header called its unexecuted state a "tracked pgTAP override," contradicting `05-01-PLAN.md:227`'s explicit no-override stance for this trust boundary; (2) `05-01-PLAN.md` lines 47/110/124/263 still described the ROUND-1 mechanism ("fires two truly concurrent discovery calls... asserts exactly one returns candidates") in the Task 1 action text, artifacts block, `<done>` text, and final verification section — missed when the mechanism was redesigned in round 2's fix (only the acceptance_criteria/verify blocks were updated then, not the prose description). Codex also explicitly reconfirmed the production migration fix as byte-identical and still statically correct, and called the redesigned test "materially stronger" than round 2's, tracing exactly why the old implementation would have let B read a stale null before blocking while the new one blocks first. **Fixed:** added the missing second `dblink_get_result` drain call for both connection A (before its `update`/`commit` reuse) and connection B (before `disconnect`) — exactly 2 send/get pairs in the file, each now fully drained. Reworded the test file's header to state execution is simply unavailable on this host, not an accepted override. Rewrote `05-01-PLAN.md`'s Task 1 action text and artifacts-block `provides` field to describe the actual manual-lock-hold + `pg_stat_activity`-observed-block + post-lock-denial mechanism, fixed the `<done>` text and final `<verification>` section's stray "Two"/"Both" wording to say "three." New `scope_hash sha256:a8fe306667b6a6bab8d3f6e2ebcf84c37f6b23f796961ffa13172316cbcff9fe` (only the race test file and the `.planning`-excluded plan doc changed; the other 5 queued files remain byte-identical since round 2). Round-4 packets written, both noting this is the third fix round for this one file and explicitly asking each reviewer to consider actually executing a reduced version of the dblink sequence if at all possible, since the pattern so far is defects only catchable by precise protocol-level reasoning. **Next action on resume:** check whether round-4 verdicts have landed with mtimes after this round's prompt generation (2026-07-18T11:15:28Z) AND scope_hash `sha256:a8fe306667...` — if both APPROVE, commit the 6 staged files (citing both round-4 verdicts) plus the along-for-the-ride `05-01-PLAN.md` doc fix, then the plan still needs a Docker-capable `supabase test db` run for `phase5_event_model`/`phase5_discovery` PLUS the isolated-runner run of `phase5_discovery_cooldown_race.test.sql` (both BLOCKING, no override accepted per Task 5) and explicit separate human authorization before Task 5's live push — this race file has never actually executed and must be treated as unproven until it runs once for real, regardless of how many static review rounds it passes. If either reviewer finds more real issues, continue the same loop.
- 2026-07-18 - **Round-4 verdicts landed split: Antigravity APPROVE (clean, confirmed both drain-call placements and both wording fixes); Codex REQUEST CHANGES with a single MAJOR finding that, on independent verification, appears to be a FALSE POSITIVE — the first credible candidate for one across this entire session's ~40+ independently-verified Codex findings.** Codex's finding: the `pg_stat_activity` polling loop in `phase5_discovery_cooldown_race.test.sql` needed `pg_stat_clear_snapshot()` before each iteration, citing PostgreSQL 17's monitoring docs for a "transaction-scoped cached snapshot" behavior. **Before implementing, the exact same documentation page was fetched and read directly (via WebFetch, not trusted from the citation alone)** — it explicitly distinguishes CUMULATIVE statistics views (`pg_stat_user_tables` etc., which genuinely need `pg_stat_clear_snapshot()` for per-transaction freshness) from DYNAMIC current-activity information: verbatim, "current-query information collected by `track_activities` is always up-to-date" — stated as an explicit exception to the caching behavor, not an instance of it. `wait_event_type`/`wait_event` are part of that `track_activities`-governed per-backend live state, not the cumulative-stats subsystem `pg_stat_clear_snapshot()` targets — so the polling loop as written should already observe live state on every iteration with nothing to clear. **User was asked directly (not assumed) how to handle disagreeing with a reviewer that has had a clean record all session** — chose to push back to Codex with the citation rather than silently implementing an unnecessary fix or silently ignoring the finding. **No code changed this round** — the staged scope_hash remains `sha256:a8fe306667...` (identical bytes to round 4; Antigravity's round-4 APPROVE is therefore still current and valid, no new Antigravity packet needed). A round-5 rebuttal-only packet was written to `.claude/codex-prompt-latest.md`, quoting the exact doc text and asking Codex to confirm/retract/refine the finding rather than presenting new fixes. **Next action on resume:** check whether a round-5 Codex verdict has landed (mtime after this round's prompt, same scope_hash `sha256:a8fe306667...`) — if Codex retracts or the finding resolves to non-blocking, and Antigravity's existing round-4 APPROVE still stands (bytes unchanged, so it does), commit the 6 staged files citing both verdicts (Antigravity round 4 + Codex round 5), then the plan still needs a Docker-capable `supabase test db` run for `phase5_event_model`/`phase5_discovery` PLUS the isolated-runner run of `phase5_discovery_cooldown_race.test.sql` (both BLOCKING, no override) and explicit separate human authorization before Task 5's live push. If Codex defends the finding with real evidence this session missed, implement the fix (add `pg_stat_clear_snapshot()`) and resume the normal fix-repackage loop with a new scope_hash.
- 2026-07-18 - **Round-5 rebuttal resolved: Codex retracted the finding after checking PostgreSQL 17's actual source, not just its docs.** `pg_stat_get_activity()` (`pgstatfuncs.c`) reads the target backend's live `PGPROC.wait_event_info` via `UINT32_ACCESS_ONCE` for each call, distinct from the cached `PgBackendStatus` fields (state, query text) the transaction-snapshot warning genuinely applies to — confirming the pushback was correct for the specific field this test polls, while noting the packet's broader "all dynamic fields are exempt" framing was itself slightly overgeneralized (doesn't change the verdict here). Codex APPROVE. Antigravity's round-4 APPROVE remains valid (scope_hash unchanged, bytes identical). **Both reviewers now converge on APPROVE for scope_hash `sha256:a8fe306667...`.** Committed the 6 review-gated files (`20260717120000_phase5_event_model.sql`, `20260717120100_phase5_discovery_rpc.sql`, `phase5_event_model.test.sql`, `phase5_discovery.test.sql`, `phase5_discovery_cooldown_race.test.sql`, `withdrawSubmission.ts`) plus the along-for-the-ride `.planning/phases/05-trust-engine-verification/05-01-PLAN.md`/`05-VALIDATION.md` doc updates and the 4 review packet/verdict files, citing Antigravity's round-4 APPROVE and Codex's round-5 APPROVE. **This closes 05-01 Tasks 1-3's commit gate — 5 real, independently-verified defects were found and fixed across 5 review rounds on Task 3's discovery RPC alone** (the round-1 TOCTOU race, 3 dblink test-harness defects across rounds 2-4, one Codex false-positive correctly identified and resolved in round 5), none rubber-stamped. **Still outstanding before Task 5's live push (BLOCKING, no override accepted for this trust boundary):** a Docker-capable `supabase test db` run for `phase5_event_model.test.sql`/`phase5_discovery.test.sql`, PLUS `node supabase/scripts/run-isolated-db-suite.js supabase/tests/phase5_discovery_cooldown_race.test.sql` for the never-yet-executed dblink race suite, PLUS separate explicit human authorization for the push itself — review APPROVE is not push authorization. **Next action on resume:** check whether a Docker-capable environment is now available to run these suites; if so, run them and report results (fix forward if anything fails for real, since 5 rounds of static review already narrowed the risk surface but did not eliminate the need for real execution). If still no Docker, this remains the standing blocker for Task 5, tracked here, not silently bypassed.
- 2026-07-18 - **COMMITTED as `56aea01`.** The pre-commit gate (`.claude/hooks/check-review-artifacts.js`) itself caught two real structural gaps before allowing the commit through, both fixed without needing new review substance: (1) the round-5 Codex rebuttal packet had dropped the required "Runtime Boundary And Mock Audit" heading — added back, unchanged-since-round-4 content, no re-review needed since nothing it covers had changed; (2) Antigravity's round-4 verdict listed applied Superpowers skills as plain `using-superpowers`/`verification-before-completion`, but the gate requires the literal colon-prefixed tokens `superpowers:using-superpowers`/`superpowers:verification-before-completion` — since editing a reviewer's own verdict text would be fabricating review evidence, Antigravity was asked to reissue the identical verdict (same APPROVE, same findings, same everything) with only that wording corrected, which they did. Gate then passed clean (`node .claude/hooks/check-review-artifacts.js` exit 0) and the commit landed with all 13 intended files, nothing extraneous swept in (verified via the commit's own file list). Working tree after commit: only the other concurrent session's `ANTIGRAVITY.md`/`CODEX.md`/`docs/schema-contract.md`/`docs/verification.md` and 4 untracked `.claude/*phase5-plan-review*` scratch leftovers remain — all correctly untouched, not this workstream's concern. **Next action:** this closes the commit gate for 05-01 Tasks 1-3. Remaining before Task 5's live push (unchanged from above): Docker-capable `supabase test db` for the two standard suites, the isolated-runner run of `phase5_discovery_cooldown_race.test.sql`, and separate explicit human push authorization.
- 2026-07-29 - **Discovered on session resume: commit `9f96648` (user + Claude, 2026-07-29 08:56, pre-dating this session's work) added `.github/workflows/phase5-db-verify.yml`** — a ubuntu-latest CI workflow running the two standard Phase 5 pgTAP suites, Phase 3/4's never-yet-executed suites, and the isolated dblink cooldown-race runner. This directly targets the missing-Docker blocker tracked since the 2026-07-17/18 entries above for 05-01 Task 5's live-push gate. Not yet confirmed green — next resume should check the workflow's actual run status before treating that blocker as cleared.
- 2026-07-29 - **Resolved the shared review-queue conflict tracked since 2026-07-15/16/18 above (docs/schema-contract.md's trust_events.user_id fix and docs/verification.md's disposable-instance note, both previously excluded from commits pending the concurrent "Shared Artifact QA Gate" session's queue).** On session resume, `.claude/review-queue.txt` and the staged index showed 7 files together — the concurrent session's Gemini 3.5→3.6 Flash routing bump (`CLAUDE.md`, `ANTIGRAVITY.md`, `CODEX.md`, `.claude/commands/antigravity-review.md`) plus its new "CLI Permission Persistence Workaround" doc (the `default-cli-project` Antigravity bug) in `ANTIGRAVITY.md`/`CODEX.md`, bundled with this workstream's own two pending doc fixes in `docs/agent-harness.md`-adjacent `docs/schema-contract.md`/`docs/verification.md` — exactly the resolution the 2026-07-16 entry anticipated ("if/when the queue conflict clears... worth including in whatever commit resolves that queue"). Initially misread this as two colliding sessions and unstaged 5 files; re-checked `.claude/review-queue.txt` and the verdict files directly, found both reviewers had already reviewed the full 7-file set as one batch at `scope_hash a75e23e8...` (Antigravity APPROVE; Codex's verdict field said REQUEST CHANGES but its own prose said substance was approved, blocked only on a packet-contract technicality) — restaged the 5 files to restore parity. User re-ran Codex against the unchanged, already-correct `.claude/codex-prompt-latest.md`; it reissued a clean APPROVE at the same scope_hash. While committing, the concurrent session staged a new in-flight file (`supabase/migrations/20260519010000_remote_schema.sql`, a PostGIS `create extension ... with schema extensions` qualification fix, plus two new untracked `*-postgis-bootstrap-fix-prompt.md` packets) — confirming that session is actively live right now, not idle. Unstaged just that one file (content untouched) to preserve exact scope_hash parity, re-verified the hash matched immediately before committing, and **committed the reviewed 7-file batch as `1f08c42`**, citing both APPROVE verdicts. The concurrent session's in-flight postgis-bootstrap-fix work (staged migration diff, 2 new prompt files) and its own `.claude/*-prompt-latest.md`/`*-review-latest.md` edits remain untouched in the working tree — not this workstream's concern. **Next action on resume:** this specific queue conflict is now closed; return to the 05-01 Task 5 live-push blockers (Docker-capable pgTAP runs — check `9f96648`'s new CI workflow first — plus explicit human push authorization) as the actual next step for this workstream.
- 2026-07-29 - **`9f96648`'s new CI workflow (`phase5-db-verify.yml`) ran and failed before any pgTAP suite executed** — `supabase start` errored applying `20260707020000_phase4_submission_staging.sql`: `type "extensions.geography" does not exist (SQLSTATE 42704)`. Root cause, traced from the raw GitHub Actions log (run `30468388101`): `20260519010000_remote_schema.sql:8` ran `create extension if not exists postgis;` with no schema qualifier, so on a genuinely fresh database PostGIS installs into the search_path default instead of `extensions`, breaking every later `extensions.geography`/`extensions.geometry` reference (4 dependent migrations across Phase 3/4/5). This was never caught before because this migration's own header says its SQL never actually ran live (schema capture, marked applied via `migration repair`) — Supabase's hosted platform auto-provisions PostGIS into `extensions` before user migrations, so live was never exposed to the gap; nothing had ever replayed these migrations from a truly empty database until this CI run. Fixed: added `with schema extensions` to that one line. Reviewed via distinctly-named packets (`.claude/antigravity-postgis-bootstrap-fix-prompt.md` / `.claude/codex-postgis-bootstrap-fix-prompt.md`), not the shared queue. Antigravity APPROVE (independently verified syntax, live-impact-is-zero claim, and search_path config). Codex APPROVE WITH CONCERNS — one real MINOR finding: the fix's explanatory comment claimed "every later migration schema-qualifies postgis references," which is false (bare `geography`/`st_makepoint` references exist in `20260519020000_fix_schema.sql`, `20260624000000_block_fixes.sql`, `20260704010002_phase3_search_rpcs.sql`, `seed.sql` — they resolve via Supabase's migration-role search_path including `extensions`, not because every reference is qualified); independently confirmed via grep before fixing the comment wording per Codex's own suggested correction (no functional SQL change). Both reviewers explicit that this is static verification only — a green CI rerun is still required to prove the fix actually works end to end, not yet done as of this entry.
- 2026-07-29 - **Committed round 1 as `67c8844`** (`REVIEW_GATE_ALLOW_UNREVIEWED=1`, user-run — this scope can't go in the shared `.claude/review-queue.txt`, same precedent as `1c84934`). Re-ran the CI workflow (run `30500398251`) — the extension fix worked (no more `extensions.geography` resolution failure at that layer) but hit a **new, one-layer-deeper failure**: `20260624000000_block_fixes.sql` statement 13 (`get_locations_in_radius`) — `type "geography" does not exist (SQLSTATE 42704)`. Traced: `20260710010000_phase3_postgis_schema_qualification_fix.sql` already retroactively schema-qualified 5 PostGIS-dependent functions (`search_locations_bbox`, `search_locations_nearby`, `get_location_detail`, `get_locations_in_radius`, `count_locations_within`) via `create or replace`, but the 4 EARLIER migrations that originally created/recreated those same functions (`20260624000000_block_fixes.sql`, `20260624000002_ratings_privacy_fix.sql`, `20260704010002_phase3_search_rpcs.sql`, `20260707010000_phase3_chill_spot_null_include_fix.sql`) still had bare references — each runs before the fix migration on a from-scratch replay. Fixed: mechanical schema-qualification only (zero logic change) across all 4 files, confirmed via `list_migrations` against the live project that all 4 versions are already recorded applied (so the edit has zero live effect, same reasoning as round 1). Reviewed via a second distinct packet pair (`*-postgis-bootstrap-fix-2-prompt.md`); Antigravity APPROVE and Codex APPROVE, both clean with no findings. **Committed as `f0c5eee`** (same `REVIEW_GATE_ALLOW_UNREVIEWED=1` precedent, user-run).
- 2026-07-30 - **Re-ran CI against `f0c5eee` (run `30521939613`) — `supabase start` finally SUCCEEDED (1m 35s) from a genuinely fresh database.** Both postgis-bootstrap fix rounds are now confirmed working end to end, not just by static review. The run then reached the actual pgTAP suite execution for the first time in this project's history and got real signal: **6 of 7 suites passed** (`phase4_access_code`, `phase4_submit`, `phase5prep_legacy_radius_rpcs_dropped`, `phase5prep_verification_events_lockdown`, `phase5_event_model`, `phase5_discovery` — including both Phase 5 suites this whole workstream exists to unblock). **`phase3_read_rpcs.test.sql` failed with a real, previously-undiscovered bug** (Failed 19/22 subtests, "Bad plan. You planned 22 tests but ran 3"): `ERROR: column reference "id" is ambiguous` inside `search_locations_bbox`'s `select family_mode into v_family from public.users where id = auth.uid()` — `RETURNS TABLE (id uuid, ...)` implicitly declares `id` as a PL/pgSQL variable throughout the function body, colliding with `users.id` under Postgres's default `plpgsql.variable_conflict = error`. `search_locations_nearby` and `get_location_detail` share the identical `returns table (id uuid, ...)` + unqualified `where id = auth.uid()` pattern, so this almost certainly affects all 3, not just the one the pgTAP file happened to hit first. **This is NOT a replay-only/zero-live-effect finding like rounds 1-2 — `plpgsql.variable_conflict = error` is Postgres's standard default and Supabase doesn't override it, so this appears to be a real, currently-live bug**: any authenticated caller (any signed-in user, since the branch only triggers `if auth.uid() is not null`) hitting these 3 RPCs for family_mode filtering would get this same exception in production today, not just in CI. Not yet fixed as of this entry — flagged to the user for direction before writing a third round of changes, given the change in severity/blast-radius from the first two (CI-bootstrap-only) rounds.

- 2026-07-30 - **The ambiguous-`id` defect is CONFIRMED LIVE, not CI-only — reproduced against the production database, then fixed (pending review).** Escalated from the entry above after two independent live checks: (1) a catalog read confirmed all three live function bodies (`search_locations_bbox`, `search_locations_nearby`, `get_location_detail`) contain both `returns TABLE(id uuid` and exactly one unqualified `where id = auth.uid()`, and that live's `plpgsql.variable_conflict` is `error` (PostgreSQL's default, not overridden by Supabase); (2) a **read-only, self-aborting runtime reproduction against live** (transaction-local `set_config('request.jwt.claims', ...)` + a call to `search_locations_bbox`, inside a DO block that always ends by raising, against `stable` SELECT-only functions — nothing written, nothing persisted) returned `ERROR: 42702: column reference "id" is ambiguous` with DETAIL/CONTEXT matching the CI failure exactly; the block's `UNEXPECTED_SUCCESS` branch was never reached. **Impact: viewport pin search, Nearby list, and location detail have been broken in production for every SIGNED-IN user since Phase 3 shipped 2026-07-04** — anonymous callers are unaffected because the failing statement sits inside `if auth.uid() is not null`, which is why the public map kept working and nothing surfaced it. `20260710010000`'s PostGIS remediation rewrote these same three bodies but carried the defect forward unchanged. It went undetected for 26 days because `phase3_read_rpcs.test.sql` covers exactly this path and had never once executed (no Docker-capable environment existed until 2026-07-29). **Fix (staged, awaiting Antigravity+Codex):** 8 identical one-line changes — `select u.family_mode into v_family from public.users u where u.id = auth.uid()` — applied at point of origin in `20260704010002`/`20260707010000`/`20260710010000` (fixes from-scratch replay and stops the copy-paste propagation that spread the defect through all three files; zero live effect since those versions are already applied and never re-execute), PLUS a NEW forward migration `20260730000000_fix_ambiguous_id_in_search_rpcs.sql` which is what actually repairs live. The new migration's three bodies were extracted programmatically from the corrected `20260710010000` and `diff`-verified byte-identical (no hand transcription); it recreates only the three still-live functions and deliberately does NOT resurrect `get_locations_in_radius`/`count_locations_within` (dropped by `20260710020000`/`20260710030000`). Chose alias qualification over `#variable_conflict use_column` to disambiguate the one real collision rather than change resolution semantics for entire function bodies. Signatures/return shapes unchanged, so no `database.types.ts` regeneration or client change. Audited and deliberately left alone: the other three `where id = auth.uid()` sites (`20260627000002:49`, `20260627000004:39`, `20260704010003:32`) are in `void`/`boolean` functions with no implicit `id` variable. Packets at `.claude/antigravity-ambiguous-id-fix-prompt.md` / `.claude/codex-ambiguous-id-fix-prompt.md`, scope_hash `sha256:7ea05231...`. **Not yet proven:** that the fix makes `phase3_read_rpcs.test.sql` pass — the next CI run after commit is the real gate.
- 2026-07-30 - **STATE DISCREPANCY FOUND: Plan 05-01 Task 5's live push HAS already happened, contradicting every entry above that records it as blocked/pending.** Discovered incidentally while checking migration sequencing for the hotfix. The live project's `supabase_migrations.schema_migrations` contains `20260717120000` (phase5_event_model, **27 recorded statements**) and `20260717120100` (phase5_discovery_rpc, **5 recorded statements**) — statement counts that indicate a genuine execution, not a `migration repair --status applied` marker. Confirmed by direct object existence checks against live: `search_pending_submissions_nearby` exists, `submission_tags` exists, `private.verification_rate_limits` exists, all three new `verification_events` columns (`gps_accuracy_m`, `captured_at`, `raw_gps_purge_after`) exist, `verification_events.submission_id` exists, and the `submissions.status` CHECK constraint includes `'cancelled'`. The 2026-07-17/18 entries above state the executor "correctly halted at Task 5 (BLOCKING live-push checkpoint) without attempting any `supabase db push`" and that separate explicit human push authorization was still outstanding — that is no longer an accurate description of live. **Not yet reconstructed:** when, by whom, or through which mechanism (`db push` vs MCP `apply_migration`) these were applied; the ledger carries no timestamp column. **Practical consequence for the current hotfix:** the local and live migration ledgers are otherwise in sync (29 versions each), so `20260730000000` would be the SINGLE pending migration — a `supabase db push` would apply only the ambiguous-`id` repair and would not drag unreviewed Phase 5 schema along with it. Live push still requires explicit human authorization per this project's standing rule; review APPROVE is not push authorization. **This discrepancy should be reconciled before any further Phase 5 work plans around Task 5 being "not yet done."**

- 2026-07-30 - **Ambiguous-`id` fix reviewed, COMMITTED as `3d0bf76`, and PROVEN by real execution — but the never-before-run dblink race harness failed on its first execution, so the run is not fully green.** Both reviewers approved the 4-file scope at `scope_hash sha256:7ea05231...`: Antigravity APPROVE (independently ran the output-column collision scan across all 13 RETURNS TABLE names and confirmed `id` was the only collision; verified the ACL posture is unchanged and the retired RPCs are not resurrected); Codex APPROVE WITH CONCERNS with **zero findings at any severity** (independently recomputed the scope hash, verified staged/worktree blob parity per file, and confirmed the new migration's three bodies are byte-identical to the corrected `20260710010000` using per-function SHA-256 digests rather than trusting the packet's `diff`). Both independently endorsed the two design choices put to them: alias qualification over `#variable_conflict use_column`, and historical-edits-plus-forward-migration as serving genuinely distinct purposes. **CI run `30524075167` on `3d0bf76`: steps 1-7 all SUCCESS. All 7 standard pgTAP suites passed, including `phase3_read_rpcs.test.sql`** — the suite that exposed this defect and had failed 19/22 subtests one run earlier. `20260730000000` applied cleanly during replay (log line 65). **Codex's explicitly enumerated 4-point gate is therefore fully satisfied**: (1) `supabase start` completed, (2) `20260730000000` applied, (3) `phase3_read_rpcs.test.sql` passed, (4) the six other suites retained their passes. **Step 8 — the isolated dblink cooldown-race suite — FAILED on its first-ever execution** (it was skipped in all three prior runs because earlier steps failed): `ERROR: password or GSSAPI delegated credentials required` at `phase5_discovery_cooldown_race.test.sql:74`, DETAIL "Non-superusers must provide a password in the connection string or send delegated GSSAPI credentials". 0 of 3 planned tests ran ("Bad plan. You planned 3 tests but ran 0"). Root cause: the fixture's `dblink_connect` call supplies no password, and PostgreSQL refuses password-less dblink connections for non-superusers by design. **This is exactly the outcome the 2026-07-18 entries predicted and deliberately refused to paper over** ("the race file's dblink mechanism has never been executed and should be treated as unproven until it actually runs once for real, regardless of how many static review rounds it passes") — roughly 9 static review rounds across two workstreams could not catch it, because it is an authentication-layer runtime behavior invisible to code reading. Note what this failure does and does not mean: the suite's target, the discovery-cooldown TOCTOU repair in `20260717120100`, is already live and is NOT implicated — what is missing is the *proof*, not the fix. The isolation runner itself worked correctly end to end (provisioned a disposable instance `gotta_go_isol_7e366e70` at port offset +2330, replayed all 29 migrations, and tore down cleanly on failure). **Live push of `20260730000000` COMPLETED by the user and INDEPENDENTLY VERIFIED against production (2026-07-30) — the 26-day signed-in search outage is repaired.** Verification did not rely on the CLI's success output: the live ledger now records `20260730000000` with 12 statements; all three functions' live bodies contain `where u.id = auth.uid()` and zero contain the unqualified form; and re-running the *exact* pre-fix reproduction (same read-only, self-aborting DO block that returned SQLSTATE 42702 hours earlier) now returns only the block's own deliberate `P0001 UNEXPECTED_SUCCESS` — the ambiguity error is gone. A second block exercised the other two paths as an authenticated caller: `search_locations_nearby` returned **5 real rows** and `get_location_detail` executed cleanly (0 rows for a synthetic id), confirming all three bodies are repaired at runtime, not just in the catalog. The push was briefly blocked on CLI credentials (since resolved by the user): (1) `supabase projects list` returns `Unauthorized`, and passing the token via `SUPABASE_ACCESS_TOKEN="$(cat ~/.supabase-gsd-token)"` returns `Invalid access token format. Must be like sbp_0102...1920` — the file holds a 73-character value not beginning with `sbp_`, i.e. not a current-format personal access token (checked by shape only, value never read or printed); the user reports an active token exists in the dashboard, so the file is stale/wrong rather than the token being revoked. Fix is `supabase login` in a real interactive terminal, or replacing that file's contents with the current `sbp_...` token. (2) `supabase db push` additionally requires `SUPABASE_DB_PASSWORD`; the `!`-prefix shell is non-interactive so `read -s` silently yields an empty string and the CLI fails the same way — it must be run from a real terminal window. **SECURITY: the live DB password was pasted into the assistant chat transcript on 2026-07-30 and was treated as compromised — user confirmed it has been ROTATED via Dashboard → Settings → Database → Reset database password.** This supersedes the earlier standing "do not rotate the DB password" decision, which applied to a secret that had not leaked; the app authenticates with the publishable/anon API keys rather than the DB password, so rotation should not affect it. The assistant did not use the pasted password.

- 2026-07-30 - **dblink race-suite fix, round 1: SPLIT VERDICT — Antigravity APPROVE, Codex REQUEST CHANGES with a real finding Antigravity's pass missed.** Root cause of the CI failure (`password or GSSAPI delegated credentials required`, 0/3 assertions run on the suite's first-ever execution): dblink's own privilege check requires non-superuser callers to supply an explicit password. Round-1 fix added `user=postgres password=postgres` to both `dblink_connect()` calls in `phase5_discovery_cooldown_race.test.sql`. Antigravity approved on the reasoning that `postgres`/`postgres` is the documented local-dev default and the role-independence of the race proof holds (confirmed correct on both counts, not disputed later). **Codex caught something deeper**: with no `host`/`hostaddr` given, libpq opens a Unix-domain socket, and this exact pinned Postgres image (`17.6.1.104`, confirmed against `supabase/.temp/postgres-version`) routes `local all all` through `peer map=supabase_map` — the server process's own OS user (`postgres`) maps straight to role `postgres` via peer auth, silently ignoring any password in the string; dblink then separately refuses non-superuser sessions that didn't actually authenticate via password, even if one was supplied. Round 1 would have failed with the identical error via a different path. Cited specific evidence: PostgreSQL's own dblink_connect docs plus the exact `pg_hba.conf.j2`/`pg_ident.conf.j2` source at the pinned image tag. **Independently re-verified before accepting**, not trusted from the citation alone: fetched both files directly at `17.6.1.104` via WebFetch and confirmed every quote verbatim; additionally fetched `postgresql.conf.j2` and confirmed `listen_addresses = '*'` and the default port 5432 (needed for the round-2 fix below); also confirmed `127.0.0.1` itself would ALSO hit this image's `trust` HBA rule and ignore the password, so it doesn't fix the problem either. **Round-2 fix**: force a genuine TCP connection to `hostaddr=127.0.0.2 port=5432` instead of the Unix socket — `127.0.0.2` doesn't match the exact `127.0.0.1/32` HBA rule, so it falls through to the `0.0.0.0/0 scram-sha-256` catch-all, which does require and verify the password; reachable at all only because `listen_addresses='*'`; `127.0.0.0/8` is loopback per POSIX regardless of Docker network topology, avoiding the need to guess the container's actual bridge/NAT address (the specific gap in Codex's own alternative suggestion, `inet_server_addr()`, which Codex itself flagged as unverified). Round-2 packets sent to both reviewers, explicitly asking them to independently re-fetch the same source files rather than trust this round's restatement, and flagging that neither round of review — nor this fix — has been proven by an actual execution; Codex's round-1 gate (a green isolated run, 3/3 assertions passing, clean teardown) is unchanged and still required. **This is a second consecutive real defect Codex caught that Antigravity's independent pass missed on the same file** — worth noting as a pattern, not just this instance.
- 2026-07-30 - **dblink race-suite fix, round 2: ALSO REQUEST CHANGES from Codex, a second real and genuinely different defect — Antigravity approved round 2 as well, making it 0-for-2 on this file.** Round 2's fix (`hostaddr=127.0.0.2 port=5432`) was reasoned to fall through the `127.0.0.1/32 trust` HBA rule since `127.0.0.2` isn't a literal match. **Codex's correction: `pg_hba.conf` matches the CLIENT'S SOURCE address, not the destination address `hostaddr=` selects.** Linux's automatically-created local route for `127.0.0.0/8` via the loopback interface carries `src 127.0.0.1` (the only address actually assigned to `lo`), so a connection dialed to `127.0.0.2` still arrives at Postgres appearing to originate FROM `127.0.0.1` absent an explicit client-side bind — hitting the identical `trust` rule round 2 was trying to avoid. Cited PostgreSQL's own `pg_hba.conf` docs (client-address semantics), libpq's connection-parameter docs (`hostaddr` sets the server address only), and Linux's `ip-route(8)` preferred-source behavior. **This is a materially different failure mode than round 1's** (peer-auth-via-Unix-socket) — a second genuinely distinct networking mistake on two consecutive attempts, not a repeat of the same one. Codex's suggested alternative (`inet_server_addr()`, keyed off the outer session's own real network address) carries its own acknowledged uncertainty — it assumes the outer `supabase test db` session connects via real TCP to a non-loopback interface, which is unconfirmed. **Two consecutive wrong attempts from careful static config analysis, on the same file, each missed by both my own reasoning and Antigravity's independent review — decided to stop attempting further speculative fixes without real execution capability** rather than risk a third. Task #8 remains open as a deferred, non-blocking item: the target RPC (`search_pending_submissions_nearby`'s discovery-cooldown TOCTOU fix) is already live, correct, and covered by the passing standard `phase5_discovery.test.sql` suite — only this one race-condition proof harness lacks execution, unchanged from before either round.

- 2026-07-30 - **Reconciled `05-01-SUMMARY.md` with the confirmed-live Task 5 push** (the discrepancy first flagged earlier today). Added a dated "Update (2026-07-30)" section documenting the live ledger/object evidence, explicitly left the original 2026-07-17 checkpoint narrative intact below it (accurate as of when it was written, not deleted or rewritten), and annotated the stale 5-item "Next Phase Readiness" blocking checklist item-by-item: items 1-4 (pgTAP pass, review, commit, push authorization) done or presumed done (commit/review mechanism not reconstructed — no session record exists between the 2026-07-17 checkpoint and today's discovery); **item 5, regenerating `database.types.ts`, confirmed still outstanding** and is this plan's actual next action. Did not alter `requirements-completed: []` or bump `.planning/STATE.md`'s `completed_plans` count — deferred until Task 4 actually completes, to avoid marking the plan done while its own checklist still has an open item. **Next action:** run `supabase gen types typescript` against live, verify it surfaces `search_pending_submissions_nearby`/`submission_tags`/the three new `verification_events` columns (05-01-PLAN.md's own Task 4 success criterion), and commit `app/src/lib/database.types.ts`.
- 2026-07-30 - **Task 4 done: regenerated `app/src/lib/database.types.ts` via the Supabase MCP `generate_typescript_types` tool against live** (CLI auth wasn't working in this session's shell; the MCP tool was already authenticated and used throughout the session). Confirms 05-01-PLAN.md's own Task 4 success criterion: `search_pending_submissions_nearby` and `submission_tags` both present. Diff is purely additive (65 insertions / 3 deletions) and matches the two Phase 5 migrations exactly: new `submission_tags` table, new `verification_events` columns (`gps_accuracy_m`, `captured_at`, `raw_gps_purge_after`, `submission_id`), and `verification_events.location_id` becoming nullable (D-39). **Caught and fixed a real transcription error in my own first draft** while manually copying the tool's output into the file (no direct pipe-to-file path available): the `CompositeTypes<>` generic helper's final line referenced the wrong type parameter, inconsistent with the other three helpers' pattern — caught by inspection before running verification, not by the tooling. Verified after the fix: `npm run typecheck` clean, `npx eslint` on the file clean, full test suite 46/46 suites and 389/389 tests pass. Staged and under review via distinct packets (`.claude/antigravity-05-01-types-regen-prompt.md` / `.claude/codex-05-01-types-regen-prompt.md`, scope_hash `sha256:ed639ed6...`) since this file matches the project's `^app/` review-gated pattern — explicitly asked both reviewers to scrutinize the generic-helper-type section further, given one real error was already found there by hand, and asked whether either has a way to independently re-generate and diff rather than rely on test-suite evidence alone.

### Quick Tasks Completed

| # | Description | Date | Commit | Directory |
|---|-------------|------|--------|-----------|
| 260704-0kt | Harness integrity fix batch: queue-path normalization, Antigravity invocation repair, roster format drift, Stop-hook gating, review-gate wording | 2026-07-04 | b413be8 | [260704-0kt-harness-integrity-fix-batch-queue-path-n](./quick/260704-0kt-harness-integrity-fix-batch-queue-path-n/) |

## Accumulated Context

### Pending Todos

- [Non-comparative engagement and novelty ideas](../todos/pending/2026-07-06-non-comparative-engagement-and-novelty-ideas.md) — dopamine/retention mechanic ideas (discovery log, private streaks, gut-health trivia, quiet aggregate social proof, self-facing badges) compatible with the standing anti-comparative-gamification decision; not yet scoped to a phase.
- [Run pgTAP suite on Docker-capable machine](../todos/pending/2026-07-07-run-pgtap-suite-on-docker-capable-machine.md) — Phase 3's 24-assertion pgTAP suite and Phase 4's two 21-assertion pgTAP suites have never executed (no Docker here); accepted as a tracked override, but must run before trusting the SQL/RPC foundation Phase 5 builds on.

### Pending Device UAT (Phase 3)

7 device-verification items deferred by design from Phase 3's 5 plans (Mapbox rendering/gestures, RPC-failure banner, Nearby screen-reader pass, family_mode end-to-end + display-name preservation, filter AND-logic/session-persist, denied-GPS fallback) — full steps in `.planning/phases/03-read-path-map/03-VERIFICATION.md` § Human Verification Required.

### Pending Device UAT (Phase 4)

2 device-verification items deferred by design from Phase 4: SubmitFlow real GPS/permission/mock-location walkthrough, plus pending-pin/withdraw/code-update device walkthrough. Full steps live in `.planning/phases/04-gps-service-submission/04-HUMAN-UAT.md` and `.planning/phases/04-gps-service-submission/04-VERIFICATION.md`.

Last activity: 2026-07-30
