---
phase: 05-trust-engine-verification
plan: 04
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - supabase/migrations/*_phase5_impact_stat.sql
  - supabase/tests/phase5_impact_stat.test.sql
  - app/src/features/profile/profileStats.ts
  - app/src/features/profile/__tests__/profileStats.test.ts
  - app/src/app/(tabs)/profile.tsx
  - app/src/lib/database.types.ts
autonomous: false
requirements:
  - "R-IMPACT: User sees a private, non-comparative personal impact stat reflecting their real GPS-verified contribution count"
user_setup: []

must_haves:
  truths:
    - "get_profile_stats returns the caller's gps_verified_contribution_count as a distinct-bathrooms-helped figure"
    - "The Profile shows a private, non-comparative sentence: 'You've helped verify N bathrooms.' (singular at N=1)"
    - "The zero state shows the encouraging copy, not a rank/badge/leaderboard"
    - "The count is server-maintained (incremented in the 05-02 publish transaction), never client-computed or fabricated"
  artifacts:
    - path: "supabase/migrations/*_phase5_impact_stat.sql"
      provides: "get_profile_stats extended with bathrooms_helped from users.gps_verified_contribution_count"
      contains: "gps_verified_contribution_count"
    - path: "app/src/features/profile/profileStats.ts"
      provides: "ProfileStats.bathroomsHelped mapping with ?? 0 fallback"
      contains: "bathroomsHelped"
    - path: "app/src/app/(tabs)/profile.tsx"
      provides: "private non-comparative impact-stat sentence"
      contains: "helped verify"
  key_links:
    - from: "app/src/app/(tabs)/profile.tsx"
      to: "get_profile_stats → gps_verified_contribution_count"
      via: "profileStats.ts service"
      pattern: "bathroomsHelped"
---

<objective>
Surface the private, non-comparative personal impact stat sourced from the server-maintained GPS-verified contribution count.

Purpose: Gives verifiers their own reward loop (D-67 routes the publication push to the creator only; verifiers get this stat instead) without any comparative gamification.

Output: `get_profile_stats` extension migration, pgTAP, `profileStats.ts` + Profile UI additions, jest, regenerated types.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05-trust-engine-verification/05-CONTEXT.md
@.planning/phases/05-trust-engine-verification/05-PATTERNS.md
@.planning/phases/05-trust-engine-verification/05-UI-SPEC.md
@.planning/phases/05-trust-engine-verification/05-02-SUMMARY.md
@docs/design/design-system.md

<interfaces>
- supabase/migrations/20260701211135_profile_stats_rpc.sql — get_profile_stats (lines 16-38): `language sql security definer stable set search_path = public`, returns a JSON object, authed-only revoke/grant triple (lines 36-38). Add a 4th key bathrooms_helped selecting gps_verified_contribution_count from users where id = auth.uid().
- users.gps_verified_contribution_count integer default 0 — incremented inside the 05-02 publish transaction for each distinct qualifying nonzero verifier (D-65). This plan only READS/displays it.
- app/src/features/profile/profileStats.ts (lines 24-35) — ProfileStats interface + GetProfileStatsRpcResult + the `?? 0` mapping. Add bathroomsHelped.
- Profile tab stats block — add a 4th stat as a plain count SENTENCE, not a competitive metric.
</interfaces>

## Copy contract (05-UI-SPEC.md, [NEW — proposed])
- Stat sentence: "You've helped verify {N} bathrooms." (singular "1 bathroom" when N=1).
- Zero state: "You haven't verified any bathrooms yet. Verify one nearby to help your community."
- Constraint: private, non-comparative — NO rank, percentile, badge, leaderboard, streak, or "more than X% of users".
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend get_profile_stats + author pgTAP</name>
  <read_first>
    - supabase/migrations/20260701211135_profile_stats_rpc.sql (whole file — get_profile_stats to extend)
    - supabase/migrations/20260519010000_remote_schema.sql (users.gps_verified_contribution_count line 23)
    - supabase/tests/phase4_submit.test.sql (pgTAP conventions)
  </read_first>
  <action>
    Create the phase5_impact_stat migration that `create or replace`s get_profile_stats to add a bathrooms_helped JSON key selecting gps_verified_contribution_count from public.users where id = auth.uid(). Keep `language sql security definer stable set search_path = public` and the existing authed-only revoke(public/anon)/grant(authenticated) triple. Do NOT recompute the count here (it is maintained by the 05-02 publish txn) and do NOT add any ranking/aggregate across users. Author supabase/tests/phase5_impact_stat.test.sql asserting get_profile_stats returns bathrooms_helped equal to the caller's stored count and that it is caller-scoped (a different user gets their own count).
  </action>
  <verify>
    <automated>ls supabase/migrations/*phase5_impact_stat.sql &amp;&amp; grep -q "gps_verified_contribution_count" supabase/migrations/*phase5_impact_stat.sql &amp;&amp; grep -q "bathrooms_helped" supabase/migrations/*phase5_impact_stat.sql &amp;&amp; test -f supabase/tests/phase5_impact_stat.test.sql</automated>
    <db-suite>supabase test db (phase5_impact_stat — BLOCKING before live push)</db-suite>
  </verify>
  <acceptance_criteria>
    - get_profile_stats returns a bathrooms_helped key from users.gps_verified_contribution_count, caller-scoped via auth.uid().
    - No cross-user ranking/aggregate is introduced; grant triple stays authed-only.
    - pgTAP suite asserts the caller-scoped count.
  </acceptance_criteria>
  <done>The server exposes the private contribution count via the existing profile-stats contract.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: profileStats.ts mapping + Profile impact-stat sentence</name>
  <read_first>
    - app/src/features/profile/profileStats.ts (whole file — ProfileStats interface + ?? 0 mapping)
    - app/src/app/(tabs)/profile.tsx (existing stats block)
    - app/src/lib/database.types.ts (regenerated get_profile_stats return shape)
    - .planning/phases/05-trust-engine-verification/05-UI-SPEC.md (copy + non-comparative constraint)
    - docs/design/design-system.md (tokens, §20 checklist)
  </read_first>
  <behavior>
    - Test 1: profileStats maps bathrooms_helped → bathroomsHelped with a `?? 0` fallback when absent.
    - Test 2: Profile renders "You've helped verify 3 bathrooms." for N=3 and the singular "You've helped verify 1 bathroom." for N=1.
    - Test 3: Profile renders the zero-state copy when N=0 and never renders a rank/percentile/badge/leaderboard string.
  </behavior>
  <action>
    Add bathroomsHelped to the ProfileStats interface, GetProfileStatsRpcResult, and the `?? 0` mapping in profileStats.ts. Render the impact-stat sentence in the Profile stats block per the locked copy (singular/plural handled; zero-state copy at N=0), as a plain non-comparative sentence alongside the existing stats — NO rank, badge, leaderboard, streak, or comparison (D-65). Tokens-only styling (Colors[colorScheme], typography, spacing); pass §20 checklist (SC11). TDD order (TDD Guard active): write app/src/features/profile/__tests__/profileStats.test.ts first (RED), implement to green.
  </action>
  <verify>
    <automated>cd app &amp;&amp; npx jest src/features/profile</automated>
  </verify>
  <acceptance_criteria>
    - profileStats.ts contains bathroomsHelped with a `?? 0` fallback.
    - Profile renders the singular/plural/zero copy exactly; grep shows no rank/percentile/badge/leaderboard/streak strings in the impact stat.
    - jest suite passes.
  </acceptance_criteria>
  <done>The Profile shows a private, non-comparative "You've helped verify N bathrooms" stat driven by the server count.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] Live schema push — profile-stats extension + regen types</name>
  <read_first>
    - supabase/migrations/*_phase5_impact_stat.sql
    - docs/verification.md (live-push authorization + verification conventions)
  </read_first>
  <files>supabase/migrations/*_phase5_impact_stat.sql</files>
  <action>Do NOT run autonomously. Require the impact-stat pgTAP plus inherited/new Phase 5 DB suites to pass first. Then PAUSE for explicit live-push authorization. On approval, run `supabase db push`, verify caller scoping live, and regenerate types.</action>
  <acceptance_criteria>
    - Execution paused for user authorization before any live push.
    - The impact-stat and prerequisite trust-engine pgTAP suites executed successfully before the push.
    - After approval, `supabase db push` applied the migrations and they are recorded in supabase_migrations.schema_migrations.
    - No live push occurred without a fresh user authorization.
  </acceptance_criteria>
  <what-built>The get_profile_stats extension migration and pgTAP are written; the client mapping/UI are implemented. Execution PAUSES for the user to authorize the live push (standing rule: every live supabase db push needs fresh authorization). Build/type checks pass without the push — skipping creates a false-positive.</what-built>
  <how-to-verify>
    1. Review the phase5_impact_stat migration.
    2. Authorize the push; the executor runs `supabase db push`.
    3. Confirm get_profile_stats returns bathrooms_helped live.
    4. After success, the executor runs `supabase gen types` into app/src/lib/database.types.ts.
  </how-to-verify>
  <resume-signal>Type "pushed" once the live push succeeded and types regenerated, or describe any failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → get_profile_stats | Caller reads only their OWN aggregate via auth.uid(); no cross-user data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-16 | Information Disclosure | cross-user impact data | mitigate | get_profile_stats is caller-scoped via auth.uid(); no ranking/aggregate across users |
| T-05-17 | Tampering | client-fabricated count | mitigate | Count is server-maintained in the publish txn; client only displays get_profile_stats output |
</threat_model>

<verification>
- pgTAP executes and proves caller-scoped bathrooms_helped before live push.
- `cd app && npx jest src/features/profile` passes singular/plural/zero + no-comparison assertions.
- Migration applies on live push; get_profile_stats returns bathrooms_helped; types regenerated.
</verification>

<success_criteria>
- Profile shows a private, non-comparative impact sentence from the real server count.
- No rankings/badges/leaderboards; count is server-maintained, never fabricated.
</success_criteria>

<output>
Create `.planning/phases/05-trust-engine-verification/05-04-SUMMARY.md` when done.
</output>
