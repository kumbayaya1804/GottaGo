---
phase: 05-trust-engine-verification
plan: 06
type: execute
wave: 5
depends_on: ["05-05"]
files_modified:
  - supabase/migrations/*_phase5_lifecycle_jobs.sql
  - supabase/tests/phase5_lifecycle_jobs.test.sql
autonomous: false
requirements:
  - "R-LIFECYCLE: raw verification GPS is purged after the reviewed retention window and expired submissions transition to expired"
  - "R-PUBLISH: the 48-hour no-flag route exists only as a disabled fail-closed stub until a measurable pending-objection signal exists"

must_haves:
  truths:
    - "Expired raw gps_location (the single raw-coordinate column; gps_lat/gps_lon do not exist live — dropped by 20260519020000) is purged while derived accuracy/distance/weight evidence remains"
    - "Pre-existing verification_events rows that predate this phase (added with a NULL raw_gps_purge_after because 05-01 added the column with no default) are backfilled to a real purge deadline so they become purge-eligible — a NULL deadline never satisfies `<= now()` and would otherwise survive forever"
    - "Pending submissions past expires_at transition to expired and retain verification evidence"
    - "The 48-hour routine exists as documented intent but cannot publish any row and has no schedule"
    - "Only the measurable expiry/purge maintenance routine is scheduled, after separate authorization"
  artifacts:
    - path: "supabase/migrations/*_phase5_lifecycle_jobs.sql"
      provides: "one-time NULL-deadline backfill, private purge/expiry routines, one schedulable maintenance wrapper, and disabled auto-promote stub"
      contains: "purge_expired_verification_gps"
    - path: "supabase/tests/phase5_lifecycle_jobs.test.sql"
      provides: "pgTAP proving selective raw-GPS purge, legacy NULL-deadline row becomes purge-eligible after backfill, expiry transition, evidence preservation, and zero auto-promotion"
      contains: "maybe_auto_promote"
  key_links:
    - from: "private.run_phase5_lifecycle_maintenance"
      to: "verification_events + submissions"
      via: "service-only scheduled purge/expiry transaction"
      pattern: "raw_gps_purge_after"
---

<objective>
Close the measurable lifecycle obligations that the original plan omitted: backfill legacy purge deadlines, actually purge expired raw GPS, transition expired pending submissions, and retain the unmeasurable 48-hour promotion only as a disabled fail-closed stub.

Purpose: D-40 and D-59 are runtime obligations, not migration comments. D-60 must not create a second unsafe publish path. A NULL `raw_gps_purge_after` (the state every pre-phase row is in, since 05-01 added the column with no default) never becomes purge-eligible, so legacy rows must be given a deadline once `raw_gps_retention_days` is seeded.

Output: one lifecycle migration (with legacy-deadline backfill), pgTAP coverage, live schema checkpoint, and separately authorized maintenance schedule.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05-trust-engine-verification/05-CONTEXT.md
@.planning/phases/05-trust-engine-verification/05-RESEARCH.md
@.planning/phases/05-trust-engine-verification/05-01-SUMMARY.md
@.planning/phases/05-trust-engine-verification/05-02-SUMMARY.md
@.planning/phases/05-trust-engine-verification/05-05-SUMMARY.md

<interfaces>
- verification_events has gps_location geography(Point,4326) as the SINGLE raw-coordinate authority (the original gps_lat/gps_lon numeric columns were backfilled into gps_location then DROPPED by 20260519020000_fix_schema.sql — they do NOT exist live), derived distance/accuracy/weight fields, a `timestamp` timestamptz recorded-at column (default now(), from 20260519010000), and raw_gps_purge_after from 05-01 (nullable, NO default — pre-existing rows therefore have a NULL deadline).
- app_config.raw_gps_retention_days was seeded in 05-02 (Wave 2) with the Task-1-locked value; 05-06 (Wave 5) runs after it, so the value is available for the backfill.
- submissions has expires_at and status including expired/cancelled from 05-01; immutable events reference retained rows.
- reports.location_id is NOT NULL, so pending submissions have no measurable objection signal (D-60).
- Publication remains exclusively inside 05-02 verify_location under the submission row lock.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author lifecycle pgTAP, then implement legacy-deadline backfill + private purge/expiry routines and fail-closed promotion stub</name>
  <read_first>
    - supabase/migrations/*_phase5_event_model.sql
    - supabase/migrations/*_phase5_app_config_seeds.sql (raw_gps_retention_days seed the backfill reads)
    - supabase/migrations/*_phase5_verify_and_publish.sql
    - supabase/migrations/20260519010000_remote_schema.sql (verification_events `timestamp` column; reports NOT NULL contract)
    - supabase/tests/phase5_event_model.test.sql
    - .planning/phases/05-trust-engine-verification/05-READINESS.md (gap 3)
  </read_first>
  <action>
    Write phase5_lifecycle_jobs.test.sql first. Then create the migration:
    1. ONE-TIME LEGACY BACKFILL (runs when this migration applies, after 05-02 seeded raw_gps_retention_days): `UPDATE public.verification_events ve SET raw_gps_purge_after = ve.timestamp + ((select value::int from public.app_config where key = 'raw_gps_retention_days') || ' days')::interval WHERE ve.raw_gps_purge_after IS NULL AND ve.gps_location IS NOT NULL;` (reference the recorded-at column as `ve.timestamp` — it is a reserved word, always qualify it). This gives every pre-phase row a concrete deadline so it can eventually become purge-eligible; without it a NULL deadline never satisfies `raw_gps_purge_after <= now()` and the row's raw GPS survives forever. Rows already carrying a deadline (created by verify_location/submit_location after 05-02) are untouched.
    2. `private.purge_expired_verification_gps()` nulls the RAW COORDINATE (gps_location — the single raw-coordinate column; do NOT reference gps_lat/gps_lon, which were dropped by 20260519020000 and would raise an undefined-column error) where raw_gps_purge_after &lt;= now(); it preserves distance_from_location_meters, gps_accuracy_m, weight, target ids, user audit identity, and timestamps. It is service-only, has a fixed empty/private search_path, and returns an affected-row count for monitoring.
    3. `private.expire_pending_submissions()` locks/updates status from pending to expired where expires_at &lt;= now(); it never deletes submissions or verification_events and returns an affected-row count.
    4. `private.run_phase5_lifecycle_maintenance()` invokes those two bounded routines for cron. Revoke EXECUTE from PUBLIC/anon/authenticated; grant only the database service/cron owner required by the deployment convention.
    5. `private.maybe_auto_promote_pending()` is the D-60 stub: prominent comment, immediate no-op return, no locations insert, no submission publish update, and no schedule. It points to Phase 7's future pending-objection model.
    pgTAP must prove: selective raw-coordinate purge (gps_location nulled — the sole raw-coordinate column; derived evidence permanent); a LEGACY row seeded with a NULL raw_gps_purge_after gets a concrete deadline after the backfill and, once that deadline is in the past, is purged by purge_expired_verification_gps (proving the backfill closed the never-purged gap); immediate account-deletion purge remains intact; expired status transition with retained events; privilege denial to authenticated; and a >48h pending row remains pending after the stub.
  </action>
  <verify>
    <automated>test -f supabase/migrations/*phase5_lifecycle_jobs.sql &amp;&amp; test -f supabase/tests/phase5_lifecycle_jobs.test.sql &amp;&amp; grep -q "purge_expired_verification_gps" supabase/migrations/*phase5_lifecycle_jobs.sql &amp;&amp; grep -q "expire_pending_submissions" supabase/migrations/*phase5_lifecycle_jobs.sql &amp;&amp; grep -q "raw_gps_purge_after IS NULL" supabase/migrations/*phase5_lifecycle_jobs.sql &amp;&amp; grep -q "maybe_auto_promote" supabase/migrations/*phase5_lifecycle_jobs.sql &amp;&amp; ! grep -q "gps_lat" supabase/migrations/*phase5_lifecycle_jobs.sql</automated>
    <db-suite>supabase test db (phase5_lifecycle_jobs plus full inherited/new suite — BLOCKING before push)</db-suite>
  </verify>
  <acceptance_criteria>
    - A one-time backfill sets raw_gps_purge_after = timestamp + retention_days for every pre-existing row where it was NULL and gps_location is present, reading raw_gps_retention_days from app_config.
    - The expired raw coordinate (gps_location — the single raw-coordinate column) is nulled; derived evidence is unchanged; no reference to gps_lat/gps_lon (they do not exist live).
    - A legacy NULL-deadline row becomes purge-eligible after the backfill (pgTAP proves it is purged once its deadline passes).
    - Past-due pending rows become expired; rows/events are retained.
    - Authenticated clients cannot invoke private maintenance routines.
    - The 48h stub changes zero rows and is not scheduled.
    - Full pgTAP suite exits successfully on a Docker-capable or isolated non-production environment.
  </acceptance_criteria>
  <done>Measurable lifecycle behavior is implemented and tested, legacy NULL-deadline rows are backfilled so no raw GPS survives forever, and unmeasurable auto-promotion remains impossible.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] Live schema push, then separately authorize measurable lifecycle schedule</name>
  <read_first>
    - supabase/migrations/*_phase5_lifecycle_jobs.sql
    - supabase/tests/phase5_lifecycle_jobs.test.sql
    - docs/verification.md
  </read_first>
  <files>supabase/migrations/*_phase5_lifecycle_jobs.sql</files>
  <action>Do not run autonomously. Require the full pgTAP suite to pass. Obtain fresh authorization for `supabase db push` and verify all function privileges live plus that the legacy backfill populated pre-existing NULL deadlines. Then obtain a separate authorization for a pg_cron schedule invoking ONLY `private.run_phase5_lifecycle_maintenance`; confirm the job exists/enabled and no job references maybe_auto_promote_pending. Without the active purge/expiry schedule, D-40/D-59 remain implemented-but-not-operational and Phase 5 must not be called lifecycle-complete.</action>
  <acceptance_criteria>
    - Full pgTAP suite passed before push.
    - Schema push and cron schedule each had separate explicit authorization.
    - The legacy-deadline backfill is confirmed applied live (no verification_events row with gps_location present still has a NULL raw_gps_purge_after).
    - Live cron metadata shows the measurable maintenance job active and no auto-promotion job.
    - A monitored dry/sample run reports bounded affected-row counts without publishing anything.
  </acceptance_criteria>
  <what-built>Private tested purge/expiry maintenance, a one-time legacy-deadline backfill, and a disabled promotion stub. The schema push and operational cron schedule are distinct approval boundaries.</what-built>
  <how-to-verify>
    1. Review migration/tests and full pgTAP output.
    2. Authorize/push schema; verify private routine privileges and that the backfill populated legacy NULL deadlines.
    3. Separately authorize the cron schedule for run_phase5_lifecycle_maintenance.
    4. Confirm cron.job has the maintenance job and no auto-promote job; inspect the first run result.
  </how-to-verify>
  <resume-signal>Report "pushed + lifecycle scheduled" with verification output, or describe the exact failure/deferment.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cron/service → sensitive evidence | Maintenance may erase only expired raw GPS, never derived audit evidence |
| scheduled job → submissions | Expiry is measurable; publication without an objection signal is forbidden |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-22 | Elevation of Privilege | false auto-promotion | mitigate | private no-op stub, no grants, no schedule, pgTAP zero-change proof |
| T-05-23 | Tampering | second publish path | mitigate | only verify_location publishes; lifecycle migration contains no publication DML |
| T-05-24 | Information Disclosure | raw GPS retained forever | mitigate | legacy NULL-deadline backfill + reviewed purge deadline + active private scheduled purge + account-deletion immediate purge |
| T-05-25 | Repudiation | purge destroys audit math | mitigate | only the raw coordinate (gps_location — the sole raw-coordinate column) is nulled; derived distance/accuracy/weight and event identity remain |
</threat_model>

<verification>
- Full inherited + Phase 5 pgTAP suite passes before live push.
- Legacy NULL-deadline rows are backfilled; no gps_location-bearing row keeps a NULL raw_gps_purge_after.
- Live function ACLs are service-only.
- Active cron schedules purge/expiry only; disabled stub has no schedule.
- Device/live behavior is not inferred from static SQL.
</verification>

<success_criteria>
- Raw GPS retention (including legacy pre-phase rows) and 14-day expiry are operational, not merely documented.
- The 48-hour route exists only as documented fail-closed intent and cannot publish.
</success_criteria>

<output>
Create `.planning/phases/05-trust-engine-verification/05-06-SUMMARY.md` when done.
</output>
