---
phase: 01-foundation-scaffold
plan: 01-02
task: 5-review-gate
status: ready
updated_at: 2026-06-24
---

# Handoff — Plan 01-02, Task 5 (review gate) — Antigravity CLI Broken

## Resume Phrase
"continue Gotta Go setup"

## Current Status
- Task 4 (smoke test): DONE — `[smoke] app_config rows: 6` confirmed
- Task 5 (review gate): READY — Antigravity CLI has been repaired and migrated to the native `agy` CLI

## Antigravity CLI Issue (RESOLVED)
- **Resolved**: The legacy npm `@google/gemini-cli` has been retired. We have updated the global wrapper scripts `antigravity.cmd` and `antigravity.ps1` to route to the native Google Antigravity CLI (`agy.exe`) installed on the system, resolving the `IneligibleTierError` authentication error.
- **Action Required by User**: If running `antigravity` for the first time prompts you, run `agy` in your terminal once to complete the browser OAuth flow if needed.

## Skill Conflicts (non-blocking)
- `supabase-postgres-best-practices`: project skill overrides global (expected behavior)
- `supabase`: project skill overrides global (expected behavior)

## Review Queue (18 files ready)
`app/.claude/review-queue.txt` — all Plan 01-02 files. SHAs:
- BASE_SHA: `41fcb7c13fb533ed5cf10537973a7e9151c4600f`
- HEAD_SHA: `d73aa9189cdeb7a15cb5d2742b23efef377a1f7f`

## After Antigravity Unblocked
1. Run Antigravity review → save to `app/.claude/antigravity-review-latest.md`
2. Generate Codex prompt via `/codex-prompt` → paste into Codex app
3. Resolve all BLOCK/REQUEST CHANGES
4. Commit Task 5 completion

---

# Original Handoff — Plan 01-02, Task 4 (smoke test) + Tool Setup

## Resume Phrase
"continue Gotta Go setup"

---

## EAS Build — SUCCEEDED
Build ID: `2acfc856-104d-4cf4-949c-2c3060d6b849`
APK URL: https://expo.dev/accounts/mrsaintarmand/projects/gotta-go/builds/2acfc856-104d-4cf4-949c-2c3060d6b849

**To complete Task 4 (user does these steps):**
1. Download APK from URL above
2. Boot Pixel 8 AVD (`C:\Users\mrsai\AppData\Local\Android\Sdk\emulator\emulator.exe -avd Pixel_8`)
3. Drag APK onto running emulator to install
4. Run: `npx expo start --dev-client` from `C:\Users\mrsai\Gotta Go\app\`
5. Open dev client on emulator, connect to Metro
6. Confirm smoke test: Metro console shows `[smoke] app_config rows: 6` with no `[smoke] supabase error:`

---

## What Changed This Session

### Dependency Fixes (caused first build failure)
- `android.package: 'com.watchthegap.gottago'` added to `app.config.ts`
- `@sentry/react-native` removed (not used yet, wrong version for Expo 55, belongs Phase 9)
- `react-native-turbo-mock-location-detector` removed (wrong version for New Arch — need v4.x when ready)
- `@react-native-async-storage/async-storage` downgraded 3.0.3 → 2.2.0 (Expo 55 requires)
- Minor expo packages patched: `expo`, `expo-camera`, `expo-image`, `expo-router`
- `jest.setup.ts`: removed orphaned mock for turbo-mock-location-detector
- All 2 tests still passing

### Tool Updates
- **Superpowers**: 5.1.0 → 6.0.3 (from `obra/superpowers` via `superpowers-dev` marketplace)
- **Metaswarm**: Newly installed → 0.12.0 (from `dsifry/metaswarm-marketplace`)
- **TDD Guard**: Hook runs `npx tdd-guard@latest` = v1.6.9 at runtime (no action needed)
- **GSD model overrides** set: opus→`claude-opus-4-8`, sonnet→`claude-sonnet-4-6`, haiku→`claude-haiku-4-5-20251001`
- **Superpowers discipline** hardcoded in `CLAUDE.md` (trigger-to-skill table)

### Superpowers v6 Breaking Changes to Know
- SDD review: one reviewer per task (was two), single whole-branch review at completion
- Antigravity is now a first-class harness
- SDD scratch files: `.superpowers/sdd/` (was `.git/`)

---

## Incomplete Tool Setup — Do These Next Session

### 1. BEADS CLI (user must run manually)
```
! curl -sSL https://raw.githubusercontent.com/steveyegge/beads/main/scripts/install.sh | bash
```
Then verify: `! bd status`

### 2. Metaswarm Project Setup
After BEADS is installed, run Metaswarm setup in Claude Code from `C:\Users\mrsai\Gotta Go`:
```
/setup
```
Answers for the setup prompts:
- Coverage threshold: **100%**
- External AI tools: **No** (Codex/Gemini optional later)
- Visual review: **No** (no web framework)
- CI pipeline: **No** (no GitHub remote yet)
- Git hooks: **No** (GSD handles this)

### 3. TDD Guard — DEFERRED (user decision: wait for jest-expo@56)
All versions of `tdd-guard-jest` (0.1.0–0.1.4) require `jest@>=30.0.5`. Project is pinned to jest@29.7.0.
**Decision:** Wait for jest-expo@56 to support jest@30, then re-run `/tdd-guard:setup`.
AI hook validation still active in the meantime.

**When jest-expo@56 ships:**
1. `npx expo install jest-expo` (upgrades jest-expo + jest together)
2. Run `/tdd-guard:setup` — will install `tdd-guard-jest` and configure `jest.config.js`
3. Create `.claude/tdd-guard/data/instructions.md` with GPS distance, trust score, PostGIS rules

### 4. CLAUDE.md Rules — ✓ DONE (2026-06-22)
Added to `C:\Users\mrsai\Gotta Go\CLAUDE.md`:
- TDD Guard scope table and enforcement rules
- Metaswarm workflow table (when to invoke which skill)
- Full workflow integration diagram (Brainstorm → Design Gate → Plan Gate → TDD → Review Gate → Self-reflect)
- Non-negotiables block for all agents

---

## Schema Drift — ✓ DONE (2026-06-22)
All active docs updated: `bathroom_locations` → `locations`, `profiles` → `users`, `location` column → `coordinates`.
Files updated: docs/schema-contract.md, SPEC.md, ROADMAP.md, research/ARCHITECTURE.md, research/STACK.md, research/PITFALLS.md, .claude/skills/rls_security_guard.md, 01-02-PLAN.md, 01-VALIDATION.md.
Historical Phase 01 plan files (01-01-PLAN.md, 01-RESEARCH.md) left as archived — they document the discovery, not instructions.

## Phase 999.1 — Travel Language Phrases (design decision recorded)
**Decision:** GPS auto-detection as default, with manual language override.
User lands in Japan → auto-detects Japanese. Tap to switch to another country/language.
Build when Phase 999.1 is scheduled.

---

## Task 5 — Review Gate (after smoke test passes)

Files in review scope (Plan 01-02):
- `app/app.config.ts`
- `app/src/app/` (9 placeholder screens)
- `app/src/lib/supabase.ts`
- `app/src/lib/__tests__/supabase.test.ts`
- `app/jest.config.js`, `app/jest.setup.ts`, `app/tsconfig.json`
- `app/eas.json`
- `app/.env.local` (excluded from review — secrets)

Review queue: `app/.claude/review-queue.txt`

**To trigger review gate:**
1. Invoke `superpowers:requesting-code-review`
2. Get BASE_SHA and HEAD_SHA from git
3. Run `/antigravity-review` in a new Claude session
4. Paste codex prompt into Codex app
5. Resolve all BLOCK/REQUEST CHANGES before committing

---

## Key Facts (always needed)
- Supabase project ref: `ebmzhjmmtmldhrojkdqw`
- EAS project: `@mrsaintarmand/gotta-go` (ID: `8c002e93-229f-4140-845f-eb3a9625b545`)
- Platform: Android only (no Apple Developer account; iOS deferred)
- `app.config.ts` owns config; `app.json` kept alongside until EAS build confirmed stable
- jest@29.7.0 is PINNED — do not upgrade (jest-expo@55 incompatible with jest@30)
- `src/app/` is routes dir — Expo Router auto-discovers it
- Live schema uses `locations`/`users` (NOT `bathroom_locations`/`profiles`)
- adb path: `C:\Users\mrsai\AppData\Local\Android\Sdk\platform-tools\adb.exe` (not in PATH)
- Android SDK: `C:\Users\mrsai\AppData\Local\Android\Sdk`
