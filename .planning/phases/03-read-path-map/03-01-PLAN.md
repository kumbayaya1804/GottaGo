---
phase: 03-read-path-map
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260704010000_phase3_suppressed_at.sql
  - supabase/migrations/20260704010001_phase3_max_pins_config.sql
  - supabase/migrations/20260704010002_phase3_search_rpcs.sql
  - supabase/migrations/20260704010003_phase3_family_mode_rpc.sql
  - supabase/seed.sql
  - supabase/tests/phase3_read_rpcs.test.sql
  - app/src/lib/database.types.ts
autonomous: true
requirements:
  - REQ-MAP
  - REQ-SEARCH
  - REQ-FILTER
  - REQ-EMERGENCY
  - REQ-DETAIL
  - REQ-FAMILY
  - REQ-FAMILY-TOGGLE

must_haves:
  truths:
    - "search_locations_bbox returns only published (non-deleted, non-suppressed, non-shadowbanned) locations inside a viewport"
    - "search_locations_bbox keeps the coordinates geography column raw on the LHS of && and casts the envelope, so the GiST index is used (no seq scan per pan/zoom)"
    - "search_locations_bbox does not crash when the viewport crosses the antimeridian (min_lng > max_lng) — it splits into two unioned envelopes"
    - "Every data-dependent bbox filter (open_now, wheelchair, changing table, cleanliness/high-conf) INCLUDES rows with missing underlying data by default (D-08), never hiding them when the filter is active"
    - "search_locations_nearby returns nearest locations ordered by ST_Distance in meters"
    - "get_location_detail returns a public-safe row that never contains access_instructions, shadowban_status, deleted_at, or suppressed_at"
    - "get_location_detail accepts optional user_lat/user_lng and returns distance_m (ST_Distance, meters) when they are supplied and null otherwise — the single server-side source for the LocationDetail peek-tier distance"
    - "A family_mode=true user cannot see access_sensitivity='sensitive' rows in any RPC result, enforced server-side via auth.uid()"
    - "update_profile updates display_name and family_mode independently via coalesce — a family-mode-only call leaves display_name unchanged, and a display-name-only call leaves family_mode unchanged"
    - "locations.suppressed_at column exists and the moderation-filtered partial GiST index covers it"
    - "app_config has a max_pins_per_viewport row that the bbox RPC reads server-side and uses to clamp its effective LIMIT — a client-supplied max_pins can never exceed the app_config value"
  artifacts:
    - path: "supabase/migrations/20260704010000_phase3_suppressed_at.sql"
      provides: "locations.suppressed_at column + rebuilt partial index + RLS select policy update"
      contains: "suppressed_at"
    - path: "supabase/migrations/20260704010002_phase3_search_rpcs.sql"
      provides: "search_locations_bbox, search_locations_nearby, get_location_detail (with optional user coords → distance_m)"
      contains: "create or replace function public.search_locations_bbox"
    - path: "supabase/migrations/20260704010003_phase3_family_mode_rpc.sql"
      provides: "update_profile extended with family_mode param, coalesce semantics on both columns"
      contains: "new_family_mode"
    - path: "supabase/migrations/20260704010001_phase3_max_pins_config.sql"
      provides: "max_pins_per_viewport app_config row"
      contains: "max_pins_per_viewport"
    - path: "supabase/seed.sql"
      provides: "dev-only seed locations (~15 rows incl. sensitive/suppressed/shadowbanned/null-data/antimeridian fixtures)"
      contains: "insert into"
    - path: "app/src/lib/database.types.ts"
      provides: "regenerated types incl. new RPCs + suppressed_at"
      contains: "search_locations_bbox"
    - path: "supabase/tests/phase3_read_rpcs.test.sql"
      provides: "pgTAP correctness assertions for moderation/family_mode/access-code/nearest-N/detail-distance/config-cap/null-include/antimeridian/coalesce"
      contains: "family_mode"
  key_links:
    - from: "search_locations_bbox / _nearby / get_location_detail"
      to: "public.users.family_mode"
      via: "auth.uid() server-side read"
      pattern: "select family_mode into .* from public.users where id = auth.uid"
    - from: "search_locations_bbox"
      to: "app_config.max_pins_per_viewport"
      via: "server-side read + LEAST() clamp of the caller max_pins"
      pattern: "least\\("
    - from: "get_location_detail"
      to: "st_distance(coordinates, user point)"
      via: "optional user_lat/user_lng → distance_m"
      pattern: "st_distance.*user_lng.*user_lat|distance_m"
---

<objective>
Build the entire Phase 3 database read path: the three SECURITY DEFINER read RPCs (`search_locations_bbox`, `search_locations_nearby`, `get_location_detail`), the `locations.suppressed_at` moderation column, the `max_pins_per_viewport` tunable config row, the `update_profile` family_mode extension, and a dev-only seed dataset. All correctness-critical filtering (four-clause moderation + family_mode + tag joins) AND the LocationDetail distance computation live here — the client is a thin renderer that must never re-implement any of it, including distance.

Purpose: Every downstream Phase 3 screen (Map, Nearby, LocationDetail, Filters) reads through these RPCs. The moderation/family_mode filters (ROADMAP SC2/SC3/SC8) are only trustworthy if enforced in SQL, and the LocationDetail distance is only correct if computed server-side by the same ST_Distance path Nearby uses. This plan also produces the regenerated `database.types.ts` that all client hooks (03-02) import.
Output: 4 migrations + dev seed + pgTAP tests + regenerated types, pushed to the live Supabase project.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-read-path-map/03-RESEARCH.md
@.planning/phases/03-read-path-map/03-PATTERNS.md
@.planning/phases/03-read-path-map/03-CONTEXT.md

# Canonical schema + analog migrations (read before writing SQL)
@docs/schema-contract.md
@supabase/migrations/20260624000002_ratings_privacy_fix.sql
@supabase/migrations/20260627000004_profile_rpcs.sql

<interfaces>
<!-- Confirmed live schema for public.locations (migration 20260519010000_remote_schema.sql). -->
<!-- Columns: id, name text NOT NULL, address text, coordinates geography NOT NULL,
     policy_tag text, access_sensitivity text, hours jsonb, is_open_now boolean,
     confidence_tier text, confidence_score text, verification_count integer default 0,
     last_verified_at timestamptz, chill_spot boolean default false,
     shadowban_status boolean default false, deleted_at timestamptz.
     NO `status` column exists (OQ-2 → publish state is flag-based only).
     `suppressed_at` does NOT exist yet — this plan adds it. -->

<!-- Existing partial index to REBUILD:
     idx_locations_coordinates_active = gist(coordinates)
       WHERE deleted_at is null AND shadowban_status = false  -->

<!-- Existing RLS select policy on locations (line 93):
       using (deleted_at is null and shadowban_status = false) -->

<!-- Analog RPC filter block from get_locations_in_radius (20260624000002): reuse its tag-join VOCAB
     (accessibility/wheelchair, amenity/changing_table) + geography ST_Distance meters expression
     (get_location_detail reuses the same distance expression). BUT do NOT copy its filter clauses
     verbatim: get_locations_in_radius's `(not filter_x or <match>)` EXCLUDES rows with null underlying
     data when the filter is active, which violates D-08 — Phase 3 adds an explicit "no data for this
     dimension" escape branch to each data-dependent filter (see Task 2 action, Antigravity MAJOR). Also do
     NOT copy its `l.coordinates::geometry` bbox cast — that casts the indexed column and bypasses the GiST
     index; cast the ENVELOPE instead (Antigravity MAJOR). -->

<!-- Analog write RPC to EXTEND (20260627000004 update_profile):
     current signature update_profile(new_display_name text) returns void;
     revokes from public AND anon, grants to authenticated. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add suppressed_at column, rebuild moderation index, seed max_pins config</name>
  <files>supabase/migrations/20260704010000_phase3_suppressed_at.sql, supabase/migrations/20260704010001_phase3_max_pins_config.sql</files>
  <read_first>
    - supabase/migrations/20260519010000_remote_schema.sql (locations DDL lines 50-93 — column list, idx_locations_coordinates_active definition, RLS select policy)
    - supabase/migrations/20260519020000_fix_schema.sql (app_config INSERT pattern + add-column shape)
    - .planning/phases/03-read-path-map/03-PATTERNS.md (§suppressed_at, §max_pins_config)
  </read_first>
  <action>
    In `20260704010000_phase3_suppressed_at.sql`: `alter table public.locations add column suppressed_at timestamptz;` (nullable, no default). Then rebuild the moderation index so it stays index-backed: `drop index if exists idx_locations_coordinates_active;` and recreate it as `gist(coordinates) where deleted_at is null and shadowban_status = false and suppressed_at is null`. Also update the existing locations RLS SELECT policy (belt-and-suspenders for direct table reads) so its `using` clause becomes `deleted_at is null and shadowban_status = false and suppressed_at is null` — drop and recreate the policy by its existing name from the remote_schema migration.
    PLAN-CHECKER FINDING (2026-07-05, not from either external reviewer — caught by gsd-plan-checker on this revision pass): the `locations_select_public` RLS policy (migration `20260519010000_remote_schema.sql:91`) currently grants direct SELECT on the base `locations` table to `anon`/`authenticated` with no `access_sensitivity`/`family_mode` filter and no column restriction — meaning a caller could bypass all three new RPCs entirely via `.from('locations').select('*')` and (a) see `access_sensitivity='sensitive'` rows regardless of family_mode, and (b) read `access_instructions` (the door PIN) and `shadowban_status` directly, defeating every protection Task 2 builds into the RPCs. This project has fixed this exact bug class twice before on other tables (`ratings` in `20260624000002_ratings_privacy_fix.sql`, `availability_flags` in `20260519030000_fix_rls.sql`) using the same remedy: revoke base-table SELECT from both roles, force all reads through a safe function. Apply it here too, in this same migration: add `revoke select on public.locations from anon;` and `revoke select on public.locations from authenticated;` after the RLS policy update. This makes the three new RPCs in Task 2 (all `security definer`, which bypasses RLS by running as owner) the ONLY read path — direct PostgREST table queries return zero rows/permission-denied for both roles.
    In `20260704010001_phase3_max_pins_config.sql`: insert exactly one app_config row `('max_pins_per_viewport', '200', 'Max locations returned per bbox/viewport query (D-32, admin-tunable)')` following the existing TEXT-value convention. The anon SELECT policy `app_config_select_anon` already exists — do not add a grant.
    Do NOT put any seed location rows in a numbered migration (Pitfall 6 — that would reach production). Seed goes in seed.sql (Task 3).
  </action>
  <verify>
    <automated>grep -v '^--' supabase/migrations/20260704010000_phase3_suppressed_at.sql | grep -c "suppressed_at is null" | grep -qv '^0$' && grep -v '^--' supabase/migrations/20260704010001_phase3_max_pins_config.sql | grep -c "max_pins_per_viewport" && grep -v '^--' supabase/migrations/20260704010000_phase3_suppressed_at.sql | grep -Eiq "revoke select on public\.locations from anon" && grep -v '^--' supabase/migrations/20260704010000_phase3_suppressed_at.sql | grep -Eiq "revoke select on public\.locations from authenticated"</automated>
  </verify>
  <done>Migration adds nullable suppressed_at, recreated partial index includes `suppressed_at is null`, RLS select policy updated, base-table SELECT revoked from anon+authenticated (all reads forced through Task 2's RPCs), and app_config gets the max_pins_per_viewport row. No location INSERTs in any numbered migration.</done>
  <acceptance_criteria>
    - `suppressed_at timestamptz` added to public.locations (nullable, no default).
    - idx_locations_coordinates_active recreated with `and suppressed_at is null` in its WHERE.
    - locations RLS SELECT policy `using` clause now excludes suppressed rows.
    - `revoke select on public.locations from anon;` and `revoke select on public.locations from authenticated;` both present — a direct `.from('locations').select()` call returns zero rows/permission-denied for both roles (pgTAP case in Task 3 asserts this).
    - Exactly one new app_config row keyed `max_pins_per_viewport` = '200' (TEXT).
    - Zero location-row INSERTs present in either migration file.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create the three read RPCs and extend update_profile with family_mode</name>
  <files>supabase/migrations/20260704010002_phase3_search_rpcs.sql, supabase/migrations/20260704010003_phase3_family_mode_rpc.sql</files>
  <read_first>
    - supabase/migrations/20260624000002_ratings_privacy_fix.sql (get_locations_in_radius — tag-join vocab + ST_Distance meters expression + grant style to mirror; do NOT copy `select l.*`/`setof locations`, its index-bypassing `coordinates::geometry` bbox cast, or its null-excluding filter clauses)
    - supabase/migrations/20260627000004_profile_rpcs.sql (update_profile signature + revoke-from-anon convention)
    - .planning/phases/03-read-path-map/03-RESEARCH.md (§Pattern 2 bbox cast [NOTE: its `coordinates::geometry` guidance is superseded by the index-friendly envelope cast below], §Pattern 4 family_mode, §Pattern 5 hardening, §Code Examples bbox skeleton, Pitfalls 2/4/5, OQ-2/OQ-3 resolutions)
    - .planning/phases/03-read-path-map/03-PATTERNS.md (§search_rpcs, §family_mode_rpc)
    - .planning/phases/03-read-path-map/03-REVIEWS.md (Antigravity MAJOR findings on bbox cast / D-08 null-include / antimeridian; Codex MEDIUM on the app_config-driven cap; and the CRITICAL/HIGH update_profile coalesce fix)
  </read_first>
  <action>
    In `20260704010002_phase3_search_rpcs.sql`, create three functions, all `language plpgsql security definer stable set search_path = public`, each reading the caller's family_mode server-side (declare `v_family boolean := false`; `if auth.uid() is not null then select family_mode into v_family from public.users where id = auth.uid(); end if;`) — never accept family_mode as a parameter (Pitfall 3).
    (a) `search_locations_bbox(min_lng numeric, min_lat numeric, max_lng numeric, max_lat numeric, filter_open_now boolean default false, filter_chill_spot boolean default false, filter_wheelchair boolean default false, filter_changing boolean default false, filter_high_conf boolean default false, max_pins integer default 200)` returning an explicit table: `id uuid, name text, lat double precision, lng double precision, policy_tag text, confidence_tier text, verification_count integer, last_verified_at timestamptz, is_open_now boolean, chill_spot boolean`.
    INDEX-FRIENDLY BBOX CAST (Antigravity MAJOR — do NOT cast the column): keep `l.coordinates` raw (geography) and cast the ENVELOPE instead, so the GiST index on `coordinates` is used — `l.coordinates && st_makeenvelope(min_lng,min_lat,max_lng,max_lat,4326)::geography`. Never write `l.coordinates::geometry && st_makeenvelope(...)` (that casts the indexed column and forces a sequential scan every pan/zoom). This supersedes RESEARCH §Pattern 2/Pitfall 5's `coordinates::geometry` guidance, which is wrong for index usage.
    ANTIMERIDIAN GUARD (Antigravity MAJOR): `st_makeenvelope` raises when `xmin > xmax` (viewport panned across longitude 180, i.e. `min_lng > max_lng`). Branch the body on `if min_lng > max_lng then` use a split two-envelope spatial predicate `(l.coordinates && st_makeenvelope(min_lng,min_lat,180,max_lat,4326)::geography or l.coordinates && st_makeenvelope(-180,min_lat,max_lng,max_lat,4326)::geography)`; `else` use the single-envelope predicate above; `end if`. Both branches apply the IDENTICAL moderation + family_mode + null-include filters and the same order/limit below. Implement via a plpgsql IF/ELSE with two `return query` statements (no dynamic SQL — CLAUDE.md), or an equivalent CASE-guarded predicate where only the valid `st_makeenvelope` branch is ever evaluated so the crossing call is never reached.
    MODERATION + FAMILY: four-clause moderation (`l.deleted_at is null and l.suppressed_at is null and l.shadowban_status = false`) plus `and (not v_family or l.access_sensitivity is distinct from 'sensitive')`.
    D-08 NULL-INCLUDE FILTERS (Antigravity MAJOR — a location with missing underlying data must be INCLUDED when the filter is active, never hidden): each data-dependent filter needs an explicit "no data for this dimension" escape branch, NOT the plain `(not filter_x or <match>)` from get_locations_in_radius:
      - Currently Open: `(not filter_open_now or l.is_open_now is not false)` — includes null + true; excludes only explicit false.
      - Wheelchair: `(not filter_wheelchair or not exists (select 1 from public.tags t where t.location_id = l.id and t.key='accessibility') or exists (select 1 from public.tags t where t.location_id = l.id and t.key='accessibility' and t.value='wheelchair'))` — a location with NO accessibility tag at all is included.
      - Changing Table: same shape with `key='amenity'` / `value='changing_table'`.
      - Cleanliness / High confidence: `(not filter_high_conf or l.confidence_tier is null or l.confidence_tier = 'High')`.
      - Chill Spot is NOT null-include (curated boolean column with default false; D-08 lists only Open/Wheelchair/Changing/Cleanliness): keep `(not filter_chill_spot or l.chill_spot = true)`.
    ORDER/LIMIT + MAX_PINS FROM CONFIG (Codex MEDIUM): the effective pin cap is server-authoritative — declare `v_max_pins integer;` and `select value::integer into v_max_pins from public.app_config where key = 'max_pins_per_viewport';` then `v_max_pins := coalesce(v_max_pins, 200);`. Order by `case l.confidence_tier when 'High' then 3 when 'Medium' then 2 when 'Low' then 1 else 0 end desc, l.verification_count desc` (NOT confidence_score — it is TEXT, Pitfall 2) and `limit least(max_pins, v_max_pins)` so a client-supplied `max_pins` can only narrow, never exceed the config cap (defense in depth). Emit lat/lng via `st_y(l.coordinates::geometry)`/`st_x(l.coordinates::geometry)` (casting for st_x/st_y is fine — it is a projection of the output, not the index predicate).
    (b) `search_locations_nearby(user_lat numeric, user_lng numeric, result_limit integer default 20, <same filter params>)` returning the same columns PLUS `distance_m double precision`. Compute `st_distance(l.coordinates::geography, st_setsrid(st_makepoint(user_lng,user_lat),4326)::geography) as distance_m`; order by the `<->` KNN operator on geography ascending; `limit result_limit`. Same four-clause + family_mode + D-08 null-include filters (no envelope/antimeridian concern — this is a point-radius/KNN query, not a bbox). This is the RPC ROADMAP SC4 / REQ-EMERGENCY consumes (UI is Phase 8; only the RPC ships here).
    (c) `get_location_detail(location_id uuid, user_lat numeric default null, user_lng numeric default null)` returning a single public-safe row: `id, name, address, lat, lng, policy_tag, confidence_tier, verification_count, last_verified_at, is_open_now, chill_spot, hours jsonb, distance_m double precision`. Compute `distance_m` with the SAME expression as search_locations_nearby, guarded for the anon/no-coords case: `case when user_lat is null or user_lng is null then null else st_distance(l.coordinates::geography, st_setsrid(st_makepoint(user_lng,user_lat),4326)::geography) end as distance_m`. This is the ONLY server-side source of the LocationDetail distance line — both the Map→pin→detail and Nearby→row→detail paths pass the caller's current coords here; the client never fabricates distance. Apply the same four-clause + family_mode filter (a suppressed/sensitive-hidden row returns zero rows). Per OQ-3/Pitfall 4: DO NOT return `access_instructions` at all in Phase 3 (reveal UX is Phase 8), and never return `shadowban_status`/`deleted_at`/`suppressed_at`. Because user_lat/user_lng are optional with null defaults, anon callers that omit them still get the row (distance_m null) — no signature break for callers who only pass an id.
    For all three: `revoke execute on function public.<fn>(<arg types>) from public;` then `grant execute ... to anon;` and `grant execute ... to authenticated;` (public reads — anon allowed, D-24). For get_location_detail the `<arg types>` is now `(uuid, numeric, numeric)`.
    In `20260704010003_phase3_family_mode_rpc.sql`: replace update_profile with the extended signature. Because adding a parameter changes the signature, first `drop function if exists public.update_profile(text);` then `create or replace function public.update_profile(new_display_name text default null, new_family_mode boolean default null) returns void language plpgsql security definer set search_path = public` — BOTH params default null so a family-mode-only call (03-02 `setFamilyMode` sends only `new_family_mode`) AND a display-name-only call (Phase 2 sends only `new_display_name`) are each valid. Body keeps the `if auth.uid() is null then raise exception 'not authenticated'; end if;` guard, then updates BOTH columns via coalesce so each call touches only what it sends: `set display_name = coalesce(new_display_name, display_name), family_mode = coalesce(new_family_mode, family_mode), updated_at = now() where id = auth.uid();`. CRITICAL (both reviewers): do NOT write `display_name = new_display_name` unconditionally — that nulls the caller's display name on every family-mode-only toggle (`update_profile(null, true)`). Re-issue `revoke execute ... from public; revoke execute ... from anon; grant execute ... to authenticated;` against the NEW arg list `(text, boolean)`.
  </action>
  <verify>
    <automated>grep -v '^--' supabase/migrations/20260704010002_phase3_search_rpcs.sql | grep -Eiq "coordinates && st_makeenvelope" && ! grep -v '^--' supabase/migrations/20260704010002_phase3_search_rpcs.sql | grep -Eiq "coordinates::geometry && st_makeenvelope" && grep -v '^--' supabase/migrations/20260704010002_phase3_search_rpcs.sql | grep -Eiq "least\(" && grep -v '^--' supabase/migrations/20260704010002_phase3_search_rpcs.sql | grep -Eq "suppressed_at is null" && grep -v '^--' supabase/migrations/20260704010002_phase3_search_rpcs.sql | grep -Eq "access_sensitivity is distinct from 'sensitive'" && grep -v '^--' supabase/migrations/20260704010002_phase3_search_rpcs.sql | grep -Eq "get_location_detail\(location_id uuid, user_lat" && ! grep -qi "setof locations\|select l\.\*\|access_instructions" supabase/migrations/20260704010002_phase3_search_rpcs.sql && grep -v '^--' supabase/migrations/20260704010003_phase3_family_mode_rpc.sql | grep -Eiq "display_name = coalesce\(new_display_name" && grep -q "new_family_mode" supabase/migrations/20260704010003_phase3_family_mode_rpc.sql</automated>
  </verify>
  <done>Three read RPCs use explicit public column lists, the four-clause moderation filter, server-side family_mode via auth.uid(), the index-friendly envelope cast, the antimeridian split, D-08 null-include filters, and a config-driven `least(max_pins, app_config cap)` limit; get_location_detail omits access_instructions and returns a server-computed distance_m from optional user coords; update_profile updates display_name and family_mode independently via coalesce. No `setof locations`/`select l.*`/`access_instructions` anywhere.</done>
  <acceptance_criteria>
    - search_locations_bbox uses the INDEX-FRIENDLY cast `l.coordinates && st_makeenvelope(...)::geography` (envelope cast to geography, column left raw) and NEVER `l.coordinates::geometry && st_makeenvelope(...)`; orders by CASE tier / verification_count (never confidence_score).
    - search_locations_bbox handles antimeridian crossing: when `min_lng > max_lng` it unions a `(min_lng..180)` and a `(-180..max_lng)` envelope instead of calling st_makeenvelope with xmin > xmax.
    - Each data-dependent bbox filter is null-include per D-08: open_now uses `is_open_now is not false`; wheelchair/changing add a "no tag for this dimension" escape branch; high_conf allows `confidence_tier is null`. Chill Spot stays a plain boolean match (not null-include).
    - search_locations_bbox resolves its pin cap from `public.app_config.max_pins_per_viewport` server-side and applies `limit least(max_pins, v_max_pins)` so a client value can only narrow, never exceed the config cap.
    - search_locations_nearby returns distance_m via ST_Distance (meters) and orders by `<->` KNN; same D-08 null-include filters.
    - get_location_detail signature is `(uuid, numeric default null, numeric default null)` and returns `distance_m` computed via the same ST_Distance expression (null when coords omitted); grants target anon + authenticated on the `(uuid, numeric, numeric)` arg list.
    - All three RPCs contain `deleted_at is null AND suppressed_at is null AND shadowban_status = false` and `(not v_family or access_sensitivity is distinct from 'sensitive')`.
    - family_mode is read from `public.users` via `auth.uid()`, never a function parameter.
    - No occurrence of `setof locations`, `select l.*`, or `access_instructions` in the RPC file.
    - update_profile new signature is `(text default null, boolean default null)` with BOTH columns updated via `coalesce(new_x, x)` (a family-mode-only call leaves display_name unchanged); old `(text)` dropped; grants target authenticated only.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Dev seed, schema push, regenerate types, pgTAP correctness tests</name>
  <files>supabase/seed.sql, supabase/tests/phase3_read_rpcs.test.sql, app/src/lib/database.types.ts</files>
  <read_first>
    - supabase/seed.sql (current stub — append seed rows here; loaded ONLY on `db reset` per config.toml db.seed.sql_paths, never on `db push` → this IS the dev-only guard, Pitfall 6)
    - supabase/config.toml (db.seed section — confirms seed.sql runs on local reset only)
    - .planning/phases/03-read-path-map/03-VALIDATION.md (SQL/pgTAP assertions list, Wave 0 fixtures)
    - .planning/phases/03-read-path-map/03-RESEARCH.md (§Runtime State Inventory — database.types.ts regeneration requirement)
  </read_first>
  <action>
    Seed (supabase/seed.sql): append ~15 fake published locations centered on Eugene, OR (approx lat 44.05, lng -123.09 — Claude's discretion per D-31, matches the project Eugene density note) using `insert into public.locations (name, coordinates, policy_tag, access_sensitivity, is_open_now, confidence_tier, verification_count, last_verified_at, chill_spot) values (...)` with `st_setsrid(st_makepoint(lng,lat),4326)::geography`. Include deliberate moderation fixtures: at least one row with `access_sensitivity = 'sensitive'` (the exact sentinel — A2), one with `suppressed_at = now()`, one with `shadowban_status = true`, one with `deleted_at = now()`, and a spread of confidence_tier ('High'/'Medium'/'Low'). Add D-08 null-data fixtures: a row with NO tags, a row with `confidence_tier = null`, and a row with `is_open_now = null` (each must survive its corresponding active filter). Also include a distinct pair of rows straddling longitude 180 (e.g. one near lng 179.9, one near -179.9) so the antimeridian pgTAP case has data on both sides — Claude's discretion on exact coords. Because seed.sql only loads on local `db reset`, no production guard flag is needed — state this in a header comment.
    Push (BLOCKING): run `supabase db push` to apply the four Task 1/2 migrations to the live project. Non-TTY: export `SUPABASE_ACCESS_TOKEN` first. If the CLI blocks on an interactive confirmation that cannot be suppressed with `--yes`/env, STOP and surface the exact command for manual run rather than guessing. This push MUST happen before types regeneration and before any downstream plan verification (build/type checks pass without it because types come from database.types.ts, creating a false-positive — ROADMAP SC9 / schema_push_requirement).
    Regenerate types: run `supabase gen types typescript --linked > app/src/lib/database.types.ts` (or the MCP equivalent) so the new RPCs + suppressed_at appear; commit the regenerated file. Confirm get_location_detail's generated Args include user_lat/user_lng and its Returns include distance_m, and update_profile's generated Args are `{ new_display_name?: string | null; new_family_mode?: boolean | null }`.
    pgTAP (supabase/tests/phase3_read_rpcs.test.sql): establish a pgTAP test file (enable the pgtap extension in the test transaction if not already) asserting, against local seed data: (1) shadowbanned/suppressed/deleted rows are ABSENT from search_locations_bbox and search_locations_nearby results; (2) a family_mode=true user (set via impersonating auth.uid or a helper) gets zero `access_sensitivity='sensitive'` rows while an anon/family_mode=false caller sees them; (3) get_location_detail for an anon caller returns a row WITHOUT an access_instructions column; (4) search_locations_nearby returns rows ordered by ascending distance_m; (5) updating `app_config.max_pins_per_viewport` (e.g. to 2) and re-calling search_locations_bbox against a larger seed set changes the returned row count accordingly — proving the cap is read server-side from config, not just a passed arg (Codex MEDIUM); (6) a null-tag AND a null-confidence_tier AND a null is_open_now row are each INCLUDED when the corresponding data-dependent filter (wheelchair/changing, high_conf, open_now) is active (D-08, Antigravity MAJOR); (7) get_location_detail returns a non-null distance_m when passed user_lat/user_lng and null when they are omitted (guards the LocationDetail distance source); (8) calling search_locations_bbox with `min_lng > max_lng` (antimeridian crossing) raises NO exception and returns rows on both sides of longitude 180 (Antigravity MAJOR); (9) update_profile: a family-mode-only call `update_profile(new_family_mode := true)` changes ONLY `family_mode` and leaves `display_name` unchanged, while a display-name-only call `update_profile(new_display_name := 'X')` updates display_name and leaves family_mode unchanged (both reviewers — the coalesce fix); (10) a direct `select * from locations` (or `.from('locations').select()` equivalent) as both `anon` and `authenticated` roles raises a permission-denied error / returns zero rows — proving base-table SELECT is revoked and the three RPCs are the only read path (plan-checker finding, 2026-07-05). Run via `supabase test db`.
  </action>
  <verify>
    <automated>cd app && test -f src/lib/database.types.ts && grep -q "search_locations_bbox" src/lib/database.types.ts && grep -q "suppressed_at" src/lib/database.types.ts</automated>
  </verify>
  <done>Migrations pushed to live project; database.types.ts regenerated with the new RPCs + suppressed_at (incl. get_location_detail distance_m and update_profile's nullable args) and committed; seed.sql carries the dev fixtures (loaded only on local reset); pgTAP suite asserts moderation/family_mode/access-code/nearest-N/config-driven-pin-cap/D-08-null-include/detail-distance/antimeridian/coalesce-update_profile and passes via `supabase test db`.</done>
  <acceptance_criteria>
    - `supabase db push` applied all four migrations with no error (or the exact blocking command surfaced for manual run).
    - app/src/lib/database.types.ts contains `search_locations_bbox`, `search_locations_nearby`, `get_location_detail`, and `suppressed_at`; get_location_detail's generated types expose user_lat/user_lng args + distance_m return; update_profile's generated args are both optional/nullable.
    - supabase/seed.sql contains sensitive + suppressed + shadowbanned + deleted + null-tag + null-confidence_tier + null-is_open_now fixtures near Eugene, plus a pair of rows straddling longitude 180 for the antimeridian case.
    - pgTAP file asserts all nine correctness properties (incl. config-driven pin cap, D-08 null-include across open_now/confidence/tags, antimeridian no-crash, and coalesce update_profile) and `supabase test db` reports pass.
    - No location INSERTs leaked into numbered migrations (only seed.sql).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anon/authenticated client → PostgREST RPC | Untrusted client calls the three read RPCs and update_profile over HTTPS; JWT may be absent (anon) or present (authenticated). |
| RPC body → base tables | SECURITY DEFINER executes as owner, bypassing RLS — the RPC body is the sole enforcement point for moderation/family_mode/column exposure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | get_location_detail / bbox / nearby return shape | mitigate | Explicit public column list; never `setof locations`/`select l.*`; access_instructions omitted entirely (OQ-3); shadowban/deleted/suppressed columns never returned (Pitfall 4). distance_m is a derived scalar, not a raw column. |
| T-03-02 | Tampering / Info Disclosure | family_mode filter | mitigate | Read family_mode from `public.users` via `auth.uid()` server-side; never a client parameter (Pitfall 3). pgTAP asserts sensitive rows absent for family_mode user regardless of client input. |
| T-03-03 | Information Disclosure | moderation filter | mitigate | Four-clause `deleted_at/suppressed_at/shadowban_status` filter in every RPC body + RLS select policy updated for direct-table belt-and-suspenders. pgTAP asserts absence. |
| T-03-04 | Elevation of Privilege | SECURITY DEFINER search_path | mitigate | `set search_path = public` + schema-qualified refs on all four functions (Pattern 5). |
| T-03-05 | Tampering (SQL injection) | RPC numeric/uuid params | mitigate | Parameterized args only (incl. detail user_lat/user_lng numerics); no dynamic SQL string-building (incl. the antimeridian branch — plpgsql IF/ELSE, not string-built SQL); bbox coords typed `numeric`, id typed `uuid` (CLAUDE.md: no raw SQL outside migrations). |
| T-03-06 | Elevation of Privilege | RPC role grants | mitigate | Reads grant anon+authenticated; update_profile revokes public+anon, grants authenticated only (Supabase ALTER DEFAULT PRIVILEGES requires explicit anon revoke). |
| T-03-07 | Information Disclosure / Integrity | dev seed reaching prod | mitigate | Seed lives in supabase/seed.sql, loaded only on local `db reset` (config.toml), never on `db push`; zero location INSERTs in numbered migrations (Pitfall 6). |
| T-03-24 | Information Disclosure | direct base-table SELECT bypassing all three RPCs | mitigate | `locations_select_public` RLS policy previously allowed anon/authenticated to `select *` directly (no access_sensitivity/family_mode filter, full column exposure incl. `access_instructions`), bypassing every protection built into the RPCs. Fixed by revoking base-table SELECT from both roles in Task 1 (same remedy already applied to `ratings` and `availability_flags` elsewhere in this project). pgTAP case #10 asserts direct table reads are denied for both roles. |
| T-03-22 | Denial of Service | client-supplied max_pins | mitigate | Pin cap resolved server-side from app_config and applied via `least(max_pins, v_max_pins)` — a client cannot request an unbounded viewport payload (Codex MEDIUM). |
| T-03-SC | Tampering | package installs | accept | No package installs in this plan (SQL/types only). Net-new package legitimacy handled in 03-02; RESEARCH audit already rated both [OK]. |
</threat_model>

<verification>
- `supabase db push` applies all four migrations cleanly against the live project.
- `supabase test db` (pgTAP) green: moderation exclusion, family_mode exclusion, access-code omission, nearest-N ordering, config-driven pin cap, D-08 null-include (open_now/confidence/tags), detail-distance source, antimeridian no-crash, update_profile coalesce.
- `grep suppressed_at app/src/lib/database.types.ts` and `grep search_locations_bbox app/src/lib/database.types.ts` both match (types regenerated).
- No `setof locations` / `select l.*` / `access_instructions` in the RPC migration; no `coordinates::geometry && st_makeenvelope` (index-bypassing cast) in the bbox RPC.
- Antigravity (PostGIS correctness) + Codex (security/privacy) review gate APPROVE before commit (project rule).
</verification>

<success_criteria>
- ROADMAP SC2 (four-clause exclusion), SC3 (family_mode RPC-layer), SC4 (nearest by meters), SC9 (suppressed_at column added before RPC references) satisfied at the DB layer.
- The three RPCs + extended update_profile are callable with correct role grants and pass pgTAP correctness assertions.
- The bbox RPC uses the spatial index (envelope cast, not column cast), survives antimeridian crossing, includes null-data rows under active filters (D-08), and clamps the pin cap to the app_config value server-side.
- update_profile updates display_name and family_mode independently (coalesce) so the family-mode toggle can never null a display name.
- get_location_detail supplies a real, server-computed distance_m so the LocationDetail peek-tier distance has a non-mocked data source.
- database.types.ts regenerated and committed so 03-02 hooks can import RPC types.
</success_criteria>

<output>
Create `.planning/phases/03-read-path-map/03-01-SUMMARY.md` when done — record RPC signatures (incl. get_location_detail's `(uuid, numeric, numeric)` arg list + distance_m return), the update_profile `(text default null, boolean default null)` coalesce semantics, that the bbox pin cap is clamped server-side from app_config (client max_pins cannot exceed it), that the bbox RPC uses the index-friendly envelope cast + antimeridian split + D-08 null-include filters, the max_pins config key, the seed fixture identities (which rows are sensitive/suppressed/shadowbanned/null-data/antimeridian), and the exact `access_sensitivity` sentinel used so downstream fixtures match.
</output>
</content>
</invoke>
