---
phase: 03-read-path-map
plan: 03
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
files_modified:
  - app/src/app/(tabs)/index.tsx
  - app/src/app/(components)/LocationDetailSheet.tsx
  - app/src/features/locations/useMapViewport.ts
  - app/src/features/locations/__tests__/useMapViewport.test.ts
autonomous: false
requirements:
  - REQ-MAP
  - REQ-DETAIL

must_haves:
  truths:
    - "MapScreen renders a Mapbox map with bathroom pins clustered natively, driven by search_locations_bbox"
    - "Pins arrive asynchronously as data loads; the map tile is never blocked by a spinner"
    - "Panning/zooming refetches the viewport bbox debounced 400ms after movement stops; viewport persists within a session"
    - "Beyond the max zoom-out threshold the map shows 'Zoom in to see individual locations' instead of a sparse pin subset"
    - "A user-location blue dot renders distinct from bathroom pins"
    - "Tapping a pin opens a peek/half/full LocationDetail bottom sheet with name, distance, policy tag, confidence badge, hours (or 'Hours not yet available'), and a Get Directions button that opens the native maps app"
    - "On RPC failure an error banner with Retry appears and previously-loaded pins remain visible"
  artifacts:
    - path: "app/src/app/(tabs)/index.tsx"
      provides: "MapScreen — Mapbox MapView, ShapeSource native clustering, user dot, viewport refetch, error banner"
      min_lines: 60
    - path: "app/src/app/(components)/LocationDetailSheet.tsx"
      provides: "@gorhom/bottom-sheet peek/half/full detail sheet incl. Get Directions"
      min_lines: 60
    - path: "app/src/features/locations/useMapViewport.ts"
      provides: "debounced viewport→bbox state + zoom-out cutoff logic"
      exports: ["useMapViewport"]
  key_links:
    - from: "app/src/app/(tabs)/index.tsx"
      to: "useLocationsBbox"
      via: "TanStack useQuery keyed on viewport"
      pattern: "useLocationsBbox"
    - from: "app/src/app/(tabs)/index.tsx"
      to: "LocationDetailSheet"
      via: "pin onPress → open sheet with location id"
      pattern: "LocationDetailSheet"
    - from: "LocationDetailSheet"
      to: "native maps app"
      via: "expo-linking openURL with destination coords"
      pattern: "Linking.openURL"
---

<objective>
Build the MapScreen (Mapbox map, native-clustered pins, user-location dot, debounced viewport refetch, zoom-out cutoff, RPC-failure banner) and the full LocationDetail bottom sheet (peek/half/full, confidence badge, distance, policy tag, hours, Get Directions deep-link, dismiss-by-gesture). This is the primary read surface of Phase 3.

Purpose: Turns the tested 03-02 data hooks into the visible map experience (ROADMAP SC1/SC5). The LocationDetail sheet is built complete now (D-12) so later phases add functionality without a rebuild; Verify/Rate/Report buttons are hidden entirely (D-13) until their phases ship.
Output: MapScreen + LocationDetailSheet component + a debounced viewport hook.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-read-path-map/03-UI-SPEC.md
@.planning/phases/03-read-path-map/03-RESEARCH.md
@.planning/phases/03-read-path-map/03-PATTERNS.md
@.planning/phases/03-read-path-map/03-02-SUMMARY.md
@docs/design/design-system.md

# Analog screen + component (read before building)
@app/src/app/(tabs)/profile.tsx
@app/src/app/(components)/DeleteAccountModal.tsx

<interfaces>
<!-- Data hooks from 03-02 (see 03-02-SUMMARY.md for exact signatures):
     useLocationsBbox(viewport, filters) → GeoJSON FeatureCollection (properties incl. id, policyTag, confidenceTier, verificationCount)
     useLocationDetail(id) → LocationDetail (name, address, distanceM, policyTag, confidenceTier, verificationCount, lastVerifiedAt, isOpenNow, hours) — no access code
     formatDistance(meters, usesMiles) → string -->

<!-- Design tokens (app/src/constants/): Colors[colorScheme], spacing, typography, radius.
     No raw hex / magic numbers (design-system.md §20; D-33 token exception stands — do not re-flag). -->

<!-- Map render pattern (RESEARCH Pattern 1): <MapView> → <ShapeSource cluster clusterRadius={50}
     clusterMaxZoomLevel={14}> with SymbolLayer(clusterCount) + CircleLayer(clusters) + SymbolLayer(singlePin,
     iconImage = match on policy_tag). Pin color = Mapbox match expression, NOT React state. User dot = built-in
     user-location layer. Requires GestureHandlerRootView at root (already present) + reanimated plugin. -->

<!-- Get Directions (RESEARCH Code Examples): Platform.select ios `maps:0,0?q=label@lat,lng`
     android `geo:0,0?q=lat,lng(label)` default `https://maps.google.com/?q=lat,lng` → Linking.openURL. No auth gate (D-21). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Debounced viewport hook + MapScreen with native-clustered pins</name>
  <files>app/src/features/locations/useMapViewport.ts, app/src/features/locations/__tests__/useMapViewport.test.ts, app/src/app/(tabs)/index.tsx</files>
  <read_first>
    - app/src/app/(tabs)/profile.tsx (screen scaffold: useColorScheme→Colors, useQuery, design tokens, skeleton/error render branches lines 84-100)
    - .planning/phases/03-read-path-map/03-UI-SPEC.md (§MapScreen contract — chip row is 03-05, this plan builds map + pins + states shell)
    - .planning/phases/03-read-path-map/03-RESEARCH.md (§Pattern 1 clustering, §Pitfall 7 gesture-handler/reanimated, Anti-Patterns)
    - app/jest.setup.ts (@rnmapbox/maps mock — extend for CircleLayer if missing)
  </read_first>
  <behavior>
    - useMapViewport: given map region-change events, exposes the current {minLng,minLat,maxLng,maxLat} bbox + zoom, debounced 400ms after movement stops (D-01). Exposes a boolean `belowPinThreshold` derived from zoom vs the max_pins-informed cutoff (D-04/D-32) — when true, callers show the zoom-in message instead of fetching pins. Viewport persists within the hook's lifetime (session), resets only on remount/cold start (D-03).
  </behavior>
  <action>
    Write useMapViewport.test FIRST (100% coverage): assert the debounce coalesces rapid region changes into one bbox update after 400ms (fake timers), assert bbox math from region, assert belowPinThreshold flips past the cutoff. Implement the hook in features/ (covered), keeping the screen thin.
    MapScreen (index.tsx, thin wrapper — excluded from coverage but TDD Guard still applies to any logic; keep logic in the hook): render `<MapView>` with light/dark style following `useColorScheme()` (D-25); map tile shows immediately, pins arrive async with no blocking spinner (D-02). Feed `useLocationsBbox(viewport, filters)` output (filters default empty until 03-05 wires the store) into `<ShapeSource cluster clusterRadius={50} clusterMaxZoomLevel={14}>` with a SymbolLayer for cluster counts, a CircleLayer for cluster bubbles (dominant policy-tag color), and a SymbolLayer for single pins whose `iconImage` is a Mapbox `match` on `policy_tag` (pinChillSpot/pinCodeRequired/pinPublicFacility/pinPurchaseRequired colors from Colors). Render the built-in user-location blue dot distinct from pins (D-26). When `belowPinThreshold`, replace pins with the centered "Zoom in to see individual locations" card (D-04). On `useLocationsBbox` error, show the RPC-failure banner + Retry while keeping the last successfully-loaded pins on screen (D-28) — do not clear to an empty map. Cite the design-system.md §20 Component Acceptance Checklist in this plan's SUMMARY.
  </action>
  <verify>
    <automated>cd app && npm test -- src/features/locations/__tests__/useMapViewport.test.ts && npx tsc --noEmit -p app/tsconfig.json</automated>
  </verify>
  <done>useMapViewport debounces to a single bbox after 400ms and exposes belowPinThreshold; MapScreen renders a themed Mapbox map with native-clustered pins from useLocationsBbox, a distinct user dot, the zoom-out cutoff message, and an error banner that preserves stale pins.</done>
  <acceptance_criteria>
    - useMapViewport test asserts 400ms debounce coalescing + belowPinThreshold flip; passes at 100% coverage.
    - index.tsx uses ShapeSource `cluster` with a `match`-on-policy_tag icon expression (not React-state pin colors).
    - Map style switches with useColorScheme; user-location dot rendered via the built-in layer.
    - Error state renders a Retry banner without clearing previously-loaded pins.
    - `npx tsc --noEmit` passes (types from regenerated database.types.ts resolve).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: LocationDetail bottom sheet (peek/half/full) with Get Directions</name>
  <files>app/src/app/(components)/LocationDetailSheet.tsx</files>
  <read_first>
    - app/src/app/(components)/DeleteAccountModal.tsx (component-folder conventions: default export, Colors[colorScheme], token StyleSheet, a11y attrs, testID, re-entrancy guard)
    - .planning/phases/03-read-path-map/03-UI-SPEC.md (§LocationDetail bottom sheet contract — snap points, action row order, copy for confidence badge / last-verified / missing hours)
    - .planning/phases/03-read-path-map/03-RESEARCH.md (§Don't Hand-Roll bottom sheet, §Pitfall 7, §Code Examples native-maps deep link)
    - .planning/phases/03-read-path-map/03-02-SUMMARY.md (useLocationDetail + formatDistance signatures)
  </read_first>
  <behavior>
    - Renders a @gorhom/bottom-sheet with snap points peek(30%)/half(55%)/full(90%) and `md` (12px) top-corner radius. Peek: name (h2), distance via formatDistance (subhead), policy-tag badge, confidence badge ("High — 14 GPS verifications" colored pill, D-15). Half: hours or "Hours not yet available" (D-18), accessibility flags, action row. Full: directions context. Ratings-summary + timing-tips sections omitted entirely (D-14), not empty states.
    - Action row order `[Get Directions] [GPS Verify] [Rate] [Report]`; ONLY Get Directions renders this phase (D-13/D-20). Get Directions opens the native maps app with destination coords via expo-linking, no auth gate (D-21). Access code never shown (omitted from the RPC — D-24).
    - Dismiss via swipe-down or tap-outside scrim only; no explicit close button (D-17). Last-verified shows a relative label ("Verified 3 days ago", D-16) via date-fns formatDistanceToNow.
  </behavior>
  <action>
    Write a component test FIRST asserting: renders name/distance/policy/confidence from a mocked useLocationDetail; renders "Hours not yet available" when hours is null; Get Directions press calls Linking.openURL with a URL containing the lat/lng; Verify/Rate/Report controls are NOT in the tree this phase (queryByText returns null); no access-code text present. Then implement using @gorhom/bottom-sheet (peek/half/full), design tokens only (no raw hex), and the DeleteAccountModal a11y conventions (accessibilityRole/Label on interactive + status elements, testID on root). Confirm GestureHandlerRootView wraps the root and the reanimated babel plugin is configured (Pitfall 7). Content animates in-place across multiple pin taps (no dismiss/reopen); on open, show skeleton bars then resolve.
  </action>
  <verify>
    <automated>cd app && npm test -- LocationDetailSheet && npx tsc --noEmit -p app/tsconfig.json</automated>
  </verify>
  <done>The sheet renders peek/half/full content tiers from useLocationDetail, shows the confidence badge + relative last-verified + missing-hours copy, exposes only Get Directions (which deep-links to native maps), hides Verify/Rate/Report, shows no access code, and dismisses by gesture.</done>
  <acceptance_criteria>
    - Component test asserts Get Directions calls Linking.openURL with lat/lng in the URL.
    - "Hours not yet available" renders when hours is null.
    - Verify/Rate/Report controls absent from the tree; no access-code text present.
    - Snap points peek/half/full configured on @gorhom/bottom-sheet; dismiss by swipe/tap-outside (no close button).
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Device verification — map clustering + LocationDetail sheet</name>
  <action>Pause for the user to verify Mapbox map rendering and bottom-sheet gestures on a device — the two manual-only surfaces from 03-VALIDATION.md that jest cannot observe (native clustering, @gorhom/bottom-sheet animation). Run only after all automated tests pass.</action>
  <what-built>MapScreen (Mapbox map, native-clustered pins, user-location blue dot, 400ms viewport refetch, zoom-out cutoff, RPC-failure banner with stale pins) and the LocationDetail bottom sheet (peek/half/full snap points, confidence badge, Get Directions to native maps, swipe/tap-outside dismiss). These are the two manual-only surfaces from 03-VALIDATION.md (native Mapbox rendering and @gorhom/bottom-sheet gestures are not observable through jest).</what-built>
  <how-to-verify>
    1. Run the app on a device/simulator (EAS dev client — Mapbox cannot run in Expo Go): `cd app && npx expo start --dev-client`.
    2. Open the Map tab. Confirm the map tile renders immediately and bathroom pins appear shortly after (seed data near Eugene, OR). Confirm pins are colored by policy tag and clusters show a count badge in the dominant policy-tag color.
    3. Pan/zoom the map; confirm pins refetch ~400ms after you stop moving, and zooming far out replaces pins with "Zoom in to see individual locations".
    4. Confirm your own location shows as a distinct blue dot.
    5. Tap a pin; confirm the LocationDetail sheet opens at peek, drags to half/full, shows name/distance/policy/confidence and "Hours not yet available"; tap "Get Directions" and confirm the native maps app opens to the location; confirm there is NO close button and swipe-down / tap-outside both dismiss.
    6. Toggle device dark mode; confirm the map style follows.
    7. (If testable) simulate an RPC failure; confirm the error banner + Retry appears and existing pins stay visible.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (e.g. wrong pin colors, sheet doesn't dismiss, directions don't open).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MapScreen → useLocationsBbox → RPC | Viewport coords sent to the server RPC; the client renders whatever the (already-filtered) RPC returns and must not re-filter. |
| LocationDetail → native maps app | Location coords handed to an external app via a deep link. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-11 | Information Disclosure | LocationDetail render | mitigate | Sheet renders only fields from useLocationDetail (03-01 RPC omits access_instructions); no access code shown anywhere (D-24); test asserts absence. |
| T-03-12 | Information Disclosure | error banner copy | mitigate | RPC-failure banner shows a generic retry message; never surfaces server error text or moderation reasons (RESEARCH Anti-Pattern 7). |
| T-03-13 | Information Disclosure | logs/telemetry | mitigate | No lat/lng/address logged in the map or sheet (CLAUDE.md no-PII rule); Get Directions passes coords only to the OS maps intent, not to logs. |
| T-03-14 | Tampering | client re-filtering | accept | Client cannot bypass moderation — all filtering is server-side in the RPC; the map only renders returned rows. Low risk, no client-side authority. |
| T-03-SC | Tampering | package installs | mitigate | No new installs in this plan; @gorhom/bottom-sheet already added + audited [OK] in 03-02. |
</threat_model>

<verification>
- `cd app && npm test` green including useMapViewport + LocationDetailSheet tests.
- `npx tsc --noEmit` passes.
- Human-verify checkpoint approved (map clustering, sheet gestures, Get Directions, dark mode, error banner).
- design-system.md §20 Component Acceptance Checklist cited in SUMMARY (ROADMAP SC10).
- Antigravity + Codex review gate APPROVE before commit.
</verification>

<success_criteria>
- ROADMAP SC1 (Mapbox map with pins from search_locations_bbox) and SC5 (LocationDetail shows name/policy/confidence/last-verified) satisfied.
- LocationDetail built complete (D-12) with Get Directions (D-19) and hidden action buttons (D-13); no access code exposed.
</success_criteria>

<output>
Create `.planning/phases/03-read-path-map/03-03-SUMMARY.md` when done — record the LocationDetailSheet prop contract and open/dismiss API, the useMapViewport signature, and how 03-05 should mount the filter chip row + empty states onto index.tsx without conflicting. Cite the §20 checklist.
</output>
