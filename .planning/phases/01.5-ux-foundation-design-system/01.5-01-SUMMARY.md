---
phase: "01.5"
plan: "01"
subsystem: design-contract
tags: [ux, flows, wireframes, design-system, navigation]
requires: []
provides:
  - docs/design/flows.md
  - docs/design/wireframes.md
affects:
  - "Phases 2-8 (all client-facing screens implement against these contracts)"
tech-stack:
  added: []
  patterns:
    - "Mermaid flowchart TD as flow-diagram format"
    - "Portrait-first ASCII wireframes as layout source of truth"
key-files:
  created:
    - docs/design/flows.md
    - docs/design/wireframes.md
  modified: []
decisions:
  - "FAB is single-tap (no expand); emergency mode switching via in-sheet chips"
  - "gps_consent written only after OS dialog grants — never on pre-prompt tap"
  - "Pending pin visibility via server-side JOIN on submissions.submitter_id, not client filter"
  - "submit_location RPC idempotent on client-generated submission_id"
  - "Verify security-sensitive rejections use generic copy, never reveal detection"
metrics:
  duration: "~12 min"
  completed: "2026-06-25"
requirements:
  - REQ-1.5-A
  - REQ-1.5-B
---

# Phase 01.5 Plan 01: UX Flows & Wireframes Summary

Design-contract docs establishing the navigation and layout source of truth for all client-facing phases (2–8): 16 Mermaid flow diagrams and 27 portrait-first ASCII wireframes, with screen names cross-matched between both documents.

## What Was Built

**Task 1 — `docs/design/flows.md`** (commit `6e72c20`): 16 `flowchart TD` Mermaid blocks — 13 named flows (First Launch, GPS Consent Grant, GPS Consent Deny, Sign-In, Sign-Up, Map Discovery, Emergency Mode, Changing Table NOW, Accessible NOW, Submit, Verify, Report, Rating) plus 3 edge-case flows (Offline, No-Location, No-Results). Every flow terminates at a defined green-styled terminal state; error exits styled red. Includes the `submit_location` idempotent server contract, the report-type DB mapping table with the RC-02 `duplicate_location` migration note, and a "Notes for Implementors" footer.

**Task 2 — `docs/design/wireframes.md`** (commit `bf1a02f`): Notation legend + 27 portrait-first ASCII wireframe screens, each with a route/modal annotation, box-drawing frame, and `←` annotations outside the frame. Bottom sheet rendered at all 3 snap points with distinct content; Emergency Mode shown with in-sheet mode chips (not FAB expand); Submit Step 3 shows the disabled "I'm Here" annotation; Rating screen shows all 4 emoji dimensions with the conditional changing-surface note. Closes with a Wireframe Index table mapping every screen to its route and creating phase.

## Verification

- `docs/design/flows.md`: 16 `mermaid` blocks confirmed (13 named + 3 edge-case) — matches required count.
- `docs/design/wireframes.md`: 27 screen H2 sections confirmed (29 total H2 minus Legend + Wireframe Index).
- `docs/design/` contains exactly 2 files (flows.md, wireframes.md).
- Cross-check: screen names (Welcome Screen, GPS Consent Pre-prompt, Map Screen, Sign-In/Sign-Up Screen, Verify Flow Screen, Auth Required Modal, Rating Screen, Submit Success Screen, Emergency Result Sheet) appear verbatim in both documents.
- TDD Guard: not applicable — both files are `*.md` (TDD Guard OFF per project scope table).

## Deviations from Plan

The plan's `<context>` block `@`-references `.planning/phases/01.5-ux-foundation-design-system/01-5-RESEARCH.md`, which does not exist on disk (only `01-5-CONTEXT.md`, the two PLAN files, and `01.5-REVIEW.md` are present). This was not blocking: the PLAN.md itself inlines every notation convention (Mermaid node shapes, ASCII frame/button/chip/FAB/skeleton symbols) and the full node list for all 16 flows and all 27 screens that the RESEARCH file would have supplied. CONTEXT.md (the locked, mandatory decisions) was read in full and drove all flow logic and screen content. No design decision was guessed. Logged here as informational; no scope change.

Otherwise: plan executed exactly as written. No Rule 1–4 deviations, no authentication gates.

## Known Stubs

None. Both deliverables are complete design contracts; no placeholder data or unwired components (these are documentation artifacts, not code).

## Self-Check: PASSED

- FOUND: docs/design/flows.md
- FOUND: docs/design/wireframes.md
- FOUND: commit 6e72c20 (flows.md)
- FOUND: commit bf1a02f (wireframes.md)
