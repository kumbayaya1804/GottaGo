---
phase: "01.5"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/design/flows.md
  - docs/design/wireframes.md
autonomous: true
requirements:
  - REQ-1.5-A
  - REQ-1.5-B
must_haves:
  truths:
    - "docs/design/flows.md exists and contains 13 named Mermaid flowchart TD blocks covering every named flow"
    - "Every flow ends at a defined terminal state — no critical path has an undefined next state"
    - "docs/design/wireframes.md exists and contains ASCII wireframes for all 27 v1 screens and modal states, portrait-first"
    - "Wireframe screen names exactly match flow diagram node names"
    - "The 3 edge-case flows (offline, no-location, no-results) are diagrammed"
  artifacts:
    - path: "docs/design/flows.md"
      provides: "13 Mermaid flowchart TD diagrams + 3 edge-case flows"
      contains: "flowchart TD"
    - path: "docs/design/wireframes.md"
      provides: "27 ASCII wireframe screens in portrait frame format"
      contains: "┌──────────────────────────────────┐"
  key_links:
    - from: "flows.md screen node names"
      to: "wireframes.md screen headings"
      via: "exact name match"
      pattern: "## .* Screen|## .* Flow|## .* State"
    - from: "wireframes.md routing annotations"
      to: "app/src/app/ file structure"
      via: "route path comments per screen"
      pattern: "Route:|Modal:"
---

<objective>
Produce two design contract documents: (1) Mermaid flowchart TD diagrams for all 13 named flows plus 3 edge-case flows, and (2) ASCII wireframes for all 27 v1 screens and modal states.

Purpose: These documents become the navigation + layout source of truth for every client-facing phase (2–8). No Phase 2+ screen ships without a matching wireframe in this doc.

Output: `docs/design/flows.md` and `docs/design/wireframes.md` in a new `docs/design/` directory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01.5-ux-foundation-design-system/01-5-CONTEXT.md
@.planning/phases/01.5-ux-foundation-design-system/01-5-RESEARCH.md
@SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docs/design/flows.md — 13 named flows + 3 edge-case flows in Mermaid</name>
  <files>docs/design/flows.md</files>

  <read_first>
  MANDATORY before writing a single line:
  - `.planning/phases/01.5-ux-foundation-design-system/01-5-CONTEXT.md` — read ALL sections: First Launch Flow, Emergency Mode, Navigation Model, Submit Flow, Verify Flow, Report Flow, Rating UI, Auth Flow. These are the locked decisions that dictate flow logic.
  - `.planning/phases/01.5-ux-foundation-design-system/01-5-RESEARCH.md` — Section 9 (Flow Diagram Format) for Mermaid notation conventions. Section 8 (GPS Error States) for all 11 error state exits.
  - `SPEC.md` — Core User Flows section for Find/Add/Verify/Report flow requirements.

  Key facts to extract before writing:
  - FAB is single-tap (no expand) per CONTEXT.md Emergency Mode section — mode switching via chips INSIDE the result sheet
  - GPS consent is written ONLY after OS dialog resolves to granted (CONTEXT.md First Launch Flow)
  - SubmitFlow has 3 steps; Report has 2 steps; Verify is single screen
  - Pending pin requires JOIN on submissions.submitter_id (server-side), not client filter
  - Auth-required actions show inline modal, return to action after auth
  </read_first>

  <action>
  Create `docs/design/` directory and write `docs/design/flows.md`.

  File structure: one H2 heading per flow, each containing exactly one ```mermaid ... ``` block using `flowchart TD` syntax. Do NOT combine flows into a single diagram.

  Use these Mermaid node shape conventions (per RESEARCH.md Section 9):
  - `([text])` = rounded rectangle = start/end terminal state
  - `[text]` = rectangle = screen or system action
  - `{text}` = diamond = decision point
  - `/text/` = parallelogram = user input/action
  - `-->|label|` = labeled transition arrow
  - Style terminal states: `style NodeId fill:#E6F4EA,stroke:#34A853`
  - Style error exits: `style NodeId fill:#FCE8E6,stroke:#EA4335`
  - Style screens: default (no style override needed)

  Write all 16 flows in this order:

  **Flow 1: First Launch**
  App Launch → Welcome Screen → /User taps Find a Bathroom/ → GPS Consent Pre-prompt → {OS Permission Dialog} → |Granted| Map Screen GPS Centered → ([Main Experience]) ; |Denied| Map Screen City View Search Active → ([Main Experience])
  Note: GPS consent (users.gps_consent write) occurs AFTER OS dialog grants — show as a system action node after the Granted branch, before Map Screen.

  **Flow 2: GPS Consent — Grant Path**
  Focused diagram: GPS Consent Pre-prompt → /Tap Enable Location/ → {OS Permission Dialog} → |Granted| [Write users.gps_consent=true, gps_consent_at=now()] → Map Screen GPS Centered

  **Flow 3: GPS Consent — Deny Path**
  GPS Consent Pre-prompt → /Tap Enable Location/ → {OS Permission Dialog} → |Denied| Map Screen City View → [Search bar active, GPS-centered view disabled] → ([User can still browse])

  **Flow 4: Sign-In Flow**
  /Protected action tapped/ OR /Profile tab tapped (unauth)/ → Auth Required Modal → {Choice} → |Sign In| Sign-In Screen → /Enter email + password/ → {Auth result} → |Success| ([Return to triggering action]) ; |Failed| [Inline error: "Invalid credentials"] → Sign-In Screen ; |Google OAuth (Android)| [Deep link to Google] → {OAuth result} → |Success| ([Return]) ; |Failed| [Inline error]

  **Flow 5: Sign-Up Flow**
  Sign-In Screen → /Tap Create Account/ → Sign-Up Screen → /Enter email, password, display name/ → {Validation} → |Valid| [Supabase signup → DB trigger creates users row] → ([Return to triggering action]) ; |Invalid| [Inline field errors] → Sign-Up Screen

  **Flow 6: Map Discovery**
  Map Screen → /Tap bathroom pin/ → Bottom Sheet Peek 30% [skeleton loading] → {Data loaded?} → |Yes| Bottom Sheet Peek 30% [real content: name, distance, policy badge, confidence badge] → /Drag or tap/ → Bottom Sheet Half 55% [hours, ratings, access code (auth only), action row] → /Drag or tap/ → Bottom Sheet Full 90% [timing tips, ratings breakdown, directions] ; |No (>2s)| [Loading banner at top]
  Also show: /Tap different pin/ → Bottom Sheet content animates in-place (no dismiss)

  **Flow 7: Emergency Mode**
  Map Screen → /Tap FAB (red, bottom-right)/ → [Fetch nearest any bathroom] → Emergency Result Sheet Half 55% [red header, NEAREST RESULT badge, mode chips: Any/Changing Table/Accessible] → /Tap Navigate/ → ([Device maps app opens])
  Also: /Tap Dismiss or FAB again/ → ([Sheet collapses, emergency mode off])

  **Flow 8: Changing Table NOW**
  Emergency Result Sheet → /Tap Changing Table chip/ → {Confirmed changing table location nearby?} → |Yes| [Map re-centers, sheet updates in-place: confirmed changing table location] → /Tap Navigate/ → ([Device maps app]) ; |No| [Fallback: nearest any bathroom shown] → [Display: "No confirmed changing table bathroom nearby — showing nearest available" + "This location has not been confirmed for changing table"] → [Alternate action: "Search more" button OR "View changing table list" button]

  **Flow 9: Accessible NOW**
  Emergency Result Sheet → /Tap Accessible chip/ → {Confirmed wheelchair-accessible location nearby?} → |Yes| [Map re-centers, sheet updates in-place] → /Tap Navigate/ → ([Device maps app]) ; |No| [Fallback: nearest any bathroom] → [Display: "No confirmed accessible bathroom nearby — showing nearest available" + "This location has not been confirmed for wheelchair access"] → [Alternate action: "Search more" OR "View accessible list"]

  **Flow 10: Submit Flow (3-step wizard)**
  /Tap Submit tab (auth required)/ → {Authenticated?} → |No| Auth Required Modal → Sign-In/Sign-Up → Submit Flow Step 1 ; |Yes| Submit Flow Step 1 [Name, Address (Places autocomplete), Policy tag picker, Accessibility tags] → /Tap Next/ → Submit Flow Step 2 [Hours picker, Access code (optional), Timing tips] → /Tap Next/ → Submit Flow Step 3 GPS Confirm [Live GPS accuracy, distance from address, "I'm at this location" button] → {GPS valid?} → |Valid| [submit_location RPC called] → {RPC result?} → |success — no duplicate| Submit Success Screen → /Tap Back to Map/ → Map Screen [pending pin visible to submitter only — via submissions.submitter_id JOIN] ; |duplicate_candidate| [Inline: "A bathroom at this address may already exist. [View existing]"] → {User choice} → |Continue| [submit_location RPC called with confirm_duplicate: true] → Submit Success Screen ; |Cancel| Map Screen ; |GPS invalid| [Inline GPS error (too far / low accuracy / stale fix)] → Submit Flow Step 3 [retry]

  > **Server contract:** The client generates a `submission_id` (UUID) when the user enters Submit Step 1 and passes it on every `submit_location` call. The RPC is idempotent on `submission_id`: a repeat call with the same `submission_id` returns the existing `{ status: 'success', location_id }` without creating a new row. On the first call: if no duplicate is found, inserts and returns `{ status: 'success', location_id }`; if a duplicate is found, does NOT insert and returns `{ status: 'duplicate_candidate', candidate: { id, name, address } }`. With `confirm_duplicate: true`: skips the duplicate check and upserts by `submission_id`, returning `{ status: 'success', location_id }`. This makes the happy path a single round-trip and the confirm path safe against double-taps, retries, and network replays.

  **Flow 11: Verify Flow**
  /Tap GPS Verify in action row (auth required)/ → {Authenticated?} → |No| Auth Required Modal → Sign-In → Verify Flow Screen ; |Yes| Verify Flow Screen [Location name, live GPS accuracy readout, distance from location, "I'm Here" button] → {GPS valid AND within 100m?} → |Yes| /Tap I'm Here/ → [verify_location RPC] → {RPC result} → |Success| Verify Success State [checkmark + "Verified! Thanks for keeping data fresh."] → ([Sheet returns]) ; |Already verified today| [Inline: "You've already verified this location today"] → Verify Flow Screen ; |RPC error (generic)| [Inline: "Unable to verify your location. Please try again."] → Verify Flow Screen ; |GPS not ready| [Inline GPS error shown, button disabled] → Verify Flow Screen [user waits/retries]

  **Flow 12: Report Flow (2 steps)**
  /Tap Report in action row (auth required)/ → {Authenticated?} → |No| Auth Required Modal → Sign-In → Report Step 1 ; |Yes| Report Step 1 [Select type: Permanently Closed / Access Denied Currently Locked / Code is Wrong / Dirty or Unsafe / Duplicate Location] → /Select report type/ → Report Step 2 [Confirm: "Report this as [type]? This helps keep data accurate." + Submit Report button + Cancel] → /Tap Submit Report/ → [report_location RPC] → Toast: "Report submitted. Thanks for helping." → ([Bottom sheet returns]) ; /Tap Cancel/ → ([Bottom sheet returns])

  **Report type DB mapping (for Phase 7 implementation):**
  | User Label | DB value (reports.report_type) |
  |---|---|
  | Permanently Closed | permanently_closed |
  | Access Denied / Currently Locked | currently_locked |
  | Code is Wrong | inaccurate_information |
  | Dirty or Unsafe | dirty_unsafe |
  | Duplicate Location | duplicate_location ⚠ NOT in live CHECK constraint — Phase 7 must add via migration |

  > **Schema dependency (RC-02):** The live `reports.report_type` CHECK constraint does NOT include 'duplicate_location'. Phase 7's `report_location` RPC will hit a DB constraint violation unless this migration runs first: add 'duplicate_location' to the CHECK array in `reports`.

  **Flow 13: Rating Flow**
  /Tap Rate in action row (auth required)/ → {Authenticated?} → |No| Auth Required Modal → Sign-In → Rating Screen ; |Yes| Rating Screen [4 emoji dimensions: Cleanliness, Accessibility, Convenience, Changing Surface Cleanliness (conditional: has_changing_table only)] → /Select emoji for each dimension/ → /Tap Submit Rating/ → [Rating RPC] → {Result} → |Success| Toast: "Rating saved. Thanks!" → ([Bottom sheet returns]) ; |Error| [Inline: "Couldn't save rating. Try again."] → Rating Screen

  **Edge-case Flow A: Offline State**
  Map Screen → {Network available?} → |No| [Top banner: "No connection" amber background] → [Cached pins remain visible] → {User taps pin?} → |Yes| Bottom Sheet [cached content or "Unable to load details"] → {Network restored?} → |Yes| [Banner dismisses, normal operation resumes]

  **Edge-case Flow B: No-Location (GPS Denied) State**
  Map Screen City View → [Search bar active by default] → /User types city or address/ → [Google Places autocomplete, debounced 300ms] → /Select result/ → [Map re-centers, search_locations_bbox RPC for new viewport] → Map Screen [pins shown for searched area]

  **Edge-case Flow C: No-Results State**
  Map Screen → [search_locations_bbox returns 0 results] → [Empty state: "No bathrooms found nearby"] → ["Search this area" button visible] → /Tap Search this area/ → [New bbox RPC fetch] → {Results?} → |Yes| Map Screen with pins ; |No| Empty state remains

  After writing all flows, add a footer section:

  ```markdown
  ---
  ## Notes for Implementors
  - Screen node names in these diagrams are the canonical names. Wireframes in `docs/design/wireframes.md` use the same names.
  - All Mermaid blocks require a Mermaid-compatible viewer (GitHub.com renders these natively).
  - "Auth Required Modal" in flows maps to the inline slide-up sheet (not full navigation).
  - Suppressed locations appear absent from results — no flow node for suppression (transparent to user per security rules).
  ```
  </action>

  <acceptance_criteria>
  - `docs/design/flows.md` exists at that exact path
  - File contains exactly 16 ```mermaid ``` code blocks (13 named flows + 3 edge-case flows)
  - Every block uses `flowchart TD` as the diagram type (not `graph TD`, not `stateDiagram`)
  - Flow 1 (First Launch) includes a node for writing `users.gps_consent=true` AFTER OS dialog grants — not before
  - Flow 7 (Emergency Mode) shows FAB as single-tap activating emergency (no expand node) with mode chips inside the result sheet
  - Flow 10 (Submit) includes a duplicate-detection branch and the pending pin visibility note referencing `submissions.submitter_id JOIN`
  - Flow 11 (Verify) includes generic error copy "Unable to verify your location. Please try again." — not a specific rejection reason
  - Flow 12 (Report) uses exactly 5 report type options matching CONTEXT.md: Permanently Closed, Access Denied/Currently Locked, Code is Wrong, Dirty or Unsafe, Duplicate Location
  - All 16 flows end at a defined terminal state (rounded rectangle node or labeled end)
  - No flow has a dead-end branch (every decision branch has at least one defined exit)
  - Footer "Notes for Implementors" section is present
  </acceptance_criteria>

  <done>
  `docs/design/flows.md` contains 16 Mermaid flowchart TD diagrams — 13 named flows (First Launch, GPS Grant, GPS Deny, Sign-In, Sign-Up, Map Discovery, Emergency Mode, Changing Table NOW, Accessible NOW, Submit, Verify, Report, Rating) plus 3 edge-case flows (Offline, No-Location, No-Results). Every flow ends at a defined terminal state. No critical path is a dead end.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create docs/design/wireframes.md — ASCII wireframes for all 27 screens</name>
  <files>docs/design/wireframes.md</files>

  <read_first>
  MANDATORY before writing:
  - `.planning/phases/01.5-ux-foundation-design-system/01-5-CONTEXT.md` — read: Navigation Model (tab bar spec), LocationDetail Bottom Sheet (3 snap points + content at each), Emergency Mode (FAB interaction, sheet structure), Submit Flow (3 steps), Verify Flow (screen content), Report Flow (2 steps), Rating UI, Auth Flow, Profile Tab, Loading & Skeleton States.
  - `.planning/phases/01.5-ux-foundation-design-system/01-5-RESEARCH.md` — Section 3 (ASCII Wireframe Conventions) for exact notation. Section 11 (Key Implementation Guidance) for complete screen list. Section 5 (FAB Guidelines) for FAB placement notation. Section 4 (Bottom Sheet patterns) for snap point content model.
  - `app/src/app/` file structure (already read): `(auth)/sign-in.tsx`, `(auth)/sign-up.tsx`, `(tabs)/index.tsx`, `(tabs)/profile.tsx`, `location/[id].tsx` — wireframes must annotate the matching route path.

  Key notation rules from RESEARCH.md Section 3:
  - Frame: 34-char wide `┌──────────────────────────────────┐`
  - Status bar line: `│  9:41          ●●●●  WiFi  🔋   │`
  - Primary button: `╔══════════════════╗` / `║  Label  ║`
  - Secondary button: `┌──────────────────┐` / `│  Label  │`
  - Text input: `┌──────────────────┐` / `│ placeholder  │`
  - Chip inactive: `( Label )`
  - Chip active: `(●Label●)`
  - FAB: `[!!!]` bottom-right corner
  - Skeleton: `░░░░░░░░░░░░░░░`
  - Badge/pill: `< text >`
  - Progress steps: `①────②────③` with `●` for active
  - Tab bar bottom: `│ [Map] [Nearby][Submit][Prf] │`
  </read_first>

  <action>
  Write `docs/design/wireframes.md`. Begin with a legend section defining all notation symbols used. Then produce one H2 section per screen, in the order listed below. Each screen includes:
  1. The screen name as H2 heading (must exactly match flow diagram node names)
  2. Route annotation: `Route: /(tabs)/index` or `Modal: router.push('/modals/verify')` etc.
  3. The ASCII frame
  4. Annotations outside the frame (with `←` arrows) for tap targets, snap points, etc.

  Produce wireframes for all 27 items in this order:

  1. **Welcome Screen** — Route: root (before tab navigation). Content: App name "Gotta Go" large centered, tagline "Find a bathroom when it matters most", single primary CTA `╔══ Find a Bathroom ══╗`, below CTA: small text "By continuing, you agree to our [Terms of Service] and [Privacy Policy]." No tab bar.

  2. **GPS Consent Pre-prompt** — No route (fullscreen overlay over welcome). Content: Location icon centered, headline "Enable Location", body "Gotta Go uses your location to find bathrooms nearby. We never share your exact location or store your GPS history." Primary button `╔══ Enable Location ══╗` (triggers OS permission dialog). No tab bar.

  3. **Map Screen — Default State** — Route: `/(tabs)/index`. Content: Status bar, search bar at top `┌── 🔍 Search a city or address ──┐`, filter chip row below search `( Changing Table ) ( Wheelchair ) ( Chill Spot ) ( Open Now ) ( Clean: 4+ )`, Mapbox map fills remaining screen, map pins shown as `[●]` with policy tag label, FAB `[!!!]` bottom-right above tab bar. Tab bar at bottom.

  4. **Map Screen — GPS Denied State** — Route: `/(tabs)/index`. Same as default but search bar is active/highlighted `┌── 🔍 Search a city or address ──┐ ← ACTIVE`, filter chips hidden, map shows city-level view, no GPS-centered pin, FAB still present.

  5. **Map Screen + Bottom Sheet Peek (30%)** — Route: `/(tabs)/index`. Map fills top 70%, bottom sheet peek occupies bottom 30%. Sheet shows: drag handle `────`, Location Name (skeleton or real), distance + policy badge + confidence badge. Tab bar hidden behind sheet.

  6. **Map Screen + Bottom Sheet Half (55%)** — Route: `/(tabs)/index`. Map fills top 45%, sheet at 55%. Sheet shows: drag handle, name, distance, policy badge, confidence badge, divider, hours, ratings summary, access code row "Access Code: ****  [Tap to reveal]" (signed-in) or hidden (unauth), divider, action row `[GPS Verify] [⭐ Rate] [🚩 Report]`. Annotation: action row is 44pt min tap target.

  7. **Map Screen + Bottom Sheet Full (90%)** — Route: `/(tabs)/index`. Map barely visible at top 10%. Sheet at 90%: full content including timing tips list, ratings breakdown (cleanliness/accessibility/convenience), report history summary, directions button.

  8. **Map Screen — Emergency Mode Active (FAB tapped)** — Route: `/(tabs)/index`. Map visible. Emergency bottom sheet opens at half (55%) immediately. Sheet header: red/orange strip `████ NEAREST RESULT ████`, mode chips `(●Any Bathroom●) ( Changing Table ) ( Accessible )`, large location name (h1 weight), bold distance "0.3 mi", primary button `╔══ Navigate ══╗`, dismiss button below. FAB shown in activated red state. Filter chip row hidden.

  9. **Emergency Mode Fallback State** — Same as #8 but with fallback copy: "No confirmed changing table bathroom found nearby — showing nearest available." and "This location has not been confirmed for changing table." Alternate action buttons: `[ Search more ]` `[ View changing table list ]`.

  10. **Sign-In Screen** — Route: `/(auth)/sign-in`. Content: "Sign In" heading, email input, password input, `╔══ Sign In ══╗` primary button, divider "or", `[ Continue with Google ]` (Android only — annotate "iOS shows Apple stub"), `[ Sign in with Apple — coming soon ]` (iOS only — annotate), "Create account" link at bottom. No tab bar.

  11. **Sign-Up Screen** — Route: `/(auth)/sign-up`. Content: "Create Account" heading, display name input, email input, password input, `╔══ Create Account ══╗`, "Already have an account? Sign In" link. No tab bar.

  12. **Submit Flow: Step 1 — Location Details** — Route: `/(tabs)/submit` (auth required). Content: progress `①────○────○`, "Add a Bathroom" heading, Name input (required), Address input with Places autocomplete active, Policy tag picker `( Chill Spot ) ( Code Required ) ( Purchase Required ) ( Public Facility )`, Accessibility multi-select `☐ Wheelchair Accessible  ☐ Baby Changing Table  ☐ Family Restroom`, `[ Next → ]` secondary button. Tab bar shown.

  13. **Submit Flow: Step 2 — Access & Hours** — Route: same. Progress `●────①────○` → `○────②────○`. Hours picker (day/time), Access code input "Access Code / PIN (optional)", Timing tips textarea (optional). `[ ← Back ]` and `[ Next → ]` buttons. Tab bar shown.

  14. **Submit Flow: Step 3 — GPS Confirm** — Route: same. Progress `○────○────③`. Live GPS readout: "Accuracy: 12m ✅" or "68m ⚠ — move to open area", distance from address: "You are 45m away", `╔══ I'm at This Location ══╗` (enabled when accuracy ≤50m and within 100m), inline GPS error area below readout. `[ ← Back ]` link. Annotation: button disabled state when GPS invalid.

  15. **Submit Flow: Success State** — Route: same. Checkmark icon, "Location Submitted!", body: "It'll appear publicly after 2 GPS verifications. You can see it on the map now." `╔══ Back to Map ══╗`. Annotation: pending pin (gray dashed) will appear on map for submitter only.

  16. **Verify Flow Screen** — Modal: `router.push('/modals/verify')`. Content: "Verify Location" heading, location name (h2), GPS accuracy: "Accuracy: 12m ✅", distance: "You are 45m away (updates live)", `╔══ I'm Here ══╗` (full-width, 56pt height — annotate), inline error area below button. "Cancel" link at top-right. No tab bar.

  17. **Verify Flow: Error State** — Same as #16 but error area populated: "You're too far away — need to be within 100m" (or "GPS accuracy too low — move to an open area and try again"). Button disabled `[ I'm Here (disabled) ]`. Annotate: button disabled until GPS valid.

  18. **Verify Flow: Success State** — Same modal. Checkmark animation area, "Verified!" heading, "Thanks for keeping data fresh." body. `[ Done ]` button. No retry needed.

  19. **Report Flow: Step 1 — Select Type** — Modal: `router.push('/modals/report')`. Content: "Report a Problem" heading, large tappable rows (44pt min each): `[ Permanently Closed ]`, `[ Access Denied / Currently Locked ]`, `[ Code is Wrong ]`, `[ Dirty or Unsafe ]`, `[ Duplicate Location ]`. "Cancel" at top-right.

  20. **Report Flow: Step 2 — Confirm** — Same modal. "Report this as [selected type]?" heading, "This helps keep data accurate." body, `╔══ Submit Report ══╗`, `[ Cancel ]` secondary. Annotate: no text input required — intentional.

  21. **Rating Screen** — Modal: `router.push('/modals/rating')`. Content: "Rate This Bathroom" heading, 4 dimension rows (each with label + 5 emoji buttons): Cleanliness `😣 😕 😐 😊 😍`, Accessibility `😣 😕 😐 😊 😍`, Convenience `😣 😕 😐 😊 😍`, Changing Surface (conditional, shown only if has_changing_table) `😣 😕 😐 😊 😍`. `╔══ Submit Rating ══╗`. Annotate: emoji buttons 48×48pt minimum, accessibilityValue required.
  > **Schema note (RC-03):** `has_changing_table` is NOT a `locations` column — it is derived from the `tags` table: `tags.find(t => t.key === 'has_changing_table')?.value === 'true'`. Phase 3 search RPC must include tags in the location payload, or Phase 8 must perform a separate tags lookup to derive this boolean. The `changing_surface_cleanliness` dimension also requires a new column (`changing_surface_cleanliness integer null check (... 1–5)`) in the `ratings` table — Phase 8 must add via migration.
  > **Accessibility tag storage (RC-03):** Submit Step 1 "Baby Changing Table" checkbox writes `{ key: 'has_changing_table', value: 'true' }` to the `tags` table. "Wheelchair Accessible" writes `{ key: 'wheelchair_accessible', value: 'true' }`. Phase 4 (Submit) must implement these as tags table inserts.

  22. **Nearby Tab** — Route: `/(tabs)/nearby` (created in Phase 2). Annotate "Screen created in Phase 2". Placeholder frame with: "Nearby" heading, list rows showing `[●] Location Name — 0.3 mi — < High > — Chill Spot`, sorted by distance. No FAB (FAB is Map tab only). Tab bar.

  23. **Profile Tab — Signed In** — Route: `/(tabs)/profile`. Content: avatar placeholder circle + display name + email (masked), Stats section: "GPS Verifications: 12", "Locations Submitted: 3", "Ratings Given: 8". Settings list: Account, Privacy Policy (Termly link), Terms of Service (Termly link), Delete Account, Sign Out. Tab bar.

  24. **Profile Tab — Unauthenticated** — Route: `/(tabs)/profile`. Content: "Sign in to contribute" headline, value prop: "Track your contributions and access door codes.", `╔══ Sign In ══╗`, `[ Create Account ]`, muted stats preview (grayed: "? GPS Verifications"). Tab bar.

  25. **Auth Required Modal** — Inline slide-up: appears over any screen when protected action tapped. Content: "Sign in to [action]" heading, `╔══ Sign In ══╗`, `[ Create Account ]`, `[ Cancel ]`. Backdrop dims underlying screen. Annotate: returns to triggering action after auth.

  26. **Pending Location Detail Sheet** — Same as Bottom Sheet Half (#6) but header shows gray dashed pin indicator, name + "Pending" badge, body: "Pending — 1 of 2 GPS verifications received. Share with friends to speed up verification." Action row: only [GPS Verify] shown (no Rate/Report for pending). Annotate: visibility requires JOIN on submissions.submitter_id in search RPC.

  27. **Filter Chip Row — Active State** — Inset frame (not full screen) showing the filter row with active chips highlighted: `(●Changing Table●) (●Wheelchair●) ( Chill Spot ) ( Open Now ) ( Clean: 4+ )`. Annotate: active chip = filled background + bold label + check; row is horizontally scrollable; hidden during emergency mode.

  After all 27 wireframes, add:

  ```markdown
  ---
  ## Wireframe Index

  | # | Screen | Route / Modal | Plan Created |
  |---|--------|---------------|--------------|
  | 1 | Welcome Screen | root | Phase 2 |
  | 2 | GPS Consent Pre-prompt | root overlay | Phase 2 |
  | 3 | Map Screen — Default State | /(tabs)/index | Phase 3 |
  | 4 | Map Screen — GPS Denied State | /(tabs)/index | Phase 3 |
  | 5 | Map Screen + Bottom Sheet Peek | /(tabs)/index | Phase 3 |
  | 6 | Map Screen + Bottom Sheet Half | /(tabs)/index | Phase 3 |
  | 7 | Map Screen + Bottom Sheet Full | /(tabs)/index | Phase 3 |
  | 8 | Map Screen — Emergency Mode Active | /(tabs)/index | Phase 8 |
  | 9 | Emergency Mode Fallback State | /(tabs)/index | Phase 8 |
  | 10 | Sign-In Screen | /(auth)/sign-in | Phase 2 |
  | 11 | Sign-Up Screen | /(auth)/sign-up | Phase 2 |
  | 12 | Submit Flow: Step 1 | /(tabs)/submit | Phase 4 |
  | 13 | Submit Flow: Step 2 | /(tabs)/submit | Phase 4 |
  | 14 | Submit Flow: Step 3 | /(tabs)/submit | Phase 4 |
  | 15 | Submit Flow: Success | /(tabs)/submit | Phase 4 |
  | 16 | Verify Flow Screen | modal: /modals/verify | Phase 5 |
  | 17 | Verify Flow: Error State | modal: /modals/verify | Phase 5 |
  | 18 | Verify Flow: Success State | modal: /modals/verify | Phase 5 |
  | 19 | Report Flow: Step 1 | modal: /modals/report | Phase 7 |
  | 20 | Report Flow: Step 2 | modal: /modals/report | Phase 7 |
  | 21 | Rating Screen | modal: /modals/rating | Phase 8 |
  | 22 | Nearby Tab | /(tabs)/nearby | Phase 2 |
  | 23 | Profile Tab — Signed In | /(tabs)/profile | Phase 2 |
  | 24 | Profile Tab — Unauthenticated | /(tabs)/profile | Phase 2 |
  | 25 | Auth Required Modal | inline slide-up | Phase 2 |
  | 26 | Pending Location Detail Sheet | /(tabs)/index | Phase 4 |
  | 27 | Filter Chip Row — Active State | /(tabs)/index | Phase 3 |
  ```
  </action>

  <acceptance_criteria>
  - `docs/design/wireframes.md` exists at that exact path
  - File contains exactly 27 H2 screen sections (counted by `## ` headings that match screen names)
  - Every screen section includes: a route/modal annotation, an ASCII frame using `┌─┐` or `╔═╗` box-drawing characters, and at least one annotation with `←` outside the frame
  - Screen names in H2 headings exactly match node names used in `docs/design/flows.md` (no synonyms — "Emergency Mode Active" in both, not "Emergency Sheet" in one and "Emergency Mode" in the other)
  - Bottom sheet wireframes (#5, #6, #7) clearly show 3 distinct snap points with different content visible
  - Emergency Mode wireframe (#8) shows FAB in activated state, red/orange sheet header, mode chips ([Any Bathroom] [Changing Table] [Accessible]) inside the sheet (NOT as FAB expand options)
  - Submit Step 3 wireframe (#14) shows "I'm Here" button with disabled-state annotation
  - Rating wireframe (#21) shows all 4 emoji dimensions with `😣 😕 😐 😊 😍` notation and the conditional changing surface note
  - Wireframe Index table at end lists all 27 screens with route and phase-created columns
  - Legend section at top defines all notation symbols
  - `docs/design/` directory was created (both files exist in it)
  </acceptance_criteria>

  <done>
  `docs/design/wireframes.md` contains portrait-first ASCII wireframes for all 27 v1 screens and modal states, with route annotations, notation legend, and a wireframe index table. Screen names exactly match flow diagram node names in flows.md.
  </done>
</task>

</tasks>

<verification>
After both tasks complete, verify:
1. `docs/design/flows.md` exists — open and count Mermaid blocks: must be 16
2. `docs/design/wireframes.md` exists — open and count H2 headings: must be 27 screen sections + index
3. Cross-check: pick 3 screen names from flows.md node text and confirm the same names appear as H2 headings in wireframes.md
4. Confirm `docs/design/` directory contains exactly 2 files at this stage (flows.md, wireframes.md)
</verification>

<success_criteria>
- `docs/design/flows.md` exists with 16 Mermaid flowchart TD blocks; all 13 named flows + 3 edge-case flows present; no dead-end branches
- `docs/design/wireframes.md` exists with 27 screen wireframes in portrait-first ASCII; all screen names match flow node names exactly; wireframe index table complete
- REQ-1.5-A satisfied: all 13 named flows covered with no critical path ending without a defined next state
- REQ-1.5-B satisfied: all 27 v1 screens have a named ASCII wireframe
</success_criteria>

<output>
Create `.planning/phases/01.5-ux-foundation-design-system/01.5-01-SUMMARY.md` when done.
</output>
