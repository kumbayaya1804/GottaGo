---
phase: 04-gps-service-submission
plan: 04
type: execute
wave: 3
depends_on: [04-02, 04-03]
files_modified:
  - app/src/features/submit/types.ts
  - app/src/features/submit/useMyPendingSubmissions.ts
  - app/src/features/submit/withdrawSubmission.ts
  - app/src/features/submit/updateAccessCode.ts
  - app/src/features/submit/__tests__/useMyPendingSubmissions.test.ts
  - app/src/features/submit/__tests__/withdrawSubmission.test.ts
  - app/src/features/submit/__tests__/updateAccessCode.test.ts
autonomous: true
requirements: [REQ-PENDING, REQ-CODE-WRITE]

must_haves:
  truths:
    - "useMyPendingSubmissions calls get_my_pending_submissions and returns a LocationFeatureCollection whose features carry confirmation_count and expires_at in properties (so the pending sheet needs no second fetch)"
    - "withdrawSubmission calls withdraw_submission with the submission id and rethrows raw RPC errors"
    - "updateAccessCode calls update_access_code (stage only) and confirmAccessCode calls confirm_access_code; neither claims the code is live immediately"
  artifacts:
    - path: "app/src/features/submit/useMyPendingSubmissions.ts"
      provides: "authed-only pending pins query → GeoJSON"
      exports: ["useMyPendingSubmissions"]
    - path: "app/src/features/submit/withdrawSubmission.ts"
      provides: "withdraw_submission RPC wrapper"
      exports: ["withdrawSubmission"]
    - path: "app/src/features/submit/updateAccessCode.ts"
      provides: "update + confirm + get access-code RPC wrappers"
      exports: ["updateAccessCode", "confirmAccessCode", "getAccessCode"]
  key_links:
    - from: "useMyPendingSubmissions"
      to: "supabase.rpc('get_my_pending_submissions')"
      via: "no-arg call → FeatureCollection transform with pending properties"
      pattern: "get_my_pending_submissions"
    - from: "updateAccessCode"
      to: "supabase.rpc('update_access_code')"
      via: "stage-then-confirm pair"
      pattern: "supabase.rpc\\('update_access_code'"
---

<objective>
Build the client service layer for the pending-submission and access-code-update surfaces: `useMyPendingSubmissions` (authed-only pending pins → GeoJSON, carrying verification-progress in properties), `withdrawSubmission` (D-28/D-29), and `updateAccessCode`/`confirmAccessCode`/`getAccessCode` (D-21/D-24 stage-then-confirm client wrappers). All are `features/**` → TDD Guard test-first, 100% coverage.

Purpose: Feeds the map's pending-pin layer, the pending-status sheet, and the "Update door code" UI (all in 04-06) tested data hooks.
Output: 3 source modules + 3 test files, green at 100%.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-gps-service-submission/04-RESEARCH.md
@.planning/phases/04-gps-service-submission/04-PATTERNS.md

<interfaces>
<!-- Reuse existing GeoJSON shapes from Phase 3 (do not redefine). -->

From app/src/features/locations/types.ts (reuse):
  LocationFeatureCollection { type:'FeatureCollection'; features: LocationFeature[] }
  LocationFeature { type:'Feature'; geometry:{type:'Point'; coordinates:[lng,lat]}; properties: {...} }

get_my_pending_submissions() returns rows: { id, name, lat, lng, policy_tag, confirmation_count, expires_at }
  → useMyPendingSubmissions must place confirmation_count + expires_at into each feature's properties (D-27 needs them without a refetch)

withdraw_submission(p_submission_id uuid) → void
update_access_code(p_location_id uuid, p_code text) → void   (STAGES only; not live)
confirm_access_code(p_location_id uuid) → void                (promotes; different-user gate is server-side, 04-02)
get_access_code(p_location_id uuid) → text                    (authed-only current code)

Analogs:
  useLocationsBbox.ts — RPC → FeatureCollection transform + toMapLocation mapper
  deleteAccount.ts — one-arg supabase.rpc wrapper that rethrows raw
  updateProfile.test.ts — supabase singleton jest mock
</interfaces>
</context>

<tasks>

<task type="execute" tdd="true">
  <name>Task 1: useMyPendingSubmissions query → GeoJSON (test-first)</name>
  <files>app/src/features/submit/useMyPendingSubmissions.ts, app/src/features/submit/__tests__/useMyPendingSubmissions.test.ts</files>
  <read_first>
    - app/src/features/locations/useLocationsBbox.ts (RPC→FeatureCollection transform + [lng,lat] order + toMapLocation)
    - app/src/features/locations/types.ts (LocationFeatureCollection/Feature/Properties)
    - app/src/features/profile/__tests__/updateProfile.test.ts (supabase mock)
    - .planning/phases/04-gps-service-submission/04-PATTERNS.md §"useMyPendingSubmissions"
  </read_first>
  <behavior>
    - calls supabase.rpc('get_my_pending_submissions') with no args
    - maps each row to a GeoJSON Point feature with coordinates [lng, lat]
    - each feature.properties includes id, name, policyTag, confirmationCount, expiresAt
    - RPC error → thrown
    - empty/null data → empty FeatureCollection (no crash)
  </behavior>
  <action>
    First write the test mocking `../../lib/supabase` and asserting the no-arg call, the [lng,lat] coordinate order, and that confirmationCount + expiresAt land in properties. Then implement `useMyPendingSubmissions(): Promise<LocationFeatureCollection>` mirroring useLocationsBbox's transform. Reuse `LocationFeatureCollection` from features/locations/types; extend features/submit/types.ts only if a pending-specific properties type is needed (carry confirmation_count/expires_at). Coordinates are `[lng, lat]`.
  </action>
  <verify>
    <automated>cd app && npm test -- src/features/submit/__tests__/useMyPendingSubmissions.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test asserts supabase.rpc called with 'get_my_pending_submissions' and no argument object (or empty)
    - Test asserts feature coordinates are [lng, lat] and properties carry confirmationCount + expiresAt
    - Test covers empty-data → empty FeatureCollection
    - 100% coverage on the module
  </acceptance_criteria>
  <done>Pending submissions are fetched authed-only and transformed to GeoJSON carrying verification progress; tests green at 100%.</done>
</task>

<task type="execute" tdd="true">
  <name>Task 2: withdrawSubmission wrapper (test-first)</name>
  <files>app/src/features/submit/withdrawSubmission.ts, app/src/features/submit/__tests__/withdrawSubmission.test.ts</files>
  <read_first>
    - app/src/features/profile/deleteAccount.ts (one-arg rpc wrapper that rethrows raw)
    - app/src/features/profile/__tests__/updateProfile.test.ts (supabase mock)
  </read_first>
  <behavior>
    - calls supabase.rpc('withdraw_submission', { p_submission_id }) with the given id
    - RPC error → rethrown raw
    - success → resolves void; does NOT navigate or refetch (caller invalidates ['pendingSubmissions', uid])
  </behavior>
  <action>
    First write the test asserting the rpc name + `p_submission_id` arg and raw-error rethrow. Then implement `withdrawSubmission(submissionId: string): Promise<void>` as the deleteAccount-shaped wrapper with one arg. No navigation/refetch inside — that is the caller's job (04-06).
  </action>
  <verify>
    <automated>cd app && npm test -- src/features/submit/__tests__/withdrawSubmission.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test asserts rpc('withdraw_submission', { p_submission_id: <id> })
    - Test asserts an RPC error is rethrown unchanged
    - 100% coverage on the module
  </acceptance_criteria>
  <done>withdrawSubmission issues the caller-scoped RPC and rethrows errors; tests green at 100%.</done>
</task>

<task type="execute" tdd="true">
  <name>Task 3: updateAccessCode / confirmAccessCode / getAccessCode wrappers (test-first)</name>
  <files>app/src/features/submit/updateAccessCode.ts, app/src/features/submit/__tests__/updateAccessCode.test.ts</files>
  <read_first>
    - app/src/features/profile/updateProfile.ts (rpc wrapper + throw)
    - app/src/features/profile/__tests__/updateProfile.test.ts (supabase mock)
    - .planning/phases/04-gps-service-submission/04-02-PLAN.md (the three code RPC signatures + stage-then-confirm contract)
  </read_first>
  <behavior>
    - updateAccessCode(locationId, code) → rpc('update_access_code', { p_location_id, p_code }); error rethrown
    - confirmAccessCode(locationId) → rpc('confirm_access_code', { p_location_id }); error rethrown
    - getAccessCode(locationId) → rpc('get_access_code', { p_location_id }) returning data as string; error rethrown
    - none of these treat the staged code as live immediately (they are thin wrappers; the gate is server-side)
  </behavior>
  <action>
    First write the test asserting each rpc name + arg mapping and raw-error rethrow for all three functions. Then implement the three wrappers in one module mirroring updateProfile's shape. getAccessCode returns `data as string`.
  </action>
  <verify>
    <automated>cd app && npm test -- src/features/submit/__tests__/updateAccessCode.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test asserts update_access_code called with { p_location_id, p_code }
    - Test asserts confirm_access_code and get_access_code arg mapping
    - Test asserts errors rethrown unchanged
    - 100% coverage on the module
  </acceptance_criteria>
  <done>The three code-path wrappers map their RPCs correctly and rethrow errors; tests green at 100%.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client → pending/withdraw/code RPCs | Authed-only calls; server enforces scoping and the D-24 gate |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-14 | Information Disclosure | pending-pin scoping | mitigate | useMyPendingSubmissions relies on the server's auth.uid() scoping (04-01); no client-side "my submissions" filter (which would leak). |
| T-04-15 | Tampering | code overwrite via client | mitigate | Wrappers only stage/confirm; the different-user promotion gate is server-side (04-02). Client cannot bypass it. |
| T-04-16 | Information Disclosure | code in logs | mitigate | No code value logged; getAccessCode returns to memory only for the authed UI. |
</threat_model>

<verification>
- `cd app && npm test` full suite green; 100% coverage on all three new modules.
</verification>

<success_criteria>
- REQ-PENDING client path (pending pins + withdraw) delivered with server-enforced scoping.
- REQ-CODE-WRITE client wrappers deliver the stage-then-confirm calls for 04-06's UI.
</success_criteria>

<output>
Create `.planning/phases/04-gps-service-submission/04-04-SUMMARY.md` when done.
</output>
