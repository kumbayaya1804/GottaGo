---
phase: 04-gps-service-submission
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/tests/phase4_submit.test.sql
  - supabase/migrations/20260707020000_phase4_submission_staging.sql
  - app/src/lib/database.types.ts
autonomous: false
requirements: [REQ-SUBMIT, REQ-GPS-VALIDATE, REQ-PENDING, REQ-SENSITIVITY, REQ-TIMING, REQ-CODE-WRITE]

must_haves:
  truths:
    - "A signed-in user's submit_location call inserts a pending submissions row (never a locations row) with confirmation_count=1"
    - "submit_location rejects mocked=true, accuracy>50m, and fixes older than 60s with a single generic 'gps rejected' error"
    - "An anonymous caller of submit_location / get_my_pending_submissions / withdraw_submission is rejected (not authenticated / no rows)"
    - "get_my_pending_submissions returns only the caller's own pending rows; another user's pending rows are never returned"
    - "withdraw_submission deletes only the caller's own pending row; it cannot delete another user's or a non-pending row"
    - "access_sensitivity is staged as the literal 'sensitive' string or NULL (never a made-up value)"
  artifacts:
    - path: "supabase/migrations/20260707020000_phase4_submission_staging.sql"
      provides: "submissions staging columns + submit_location, get_my_pending_submissions, withdraw_submission RPCs"
      contains: "create or replace function public.submit_location"
    - path: "supabase/tests/phase4_submit.test.sql"
      provides: "pgTAP correctness suite for the three submission RPCs"
      contains: "select plan("
    - path: "app/src/lib/database.types.ts"
      provides: "regenerated types including new submissions columns + 3 RPCs"
      contains: "submit_location"
  key_links:
    - from: "submit_location"
      to: "app_config"
      via: "value::numeric read of max_accuracy_m / max_gps_age_s with coalesce fallback"
      pattern: "app_config where key = 'max_accuracy_m'"
    - from: "submit_location"
      to: "submissions.coordinates"
      via: "st_setsrid(st_makepoint(lng,lat),4326)::geography"
      pattern: "st_makepoint\\(p_lng, p_lat\\)"
    - from: "get_my_pending_submissions"
      to: "auth.uid()"
      via: "submitter_id = auth.uid() AND status='pending' scoping"
      pattern: "submitter_id = auth.uid\\(\\)"
---

<objective>
Build the submission write path in the database: add typed staging columns to `submissions`, and create the three submission-lifecycle SECURITY DEFINER RPCs (`submit_location`, `get_my_pending_submissions`, `withdraw_submission`). This resolves the phase's central storage question as **Option A** (RESEARCH §Pattern 1): pending bathroom data lives ONLY on the `submissions` row — no `locations` row is created until Phase 5's publish gate — so Phase 3's five shipped readers are untouched and cannot leak pending data.

Purpose: Everything else in Phase 4 (client services, wizard, pending pins) depends on these RPC contracts and the regenerated types.
Output: One migration, one pgTAP suite, regenerated `database.types.ts`, and a live-pushed schema.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/04-gps-service-submission/04-RESEARCH.md
@.planning/phases/04-gps-service-submission/04-PATTERNS.md
@docs/schema-contract.md

<interfaces>
<!-- Existing live shapes the executor must respect. Do NOT re-derive; use directly. -->

submissions table (live, pre-migration) — from schema + database.types.ts:
  id uuid, location_id uuid NULL, submitter_id uuid NULL, status text NOT NULL default 'pending',
  confirmation_count int NULL, expires_at timestamptz NOT NULL default now()+14d,
  created_at timestamptz, updated_at timestamptz
  status CHECK: status = any(array['pending','published','expired','rejected'])  -- NO 'withdrawn' value allowed

app_config: (key text, value text NOT NULL) — seeded rows:
  'max_accuracy_m'='50', 'max_gps_age_s'='60', 'submission_publish_threshold'='2', 'verify_radius_m'='100'

Canonical write-RPC template (mirror verbatim): supabase/migrations/20260627000004_profile_rpcs.sql
  - `security definer` + `set search_path = public`
  - opens: `if auth.uid() is null then raise exception 'not authenticated'; end if;`
  - grant triple per RPC: revoke from public; revoke from anon; grant to authenticated (full arg-type signature each time)

app_config read pattern (mirror): 20260704010002_phase3_search_rpcs.sql lines 75-77
  `select value::numeric into v_x from public.app_config where key = '<key>'; v_x := coalesce(v_x, <default>);`

PostGIS coordinate handling (mirror): st_setsrid(st_makepoint(lng, lat), 4326)::geography  -- lng FIRST
  extract for pins: st_y(coordinates::geometry)=lat, st_x(coordinates::geometry)=lng
</interfaces>
</context>

<tasks>

<task type="execute">
  <name>Task 1: pgTAP correctness suite for the three submission RPCs (test-first)</name>
  <files>supabase/tests/phase4_submit.test.sql</files>
  <read_first>
    - supabase/tests/phase3_read_rpcs.test.sql (the harness to copy: begin; / create extension pgtap / select plan(N) / auth.users insert firing handle_new_user / set_config('request.jwt.claims',...) impersonation / throws_ok)
    - .planning/phases/04-gps-service-submission/04-RESEARCH.md §Validation Architecture → Phase Requirements → Test Map (the exact assertions required)
    - .planning/phases/04-gps-service-submission/04-PATTERNS.md §"supabase/tests/phase4_submit.test.sql"
  </read_first>
  <action>
    Write the pgTAP suite BEFORE the migration (RED). Copy the Phase 3 scaffold: `begin;`, `create extension if not exists pgtap with schema extensions;`, `select plan(N);` with N matching the assertion count, an `insert into auth.users(...)` fixture (fires `handle_new_user` to provision `public.users`), and `set_config('request.jwt.claims', json_build_object('sub',<uuid>,'role','authenticated')::text, true)` impersonation, closed with `set_config('request.jwt.claims','',true)` and `select finish(); rollback;`.
    Assert, against an authed caller: (a) submit_location inserts exactly one submissions row with status='pending' and confirmation_count=1 and coordinates NOT NULL; (b) submit_location with p_mocked=true raises 'gps rejected' via throws_ok; (c) submit_location with p_accuracy_m=51 raises 'gps rejected'; (d) submit_location with p_captured_at = now() - interval '61 seconds' raises 'gps rejected'; (e) submit_location with p_access_sensitivity='sensitive' stages the literal 'sensitive'; default p_access_sensitivity NULL stages NULL; (f) submit_location called with NO jwt claims raises 'not authenticated'; (g) get_my_pending_submissions returns the caller's pending row and, after impersonating a SECOND auth.users fixture, returns zero of the first user's rows; (h) get_my_pending_submissions with empty jwt claims returns zero rows; (i) withdraw_submission(id) by a non-owner leaves the row intact, and by the owner deletes it (count goes to 0); (j) withdraw_submission never introduces a status='withdrawn' value.
    Do NOT insert any `locations` row anywhere in this suite — pending data must be provable in `submissions` alone.
  </action>
  <verify>
    <automated>MISSING — pgTAP requires Docker (unavailable here, tracked override consistent with Phase 3). Static gate instead: grep -v '^--' supabase/tests/phase4_submit.test.sql | grep -c "throws_ok\|select is\|select ok" returns >= 10</automated>
  </verify>
  <acceptance_criteria>
    - File exists and begins with `begin;` and `create extension if not exists pgtap`
    - `select plan(N)` present with N equal to the actual number of assertions
    - Contains a `throws_ok(...)` asserting the generic 'gps rejected' error for each of mocked/accuracy/staleness
    - Contains a two-user impersonation block proving get_my_pending_submissions and withdraw_submission are caller-scoped
    - No `insert into public.locations` / `insert into locations` appears anywhere in the file
  </acceptance_criteria>
  <done>The pgTAP suite exists covering all Test Map rows for the three submission RPCs and asserts caller-scoping + generic-error behavior with zero locations-row dependency.</done>
</task>

<task type="execute">
  <name>Task 2: Migration — submissions staging columns + submit/pending/withdraw RPCs</name>
  <files>supabase/migrations/20260707020000_phase4_submission_staging.sql</files>
  <read_first>
    - supabase/migrations/20260627000004_profile_rpcs.sql (auth-gate header + revoke/grant triple to copy verbatim)
    - supabase/migrations/20260704010002_phase3_search_rpcs.sql (app_config read+coalesce, st_makepoint/st_x/st_y coordinate handling, stable read-RPC header)
    - supabase/migrations/20260519010000_remote_schema.sql lines 189-212 (submissions DDL, status CHECK, submissions_select_published RLS)
    - .planning/phases/04-gps-service-submission/04-RESEARCH.md §Pattern 1/2/3/4 (RPC bodies already written there — transcribe)
  </read_first>
  <action>
    Create the migration (timestamp 20260707020000, after the latest 20260707010000). Section 1 — `alter table public.submissions add column` for: `name text`, `coordinates geography(Point,4326)`, `address text`, `policy_tag text`, `access_sensitivity text`, `hours jsonb`, `access_instructions text`, `access_code_confirmed_at timestamptz`, `timing_tip text`. All nullable (zero production rows → no backfill). Per OQ-4, timing tips are staged on `timing_tip` ONLY this phase — do NOT create a `timing_tips` table.
    Section 2 — `submit_location` (12 params exactly per RESEARCH §Pattern 2 signature: p_name text, p_lat numeric, p_lng numeric, p_accuracy_m numeric, p_mocked boolean, p_captured_at timestamptz, p_policy_tag text, p_address text default null, p_access_sensitivity text default null, p_hours jsonb default null, p_access_code text default null, p_timing_tip text default null) returns uuid, `security definer set search_path = public`, NOT `stable`. Body: auth.uid() null-check → raise 'not authenticated' (D-18); read max_accuracy_m (default 50) and max_gps_age_s (default 60) from app_config with coalesce; reject with a single generic `raise exception 'gps rejected'` when p_mocked is true, OR p_accuracy_m is null or > max, OR p_captured_at is null or (now()-p_captured_at) > make_interval(secs=>max) — never echo which check failed (SC7). Then INSERT into submissions (submitter_id=auth.uid(), status='pending', confirmation_count=1 per OQ-2 creator-initial, coordinates=st_setsrid(st_makepoint(p_lng,p_lat),4326)::geography, and the staged fields; access_code_confirmed_at=now() per D-22) returning id. Do NOT set expires_at (existing 14-day default). Do NOT insert into locations.
    Section 3 — `get_my_pending_submissions()` returns table(id uuid, name text, lat double precision, lng double precision, policy_tag text, confirmation_count integer, expires_at timestamptz), `security definer stable set search_path = public`. Body: if auth.uid() is null then return; then return query select ... where s.submitter_id = auth.uid() and s.status='pending', extracting lat via st_y(coordinates::geometry) and lng via st_x(coordinates::geometry).
    Section 4 — `withdraw_submission(p_submission_id uuid)` returns void, `security definer set search_path = public`, NOT stable. Body: capture `uid := auth.uid()`; null-check → raise 'not authenticated'; `delete from public.submissions where id = p_submission_id and submitter_id = uid and status = 'pending'` (D-29 as-if-never-submitted; do NOT add a 'withdrawn' status — the CHECK forbids it).
    For ALL three functions apply the full grant triple with exact arg-type signatures: revoke execute ... from public; revoke ... from anon; grant ... to authenticated. These are authed-only (get_my_pending_submissions is NOT granted to anon, unlike the Phase 3 search RPCs).
  </action>
  <verify>
    <automated>grep -c "revoke execute on function public.submit_location" supabase/migrations/20260707020000_phase4_submission_staging.sql returns 2 (public + anon), AND grep -c "insert into public.locations\|insert into locations" returns 0</automated>
  </verify>
  <acceptance_criteria>
    - `alter table public.submissions add column coordinates geography(Point, 4326)` present (not raw numeric lat/lng)
    - `submit_location` has exactly 12 parameters and inserts `confirmation_count` with the literal value 1
    - Every rejection path raises the identical string `'gps rejected'` (no branch-specific message)
    - `submit_location` reads `max_accuracy_m` and `max_gps_age_s` from `app_config` (not hardcoded literals in the WHERE/IF logic)
    - Each of the three functions has all three grant statements (revoke public, revoke anon, grant authenticated) with matching arg signatures
    - `get_my_pending_submissions` and the writes contain no reference to the `locations` table
    - No `status = 'withdrawn'` string anywhere; withdraw uses DELETE
  </acceptance_criteria>
  <done>Migration adds all 9 staging columns and the three caller-scoped, auth-gated RPCs; pending data never enters `locations`; GPS thresholds are config-driven and rejections are generic.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Authorize live Supabase push + regenerate types (submission schema)</name>
  <action>Pause and ask the user for fresh, explicit authorization before running `supabase db push` against the live project (project rule). On approval, source the git-ignored token and run the push + type regen in one shell invocation, then confirm the new RPCs and columns appear in database.types.ts. Run only after Tasks 1-2 are complete. pgTAP execution is the tracked no-Docker override; the live push is NOT deferred.</action>
  <what-built>Migration 20260707020000_phase4_submission_staging.sql (staging columns + 3 RPCs) is complete and ready to apply to the live Supabase project.</what-built>
  <how-to-verify>
    This project requires FRESH, explicit user authorization before every live `supabase db push`. Pause and ask the user to confirm before running anything against the live database.
    On approval, run the push and type regen in ONE shell invocation, sourcing the git-ignored token file (never echo or inline the token):
    1. Verify the token file shape first: `grep -q '^export SUPABASE_ACCESS_TOKEN=' ~/.supabase-gsd-token` (do NOT print its contents).
    2. `source ~/.supabase-gsd-token && supabase db push`
    3. On success: `source ~/.supabase-gsd-token && supabase gen types typescript --linked > app/src/lib/database.types.ts` (use the project's exact existing regen command if it differs — check 04-PATTERNS/03 docs first).
    4. Confirm the regenerated `database.types.ts` now contains `submit_location`, `get_my_pending_submissions`, `withdraw_submission`, and the new `submissions` columns (name, coordinates, access_sensitivity, timing_tip, access_code_confirmed_at).
    pgTAP execution: no Docker in this environment. Record the same tracked override as Phase 3 (todo `2026-07-07-run-pgtap-suite-on-docker-capable-machine`) and rely on Supabase MCP / manual RPC probing as the stopgap. The pgTAP FILE must exist; its RUN is the tracked-override item — the live push itself is NOT deferred.
  </how-to-verify>
  <resume-signal>Type "pushed" once the live push + type regen succeed, or describe the failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client → submit_location RPC | Untrusted GPS sample (lat/lng/accuracy/mocked/captured_at) and bathroom fields cross here |
| anon → authed-only RPCs | Grant surface for all three submission RPCs |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Spoofing | submit_location GPS sample | mitigate | Reject p_mocked=true; server-validate accuracy (≤max_accuracy_m) and freshness (≤max_gps_age_s) independently of the client. Real presence is Phase 5's proximity gate (documented boundary, RESEARCH Pitfall 1/2). |
| T-04-02 | Information Disclosure | pending bathroom data | mitigate | Option A — pending data stays on `submissions`, never inserted into `locations`; get_my_pending_submissions scoped by auth.uid(). |
| T-04-03 | Information Disclosure | generic error text | mitigate | Single `'gps rejected'` string for mock/accuracy/staleness; no lat/lng/address in any raised message or log (SC7, CLAUDE.md). |
| T-04-04 | Elevation of Privilege | authed-only RPC grant drift | mitigate | revoke from public AND anon, then grant to authenticated, with exact arg signatures (Pitfall 5). |
| T-04-05 | Tampering | withdraw others' submissions | mitigate | DELETE guarded by `submitter_id = auth.uid() AND status='pending'` (Pitfall 6). |
| T-04-06 | Elevation of Privilege | SECURITY DEFINER search_path | mitigate | `set search_path = public`, schema-qualified refs. |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase (Places autocomplete deferred, OQ-1) — no install surface. |
</threat_model>

<verification>
- Migration file applied to live project (checkpoint confirmed); `database.types.ts` regenerated and contains the 3 RPCs + new columns.
- pgTAP file exists with full assertion set (execution tracked-override, no Docker).
- Static gates in each task pass.
</verification>

<success_criteria>
- SC2 (mock rejected at RPC), SC3 (pending insert + confirmation_count=1), SC7 (accuracy>50/stale>60 rejected generically) proven by the pgTAP suite and the migration body.
- SC9 data path (pending visible only to submitter) enforced server-side via get_my_pending_submissions auth.uid() scoping.
- No pending data can reach any public reader (Option A).
</success_criteria>

<output>
Create `.planning/phases/04-gps-service-submission/04-01-SUMMARY.md` when done.
</output>
