---
phase: 04-gps-service-submission
plan: 03
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - app/src/features/submit/types.ts
  - app/src/features/submit/useGpsSample.ts
  - app/src/features/submit/submitLocation.ts
  - app/src/features/submit/submitSchema.ts
  - app/src/features/submit/__tests__/useGpsSample.test.ts
  - app/src/features/submit/__tests__/submitLocation.test.ts
  - app/src/features/submit/__tests__/submitSchema.test.ts
autonomous: true
requirements: [REQ-GPS-VALIDATE, REQ-SUBMIT, REQ-SENSITIVITY, REQ-TIMING]

must_haves:
  truths:
    - "useGpsSample returns {coord, accuracy, mocked, timestamp} using high-accuracy mode, and returns a {denied:true} sentinel (never throws) when permission is denied"
    - "On iOS where pos.mocked is undefined, useGpsSample surfaces mocked=false"
    - "submitLocation maps accessSensitivity===true to 'sensitive' and false to null, and only sends the access code when policyTag==='code_required'"
    - "submitLocation rethrows the raw RPC error (including the generic 'gps rejected') without destructuring it"
    - "submitSchema requires name and policy tag, treats accessCode as optional, and flags accessCode supplied when policyTag is not code_required"
  artifacts:
    - path: "app/src/features/submit/useGpsSample.ts"
      provides: "high-accuracy GPS sample hook with mock/accuracy/timestamp"
      exports: ["getGpsSample", "GpsSample"]
    - path: "app/src/features/submit/submitLocation.ts"
      provides: "submit_location RPC client wrapper"
      exports: ["submitLocation", "SubmitInput"]
    - path: "app/src/features/submit/submitSchema.ts"
      provides: "Zod schema for the SubmitFlow wizard incl. conditional PIN"
      exports: ["submitSchema"]
  key_links:
    - from: "submitLocation"
      to: "supabase.rpc('submit_location')"
      via: "p_* argument mapping"
      pattern: "supabase.rpc\\('submit_location'"
    - from: "useGpsSample"
      to: "expo-location"
      via: "Accuracy.BestForNavigation + pos.mocked"
      pattern: "BestForNavigation"
---

<objective>
Build the client-side submission service layer under `app/src/features/submit/`: the high-accuracy GPS sample hook (`useGpsSample`, SC1), the `submit_location` RPC wrapper (`submitLocation`, mapping D-09 sensitivity + D-17 conditional PIN), and the SubmitFlow Zod schema (`submitSchema`, SC8). All are `features/**` modules → TDD Guard requires test-first and 100% coverage.

Purpose: Gives the SubmitFlow wizard (04-05) tested, typed building blocks so the screen is pure composition + rendering.
Output: 4 source modules + 3 test files, all green at 100% coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-gps-service-submission/04-RESEARCH.md
@.planning/phases/04-gps-service-submission/04-PATTERNS.md
@.planning/phases/04-gps-service-submission/04-UI-SPEC.md

<interfaces>
<!-- Analogs to mirror. Copy structure; diverge only where noted. -->

GpsSample (target shape, RESEARCH Code Examples):
  { coord: {lat:number; lng:number}; accuracy: number|null; mocked: boolean; timestamp: number }
  getGpsSample(): Promise<GpsSample | { denied: true }>

useCurrentPosition.ts (analog) DIVERGENCES for useGpsSample:
  - use Location.Accuracy.BestForNavigation (NOT Balanced)
  - read pos.mocked, map `pos.mocked ?? false` (iOS undefined → false)
  - return {denied:true} sentinel on non-granted permission (never throw)

updateProfile.ts (analog for submitLocation): supabase.rpc(...) → if(error) throw error
  submit_location p_* args (from 04-01 migration signature):
    p_name, p_lat, p_lng, p_accuracy_m, p_mocked, p_captured_at (ISO string),
    p_policy_tag, p_address, p_access_sensitivity ('sensitive'|null via D-09),
    p_hours, p_access_code (only when policyTag==='code_required', D-17), p_timing_tip
  returns the submission id (data as string)

validation.ts (analog for submitSchema): zod v4 z.object composition
  policyTag enum: ['chill_spot','purchase_required','code_required','public_facility']
  LOCKED error strings come from 04-UI-SPEC §Copywriting Contract — do NOT invent copy
</interfaces>
</context>

<tasks>

<task type="execute" tdd="true">
  <name>Task 1: useGpsSample hook + types.ts (test-first)</name>
  <files>app/src/features/submit/types.ts, app/src/features/submit/useGpsSample.ts, app/src/features/submit/__tests__/useGpsSample.test.ts</files>
  <read_first>
    - app/src/features/locations/useCurrentPosition.ts (the analog — copy permission+read shape, diverge on accuracy + mocked)
    - app/src/features/locations/__tests__/useCurrentPosition.test.ts (expo-location jest-mock pattern)
    - app/src/features/locations/types.ts (types-file conventions)
    - https://docs.expo.dev/versions/v55.0.0/sdk/location (mandatory per app/AGENTS.md before writing Expo code — LocationObject.mocked is Android-only)
  </read_first>
  <behavior>
    - granted permission + fix → returns {coord:{lat,lng}, accuracy, mocked, timestamp}
    - pos.mocked=true → mocked:true; pos.mocked=undefined (iOS) → mocked:false
    - low accuracy fix still returned (server rejects; hook is advisory) — accuracy surfaced as-is
    - permission denied → returns {denied:true}, does not throw
    - requests Location.Accuracy.BestForNavigation (assert the arg passed to getCurrentPositionAsync)
  </behavior>
  <action>
    First write `useGpsSample.test.ts` mocking `expo-location` (mirror useCurrentPosition.test.ts): stub requestForegroundPermissionsAsync (granted/denied) and getCurrentPositionAsync (coords/accuracy/timestamp + mocked true/false/undefined). Then define `GpsSample` and the `{denied:true}` union in `types.ts` and implement `getGpsSample` in `useGpsSample.ts` per the interface above. Map `pos.mocked ?? false`; pass `{ accuracy: Location.Accuracy.BestForNavigation }`; return the denied sentinel without throwing. Keep behavioral logic here (not in the screen) so it is coverage-gated.
  </action>
  <verify>
    <automated>cd app && npm test -- src/features/submit/__tests__/useGpsSample.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test asserts getCurrentPositionAsync was called with accuracy BestForNavigation
    - Test with mocked:undefined asserts result.mocked === false
    - Test with denied permission asserts result is {denied:true} and no throw
    - `npm test` reports 100% coverage for useGpsSample.ts and types.ts
  </acceptance_criteria>
  <done>useGpsSample returns the full GPS sample in high-accuracy mode, normalizes iOS mocked→false, and never throws on denial; tests green at 100%.</done>
</task>

<task type="execute" tdd="true">
  <name>Task 2: submitLocation RPC wrapper (test-first)</name>
  <files>app/src/features/submit/submitLocation.ts, app/src/features/submit/__tests__/submitLocation.test.ts</files>
  <read_first>
    - app/src/features/profile/updateProfile.ts (RPC-call + throw analog)
    - app/src/features/profile/__tests__/updateProfile.test.ts (supabase singleton mock)
    - app/src/features/submit/types.ts (created in Task 1 — extend with SubmitInput)
    - .planning/phases/04-gps-service-submission/04-RESEARCH.md §Code Examples "Submit mutation (client)"
  </read_first>
  <behavior>
    - accessSensitivity true → p_access_sensitivity:'sensitive'; false → null
    - policyTag==='code_required' with accessCode → p_access_code sent; any other policyTag → p_access_code:null even if accessCode present
    - timestamp mapped to ISO string for p_captured_at
    - RPC error → rethrown raw (assert the exact error object propagates; 'gps rejected' NOT destructured or remapped here)
    - success → returns data as the submission id string
  </behavior>
  <action>
    First write `submitLocation.test.ts` mocking `../../lib/supabase` (mirror updateProfile.test.ts): assert `supabase.rpc` is called with `'submit_location'` and the exact `p_*` mapping for each behavior case. Then add `SubmitInput` to `types.ts` and implement `submitLocation(input): Promise<string>` mapping fields per the interface, returning `data as string`, and `if (error) throw error` (rethrow raw — the wizard maps 'gps rejected' to locked ERR copy, not this layer).
  </action>
  <verify>
    <automated>cd app && npm test -- src/features/submit/__tests__/submitLocation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test proves sensitive:true→'sensitive' and sensitive:false→null
    - Test proves accessCode omitted (null) when policyTag !== 'code_required'
    - Test proves an RPC error is rethrown unchanged (no message rewrite)
    - 100% coverage on submitLocation.ts
  </acceptance_criteria>
  <done>submitLocation forwards a correctly-mapped RPC call, enforces the D-09/D-17 client mappings, and rethrows raw errors; tests green at 100%.</done>
</task>

<task type="execute" tdd="true">
  <name>Task 3: submitSchema Zod validation with conditional PIN (test-first)</name>
  <files>app/src/features/submit/submitSchema.ts, app/src/features/submit/__tests__/submitSchema.test.ts</files>
  <read_first>
    - app/src/features/auth/validation.ts (zod v4 composition + how it sources LOCKED copy)
    - .planning/phases/04-gps-service-submission/04-RESEARCH.md §Pattern 7 (submitSchema body)
    - .planning/phases/04-gps-service-submission/04-UI-SPEC.md §Copywriting Contract + §Error states (LOCKED strings)
  </read_first>
  <behavior>
    - name empty → invalid (required); name present → valid
    - policyTag must be one of the 4 enum values
    - accessCode optional (valid when absent) even for code_required (D-19 "optional")
    - accessCode present while policyTag !== 'code_required' → superRefine issue on accessCode
    - accessSensitivity is a boolean (default false); timingTip optional with max length
  </behavior>
  <action>
    First write `submitSchema.test.ts` covering each behavior case with valid/invalid fixtures. Then implement `submitSchema` per RESEARCH §Pattern 7: `z.object` with name (min 1, max 120), address (optional, max 200 — label only per D-05), policyTag enum of the four values, accessSensitivity boolean default false, hours optional, accessCode optional (max 100 per D-20), timingTip optional (max 280), plus a `superRefine` flagging accessCode when policyTag !== 'code_required'. Pull any user-facing error strings verbatim from 04-UI-SPEC — do not invent copy.
  </action>
  <verify>
    <automated>cd app && npm test -- src/features/submit/__tests__/submitSchema.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test: empty name rejected; valid full object accepted
    - Test: accessCode with a non-code_required policyTag produces a validation issue on the accessCode path
    - Test: accessCode absent for code_required still validates (optional)
    - 100% coverage on submitSchema.ts
  </acceptance_criteria>
  <done>submitSchema enforces required name/policy, optional-but-conditional PIN (D-17/D-19), and label-only address (D-05); tests green at 100%.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| wizard form → submitLocation | Client-collected fields serialized into the RPC call |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-11 | Input Validation (Tampering) | submitSchema | mitigate | Zod validates required/optional/conditional fields client-side; the RPC re-validates GPS server-side (client checks are advisory only). |
| T-04-12 | Information Disclosure | error handling | mitigate | submitLocation rethrows the generic 'gps rejected'; no lat/lng/address logged; wizard maps to locked friendly copy (SC7). |
| T-04-13 | Spoofing | client accuracy/mocked | accept | Client cannot be trusted for GPS truth; server (04-01) enforces. Documented boundary (RESEARCH Pitfall 1). |
</threat_model>

<verification>
- `cd app && npm test` full jest suite green, 100% coverage gate satisfied on all new features/submit modules.
- No new package installed (Places autocomplete deferred, OQ-1).
</verification>

<success_criteria>
- SC1 (GpsService hook shape + high-accuracy) delivered by useGpsSample.
- SC8 (Zod validation incl. conditional PIN + error states) delivered by submitSchema.
- D-09 sensitivity mapping and D-17 conditional PIN enforced in submitLocation.
</success_criteria>

<output>
Create `.planning/phases/04-gps-service-submission/04-03-SUMMARY.md` when done.
</output>
