---
phase: 04-gps-service-submission
plan: 05
type: execute
wave: 3
depends_on: [04-03]
files_modified:
  - app/src/app/(tabs)/submit.tsx
  - app/src/app/(components)/SensitivityConfirmModal.tsx
  - app/src/app/(tabs)/__tests__/submit.test.tsx
autonomous: false
requirements: [REQ-SUBMIT, REQ-SENSITIVITY, REQ-TIMING, REQ-CODE-WRITE]

must_haves:
  truths:
    - "A signed-out user opening the Submit tab sees the AuthRequiredModal (D-18) and cannot reach the form"
    - "Step 1 collects name, an address field with a 'No address? Describe the location instead' free-text toggle (D-04), a policy_tag segmented picker, accessibility checkboxes, and a 'Not suitable for kids' switch with the effect explainer (D-10/D-12/D-13)"
    - "The PIN field appears in Step 2 only when policy_tag='code_required' (D-17) with the locked helper copy (D-19)"
    - "Step 3 shows a live GPS accuracy readout and blocks 'I'm at This Location' until GPS is valid, mapping the generic RPC rejection to the locked ERR-01/02/03/08 copy"
    - "Submitting with the sensitivity switch ON triggers the D-15 confirmation dialog before the RPC call"
    - "On success the Success screen shows 'Location Submitted!' with the 2-verification body copy and 'Back to Map'"
  artifacts:
    - path: "app/src/app/(tabs)/submit.tsx"
      provides: "3-step SubmitFlow wizard + Success screen (RHF + Zod)"
      min_lines: 120
    - path: "app/src/app/(components)/SensitivityConfirmModal.tsx"
      provides: "D-15 confirm-before-submit dialog"
  key_links:
    - from: "submit.tsx"
      to: "submitLocation"
      via: "useMutation on final submit with useGpsSample sample"
      pattern: "submitLocation"
    - from: "submit.tsx"
      to: "submitSchema"
      via: "zodResolver(submitSchema)"
      pattern: "zodResolver"
---

<objective>
Build the 3-step SubmitFlow wizard in `app/src/app/(tabs)/submit.tsx` (react-hook-form + Zod, SC8) plus the Success screen and the D-15 sensitivity confirmation dialog. This is the phase's primary new screen and has no single existing analog — compose it from shipped patterns (FilterChipRow toggles, sign-up form layout, DeleteAccountModal confirm, AuthRequiredModal gate). The whole wizard is auth-gated from the start (D-18).

**OQ-1 resolution (explicit out-of-scope call):** Google Places autocomplete is DEFERRED. The address field ships as free-text with the D-04 "No address? Describe the location instead" toggle only — NO live autocomplete, NO Google API key, NO new package. Coordinates always come from the Step-3 GPS fix (D-05), so autocomplete is pure UX sugar; it is deferred like Phase 3 deferred geocoding. This is a deliberate scope decision, not a silent omission.

Purpose: Lets a signed-in, physically-present user submit a bathroom end-to-end.
Output: The wizard screen, the sensitivity dialog, and component tests. Must cite the Component Acceptance Checklist (design-system.md §20, ROADMAP SC10).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-gps-service-submission/04-UI-SPEC.md
@.planning/phases/04-gps-service-submission/04-PATTERNS.md
@docs/design/design-system.md

<interfaces>
<!-- Consume these from 04-03 (same wave, but this plan depends_on 04-03). -->
From app/src/features/submit/: getGpsSample, GpsSample, {denied:true};  submitLocation(input): Promise<string>, SubmitInput;  submitSchema (zod)
From app/src/features/auth/useSession: useSession() → { session, loading } | null

Compose-from analogs (copy sub-patterns; no multi-step analog exists):
  app/src/app/(components)/FilterChipRow.tsx — segmented/toggle visuals + design-token opener
  app/src/app/(auth)/sign-up.tsx — nearest RHF-adjacent field/error form layout
  app/src/app/(components)/DeleteAccountModal.tsx — confirm dialog skeleton (scrim+card+confirm+cancel, submittingRef guard, reset-on-open, onRequestClose no-op)
  app/src/app/(components)/AuthRequiredModal.tsx — D-18 gate; action='submit' is an existing enum value

LOCKED copy (verbatim from 04-UI-SPEC — do NOT paraphrase):
  CTAs: 'Next →' (Steps 1,2), 'I'm at This Location' (Step 3), 'Back to Map' (Success)
  Sensitivity switch label: 'Not suitable for kids'; explainer states it hides the location from Family mode users
  PIN label: 'Door code (optional) — only shown to signed-in users'
  Address affordance: 'No address? Describe the location instead'
  Success: 'Location Submitted!' / 'It'll appear publicly after 2 GPS verifications. You can see it on the map now.'
  ERR-08: 'Couldn't submit your location. Check your connection and try again.'
  ERR-02: 'GPS accuracy too low — move to an open area and try again.'
  ERR-03: 'GPS fix is stale — wait a moment and retry.'
  ERR-01: 'We can't find your location. Use search to browse bathrooms near an address.'
  Sensitivity confirm: 'This location will be hidden from Family mode users'
  Declare all locked strings as `// [LOCKED ...]` consts (mirror index.tsx lines 47-57 convention).
</interfaces>
</context>

<tasks>

<task type="execute">
  <name>Task 1: Wizard scaffold + Step 1 + Step 2 + sensitivity dialog</name>
  <files>app/src/app/(tabs)/submit.tsx, app/src/app/(components)/SensitivityConfirmModal.tsx, app/src/app/(tabs)/__tests__/submit.test.tsx</files>
  <read_first>
    - app/src/app/(tabs)/submit.tsx (current placeholder to replace)
    - app/src/app/(components)/FilterChipRow.tsx (segmented/toggle + design-token opener lines 68-70)
    - app/src/app/(auth)/sign-up.tsx (form field/error layout)
    - app/src/app/(components)/DeleteAccountModal.tsx (confirm-dialog skeleton to copy for SensitivityConfirmModal)
    - app/src/app/(components)/AuthRequiredModal.tsx (D-18 gate + action enum)
    - app/src/features/submit/submitSchema.ts (the resolver schema)
    - .planning/phases/04-gps-service-submission/04-UI-SPEC.md (spacing/typography/color token names + LOCKED copy + §Surface inventory)
    - docs/design/design-system.md §20 (Component Acceptance Checklist — cite in this plan)
  </read_first>
  <action>
    Replace the placeholder with an RHF wizard using `useForm({ resolver: zodResolver(submitSchema) })` and a `step` state (1|2|3|'success'). Open every component with the `Colors[useColorScheme()==='dark'?'dark':'light']` token pattern; import Colors/spacing/typography/radius from ../../constants/*; NO raw hex/inline sizes. Render the `①────○────○` progress indicator using `primary` for done/active dots and `border`/`textDisabled` for upcoming.
    Auth gate (D-18): read `useSession()`; if there is no session, render `AuthRequiredModal` (action='submit') inline (never a hard redirect) and do not mount the form.
    Step 1: name (required text input), address field with the D-04 free-text toggle — a Pressable text affordance labeled 'No address? Describe the location instead' that switches the field into plain free-text mode (NO autocomplete this phase, OQ-1 deferred); policy_tag segmented picker (4 values: chill_spot/purchase_required/code_required/public_facility) styled active=primary fill+textInverse, inactive=surface+border, minHeight 44, accessibilityState selected; accessibility checkboxes; and the `access_sensitivity` control as an RN `Switch` (D-13, distinct from the segmented picker) with ON-track tint `primary`, the LOCKED label 'Not suitable for kids', and the D-12 effect-explainer subhead underneath. 'Next →' advances (Step 1 validates before advancing).
    Step 2: hours input, the conditional PIN field rendered ONLY when the watched policy_tag === 'code_required' (D-17) with the LOCKED helper copy 'Door code (optional) — only shown to signed-in users', and a timing-tip input. Back/Next row.
    Create `SensitivityConfirmModal.tsx` by copying DeleteAccountModal's scrim+card structure (reset-on-open effect; onRequestClose no-op; explicit Cancel; standard confirm button = `primary`, this dialog is non-destructive so it may skip the network submittingRef guard). Body copy LOCKED: 'This location will be hidden from Family mode users'. It is wired/fired in Task 2 (Step 3 final submit).
    Write `submit.test.tsx` (TDD Guard requires test-first for src/app screens even though they are outside the coverage gate): assert the signed-out gate shows AuthRequiredModal; assert the PIN field is absent for a non-code_required policy_tag and present for code_required; assert the sensitivity switch label renders.
  </action>
  <verify>
    <automated>cd app && npm test -- src/app/(tabs)/__tests__/submit.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Signed-out render shows the AuthRequiredModal and no form fields (test asserts)
    - PIN field is conditional on policy_tag==='code_required' (test asserts present/absent)
    - Exact LOCKED strings present: 'Not suitable for kids', 'Door code (optional) — only shown to signed-in users', 'No address? Describe the location instead'
    - `access_sensitivity` uses an RN Switch (not a Pressable chip)
    - No raw hex color or inline fontSize literals in the file (tokens only)
    - No Google Places / autocomplete package import anywhere
  </acceptance_criteria>
  <done>Steps 1-2, the auth gate, and the sensitivity dialog render with locked copy, conditional PIN, and a switch-based sensitivity control; tests green.</done>
</task>

<task type="execute">
  <name>Task 2: Step 3 GPS confirm + submit wiring + Success screen</name>
  <files>app/src/app/(tabs)/submit.tsx, app/src/app/(tabs)/__tests__/submit.test.tsx</files>
  <read_first>
    - app/src/app/(tabs)/submit.tsx (Steps 1-2 from Task 1)
    - app/src/features/submit/useGpsSample.ts (getGpsSample + {denied:true})
    - app/src/features/submit/submitLocation.ts (SubmitInput mapping)
    - app/src/app/(tabs)/index.tsx lines 47-57 (LOCKED-const declaration convention)
    - .planning/phases/04-gps-service-submission/04-UI-SPEC.md §Error states + §"SubmitFlow Step 3" + §Destructive actions (D-15)
  </read_first>
  <action>
    Step 3: call `getGpsSample()` and render a live accuracy readout — 'good' uses successGreen + checkmark-circle icon, 'poor' uses warningAmber + warning-outline (design-system §18.4); every status pairs color+icon+text. Render the inline GPS error area mapping states to LOCKED copy: denied sample ({denied:true}) → ERR-01; accuracy > 50m → ERR-02 with the 'I'm at This Location' button DISABLED (re-enables live as accuracy improves); stale fix (>60s old vs timestamp) → ERR-03 with button disabled + a Retry that requests a fresh read. The 56pt 'I'm at This Location' primary CTA is enabled only when the sample is present, non-mocked-advisory, accuracy ≤ 50m, and fresh.
    On press: if the sensitivity switch is ON, open SensitivityConfirmModal (D-15) and only proceed on confirm; otherwise submit directly. Submit via `useMutation` calling `submitLocation` with the mapped SubmitInput (name, lat/lng/accuracy/mocked/timestamp from the sample, policyTag, address label, accessSensitivity boolean, hours, accessCode, timingTip). On RPC error (including the generic 'gps rejected') show ERR-08 inline below Submit with a Retry secondary button and PRESERVE form data (do not clear). Never surface the specific rejection reason (SC7).
    On success advance to the Success screen: checkmark, LOCKED 'Location Submitted!' headline, the LOCKED 2-verification body copy, and a 'Back to Map' button navigating to the map tab. Extend `submit.test.tsx`: assert ERR-02 disables the CTA at accuracy>50; assert the sensitivity-ON path opens the confirm dialog before calling submitLocation (mock the mutation); assert a submit error renders ERR-08 and keeps the form.
  </action>
  <verify>
    <automated>cd app && npm test -- src/app/(tabs)/__tests__/submit.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - 'I'm at This Location' is disabled while accuracy > 50m and re-enables when it drops (test asserts)
    - Sensitivity switch ON opens SensitivityConfirmModal before submitLocation is called (test asserts via mocked mutation)
    - Submit failure renders LOCKED ERR-08 and does not clear the form (test asserts)
    - Success screen renders LOCKED 'Location Submitted!' + the 2-verification body copy + 'Back to Map'
    - The RPC rejection reason is never rendered (only mapped ERR-01/02/03/08 copy)
  </acceptance_criteria>
  <done>Step 3 gates submission on a valid GPS sample, honors the D-15 confirm gate, maps all errors to locked copy, and reaches the Success screen; tests green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Device walkthrough — SubmitFlow wizard end-to-end</name>
  <action>Pause for the user to verify the SubmitFlow wizard on a device — real GPS accuracy, permission prompts, and the mock-location path cannot be observed in jest (deferred device-UAT pattern from Phase 3). Run only after all automated tests pass.</action>
  <what-built>The full 3-step SubmitFlow wizard (auth gate, Step 1 fields incl. sensitivity switch, conditional PIN in Step 2, Step 3 GPS confirm with error states, sensitivity confirm dialog, Success screen).</what-built>
  <how-to-verify>
    Device walkthrough (real GPS + permission prompts cannot be verified in jest — consistent with the project's deferred device-UAT pattern from Phase 3):
    1. Sign out → open the Submit tab → confirm the AuthRequiredModal appears and the form is unreachable.
    2. Sign in → Step 1: enter a name, tap 'No address? Describe the location instead' and confirm free-text mode, pick a policy tag, toggle 'Not suitable for kids' ON and read the effect explainer.
    3. Step 2: select 'Code Required' policy and confirm the 'Door code (optional) — only shown to signed-in users' field appears; switch to another policy and confirm it disappears. Enter a timing tip.
    4. Step 3: confirm the live accuracy readout; indoors/low-accuracy → 'I'm at This Location' stays disabled with ERR-02; move to open area → it enables.
    5. Because sensitivity is ON, confirm the D-15 dialog fires before submit; approve → confirm the Success screen ('Location Submitted!') and that 'Back to Map' returns to the map.
    Component Acceptance Checklist (design-system.md §20) must be walked for this screen before Codex review.
  </how-to-verify>
  <resume-signal>Type "verified" once the walkthrough passes, or describe issues. Device UAT may be deferred as a tracked item consistent with Phase 3 if no device is available — record it, do not silently skip.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| unauthenticated user → Submit tab | D-18 requires sign-in before the form is reachable |
| wizard input → submit_location | Client fields + GPS sample cross to the server |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-17 | Elevation of Privilege | anonymous submit | mitigate | AuthRequiredModal gates the whole wizard (D-18); server also rejects anon (04-01). |
| T-04-18 | Information Disclosure | error copy | mitigate | Only mapped ERR-01/02/03/08 strings shown; generic 'gps rejected' never surfaced (SC7). |
| T-04-19 | Tampering | submitting sensitive content silently | mitigate | D-15 confirm dialog forces a conscious acknowledgement before hiding from Family mode. |
| T-04-SC | Tampering | package install | accept | No package installed — Places autocomplete deferred (OQ-1). |
</threat_model>

<verification>
- `cd app && npm test` green including submit.test.tsx.
- Component Acceptance Checklist (§20) cited and walked.
- No autocomplete/Places dependency introduced.
</verification>

<success_criteria>
- SC8 (SubmitFlow validates with Zod, handles denied/low-accuracy/failed-write states) delivered.
- SC5/D-10/D-12/D-13/D-15 sensitivity UX delivered; SC10 checklist cited.
- SC4 access code + timing tip captured in Step 2 and forwarded on submit.
</success_criteria>

<output>
Create `.planning/phases/04-gps-service-submission/04-05-SUMMARY.md` when done.
</output>
