---
phase: "02-auth-profiles"
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
autonomous: false   # [ASSUMED]-package install checkpoint + Supabase dashboard config + [BLOCKING] supabase db push
requirements: [REQ-2-2, REQ-2-3, REQ-2-5, REQ-2-6]
files_modified:
  - app/package.json
  - supabase/migrations/20260627000003_nullable_user_fks.sql
  - supabase/migrations/20260627000004_profile_rpcs.sql
  - app/src/features/auth/oauth.ts
  - app/src/features/profile/updateProfile.ts
  - app/src/features/profile/deleteAccount.ts
  - app/src/features/profile/profileStats.ts
  - app/src/features/auth/__tests__/oauth.test.ts
  - app/src/features/profile/__tests__/updateProfile.test.ts
  - app/src/features/profile/__tests__/deleteAccount.test.ts
  - app/src/features/profile/__tests__/profileTrigger.test.ts
  - app/src/app/auth/callback.tsx
  - app/src/app/(auth)/sign-in.tsx
  - app/src/app/(auth)/sign-up.tsx
  - app/src/app/(tabs)/profile.tsx
  - app/src/app/(components)/AuthRequiredModal.tsx
  - app/src/app/(components)/DeleteAccountModal.tsx

user_setup:
  - service: google-oauth
    why: "Google OAuth provider for Android sign-in (REQ-2-2)"
    env_vars:
      - name: GOOGLE_CLIENT_ID
        source: "Google Cloud Console → APIs & Services → Credentials → OAuth 2.0 Client ID (Web), added as an EAS secret"
      - name: GOOGLE_SECRET
        source: "Same OAuth client; stored as an EAS secret / Supabase dashboard, never in the repo"
    dashboard_config:
      - task: "Enable Google provider with client id/secret"
        location: "Supabase Dashboard (project ebmzhjmmtmldhrojkdqw) → Authentication → Providers → Google"
      - task: "Add gotta-go://auth/callback (and gotta-go://**) to the redirect allow-list"
        location: "Supabase Dashboard → Authentication → URL Configuration → Redirect URLs"
      - task: "Confirm email confirmations are DISABLED remotely (matches config.toml)"
        location: "Supabase Dashboard → Authentication → Providers → Email"
  - service: legal-urls
    why: "Termly Privacy Policy + Terms of Service URLs for onboarding + Settings (REQ-2-6)"
    dashboard_config:
      - task: "Provide the live Termly Privacy Policy and ToS URLs to populate LEGAL_URLS"
        location: "Termly account → Published policy URLs"

must_haves:
  truths:
    - "On Android the Sign-In screen shows a functional 'Continue with Google' button that opens the consent browser and returns a session via the gotta-go://auth/callback deep link"
    - "On iOS the Sign-In screen shows a disabled 'Sign in with Apple — coming soon' stub and NO Google button (Apple guideline 4.8)"
    - "The deep-link callback parses the PKCE code and exchanges it for a session"
    - "display_name is written to public.users after signup via the update_profile RPC"
    - "User can delete their account: type DELETE → delete_account RPC anonymizes 7 FK tables and deletes auth.users → SIGNED_OUT → Welcome"
    - "Account deletion no longer fails on NOT NULL/RESTRICT FKs — 7 columns are nullable ON DELETE SET NULL"
    - "Settings (in Profile) exposes sign out, Privacy Policy link, Terms of Service link, Delete Account entry, and a Location Permissions section with an OS-settings deep link"
    - "The handle_new_user trigger from 02-01 is proven to create a public.users row on signup"
  artifacts:
    - path: "supabase/migrations/20260627000003_nullable_user_fks.sql"
      provides: "7 FK columns altered to nullable ON DELETE SET NULL"
      contains: "on delete set null"
    - path: "supabase/migrations/20260627000004_profile_rpcs.sql"
      provides: "update_profile + delete_account SECURITY DEFINER RPCs"
      contains: "delete_account"
    - path: "app/src/features/auth/oauth.ts"
      provides: "Google OAuth: build redirect, open browser, parse callback, exchange code"
      exports: ["signInWithGoogle", "handleAuthCallback"]
    - path: "app/src/features/profile/deleteAccount.ts"
      provides: "delete_account RPC caller"
      exports: ["deleteAccount"]
    - path: "app/src/features/profile/updateProfile.ts"
      provides: "update_profile RPC caller (sets display_name post-signup)"
      exports: ["updateProfile"]
    - path: "app/src/app/auth/callback.tsx"
      provides: "deep-link target route for OAuth + recovery"
      contains: "exchangeCodeForSession"
  key_links:
    - from: "app/src/app/(auth)/sign-in.tsx"
      to: "app/src/features/auth/oauth.ts"
      via: "Platform.OS==='android' Google button → signInWithGoogle"
      pattern: "Platform.OS|signInWithGoogle"
    - from: "app/src/app/auth/callback.tsx"
      to: "supabase.auth.exchangeCodeForSession"
      via: "handleAuthCallback"
      pattern: "exchangeCodeForSession"
    - from: "app/src/app/(tabs)/profile.tsx (Delete modal)"
      to: "app/src/features/profile/deleteAccount.ts"
      via: "type-DELETE confirm → deleteAccount → delete_account RPC"
      pattern: "deleteAccount|delete_account"
    - from: "app/src/app/(auth)/sign-up.tsx"
      to: "app/src/features/profile/updateProfile.ts"
      via: "post-signUp call sets display_name"
      pattern: "updateProfile|update_profile"
---

<objective>
Complete Phase 2: Google OAuth (Android-only) + Apple Sign-In stub (iOS) with the deep-link callback,
the update_profile and delete_account RPCs (with the nullable-FK migration that unblocks deletion), the
Profile + Settings stub (sign out, privacy/ToS links, delete entry, location permissions), the
Auth-Required modal, and a test proving the handle_new_user trigger provisions profiles.

Purpose: Land OAuth, the GDPR/Apple-5.1.1 account-deletion path, profile provisioning, and the compliance
links — the remaining REQ-2-2/3/5/6 surface — on top of the 02-01 auth foundation.

Output: Two Wave 0 migrations applied live; covered oauth/profile modules; OAuth UI + callback route;
Profile + Settings + Delete/Auth-Required modals; profile-trigger integration test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@CLAUDE.md
@.planning/phases/02-auth-profiles/02-CONTEXT.md
@.planning/phases/02-auth-profiles/02-RESEARCH.md
@.planning/phases/02-auth-profiles/02-PATTERNS.md
@.planning/phases/02-auth-profiles/02-UI-SPEC.md
@docs/design/design-system.md
@docs/schema-contract.md
@.planning/phases/02-auth-profiles/02-01-SUMMARY.md

<interfaces>
<!-- Contracts produced by 02-01 + the codebase. Use directly. -->

From 02-01 (already built):
  app/src/features/auth/SessionProvider.tsx → SessionProvider, useSession (signOut())
  app/src/features/auth/validation.ts → signUpSchema/signInSchema and field schemas
  app/src/features/auth/displayName.ts → checkDisplayNameAvailable, isDisplayNameTakenError
  app/src/constants/legal.ts → LEGAL_URLS { termsOfService, privacyPolicy }
  app/src/app/(auth)/sign-in.tsx → has a marked placeholder region below the divider for OAuth/Apple buttons
  app/src/app/(auth)/sign-up.tsx → calls supabase.auth.signUp; this plan wires the post-signup updateProfile call
  handle_new_user trigger + users_display_name_lower_uniq index + check_display_name_available/set_gps_consent RPCs (applied)

Deep-link scheme (authoritative): gotta-go://auth/callback (app.config.ts scheme 'gotta-go').

OAuth flow (RESEARCH §Pattern 3): supabase-js v2 defaults to PKCE → callback carries ?code= →
  supabase.auth.exchangeCodeForSession(code). Build redirect via expo-auth-session makeRedirectUri({ path: 'auth/callback' });
  open with WebBrowser.openAuthSessionAsync(data.url, redirectTo) after signInWithOAuth({ provider:'google',
  options:{ redirectTo, skipBrowserRedirect:true } }); call WebBrowser.maybeCompleteAuthSession() once at module top.

FK columns to make nullable + ON DELETE SET NULL (PATTERNS table, verified line numbers in 20260519010000):
  submissions.submitter_id, ratings.user_id, trust_events.user_id, verification_events.user_id,
  reports.user_id, failure_events.user_id, flags.reporter_id  (7 total — CONTEXT §6 names six; flags.reporter_id is the 7th).
  ratings.unique(user_id, location_id) is unaffected (NULLs are distinct in Postgres unique indexes).

SECURITY DEFINER + grant style: copy supabase/migrations/20260624000002_ratings_privacy_fix.sql:13-58,70-75.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Nullable-FK migration + update_profile/delete_account RPCs + supabase db push</name>
  <files>supabase/migrations/20260627000003_nullable_user_fks.sql, supabase/migrations/20260627000004_profile_rpcs.sql</files>
  <read_first>
    - .planning/phases/02-auth-profiles/02-CONTEXT.md (§6 account deletion, SET NULL strategy)
    - .planning/phases/02-auth-profiles/02-PATTERNS.md (FK columns table with line numbers; delete_account body; SECURITY DEFINER + grant style; flags.reporter_id 7th ref)
    - .planning/phases/02-auth-profiles/02-RESEARCH.md (§Pattern 4 delete_account, §Pitfall 3 FK blocker)
    - supabase/migrations/20260519010000_remote_schema.sql (current NOT NULL FK definitions to alter)
    - supabase/migrations/20260624000002_ratings_privacy_fix.sql (style)
    - docs/schema-contract.md (migration review checklist)
  </read_first>
  <action>
    Migration 3 (nullable_user_fks): for each of the 7 columns — submissions.submitter_id, ratings.user_id,
    trust_events.user_id, verification_events.user_id, reports.user_id, failure_events.user_id, flags.reporter_id —
    `alter table ... alter column ... drop not null;` then drop the existing FK constraint and re-add it as
    `references users(id) on delete set null` (so deleting a user anonymizes rather than RESTRICT-blocks). Open with the
    header-comment block citing CONTEXT §6 + Pitfall 3 and the live-schema gap; use `-- ─── N. Title ──` dividers.
    Migration 4 (profile_rpcs): `update_profile(new_display_name text) returns void language plpgsql security definer
    set search_path = public as $$ begin if auth.uid() is null then raise exception 'not authenticated'; end if;
    update public.users set display_name = new_display_name, updated_at = now() where id = auth.uid(); end; $$;` granted to
    `authenticated` — sets display_name post-signup (CONTEXT §10; relies on the lower(display_name) unique index from 02-01 as
    the race-safe backstop). And `delete_account() returns void language plpgsql security definer set search_path = public, auth
    as $$ declare uid uuid := auth.uid(); begin if uid is null then raise exception 'not authenticated'; end if;
    update submissions set submitter_id = null where submitter_id = uid; update ratings set user_id = null where user_id = uid;
    update trust_events set user_id = null where user_id = uid; update verification_events set user_id = null where user_id = uid;
    update reports set user_id = null where user_id = uid; update failure_events set user_id = null where user_id = uid;
    update flags set reporter_id = null where reporter_id = uid; delete from auth.users where id = uid; end; $$;` granted to
    `authenticated` — anonymizes community data then deletes the auth user (cascades to public.users). T-02-03 mitigated at the
    UI layer (type-DELETE), the RPC enforces auth.uid() server-side. Then run `cd supabase && supabase db push` to apply live.
    [BLOCKING]: Tasks 3–6 must not start until the push succeeds.
  </action>
  <acceptance_criteria>
    - `grep -ci 'on delete set null' supabase/migrations/20260627000003_nullable_user_fks.sql` returns 7 (after `grep -v '^--'`).
    - `grep -in 'drop not null' supabase/migrations/20260627000003_nullable_user_fks.sql` lists all 7 target columns.
    - `grep -n 'update_profile' supabase/migrations/20260627000004_profile_rpcs.sql` and `grep -n 'delete_account' supabase/migrations/20260627000004_profile_rpcs.sql` match, each with a following `grant execute ... to authenticated`.
    - `grep -ic 'set user_id = null\|set submitter_id = null\|set reporter_id = null' supabase/migrations/20260627000004_profile_rpcs.sql` (filtered for comments) reflects all 7 anonymized columns.
    - `supabase db push` completes with no error (paste output into SUMMARY); Supabase MCP confirms update_profile + delete_account are callable.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: [ASSUMED-package + dashboard] expo-auth-session verify, install, and Supabase OAuth config</name>
  <read_first>
    - .planning/phases/02-auth-profiles/02-RESEARCH.md (## Package Legitimacy Audit; §Pitfall 1 scheme; §Pitfall 2 Google provider; §Environment Availability)
    - .planning/phases/02-auth-profiles/02-CONTEXT.md (§2 Google OAuth)
  </read_first>
  <what-built>
    Nothing yet. This gate covers (a) the [ASSUMED] expo-auth-session package and (b) the live-project OAuth config
    that code cannot set. RESEARCH tagged expo-auth-session [ASSUMED] because slopcheck was unavailable.
  </what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/expo-auth-session — confirm publisher is Expo (github.com/expo/expo), sdk-55 dist-tag = 55.0.17, no suspicious postinstall.
    2. After approval the executor runs `cd app && npx expo install expo-auth-session` (Expo-resolved pin; jest must stay 29.x).
    3. In the Supabase Dashboard (project ebmzhjmmtmldhrojkdqw): enable the Google provider with the Google OAuth client id/secret (EAS secret), add `gotta-go://auth/callback` and `gotta-go://**` to the redirect allow-list (T-02-02), and confirm email confirmations are disabled.
    4. Confirm GOOGLE_CLIENT_ID / GOOGLE_SECRET exist as EAS secrets — never committed.
  </how-to-verify>
  <resume-signal>Type "approved — provider configured" once the package is verified and the dashboard is set, or describe blockers.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Covered OAuth + profile modules (oauth, updateProfile, deleteAccount, profileStats)</name>
  <files>app/src/features/auth/oauth.ts, app/src/features/profile/updateProfile.ts, app/src/features/profile/deleteAccount.ts, app/src/features/profile/profileStats.ts, and colocated __tests__/*.test.ts</files>
  <behavior>
    - oauth.signInWithGoogle: builds redirectTo via makeRedirectUri({ path:'auth/callback' }); calls signInWithOAuth({ provider:'google', options:{ redirectTo, skipBrowserRedirect:true } }); opens WebBrowser.openAuthSessionAsync(data.url, redirectTo); on result.type==='success' parses the code and returns it; on 'cancel'/'dismiss' returns null without throwing.
    - oauth.handleAuthCallback(url): extracts the PKCE code from the url and calls supabase.auth.exchangeCodeForSession(code); returns the session or throws on error.
    - updateProfile(displayName): calls rpc('update_profile', { new_display_name }); surfaces isDisplayNameTakenError mapping on 23505.
    - deleteAccount: calls rpc('delete_account'); on success the SIGNED_OUT event (handled by 02-01 SessionProvider) drives the redirect — the module itself does not navigate.
    - profileStats: a TanStack Query fetcher returning { gpsVerifications, locationsSubmitted, ratingsGiven } for the signed-in user; never fetched inside SessionProvider (CONTEXT §1/#3).
  </behavior>
  <read_first>
    - .planning/phases/02-auth-profiles/02-RESEARCH.md (§Pattern 3 OAuth, §Pattern 4 delete_account, §Code Examples)
    - .planning/phases/02-auth-profiles/02-PATTERNS.md (service import convention; test mock + isolateModules; jest.setup expo-web-browser mock added in 02-01)
    - app/src/lib/__tests__/supabase.test.ts (mock structure)
    - app/src/lib/supabase.ts (import { supabase })
    - app/src/features/auth/displayName.ts (reuse isDisplayNameTakenError)
  </read_first>
  <action>
    TDD test-first for all four modules (TDD-Guard-ON, 100% coverage). Import the shared `{ supabase }` client and
    expo-auth-session (makeRedirectUri, QueryParams) + expo-web-browser per the behavior block. oauth.ts calls
    WebBrowser.maybeCompleteAuthSession() once at module top. Branch the callback on PKCE `code` (supabase-js v2 default) —
    verify the installed v2.106 flowType default during RED and assert the code path; do not assume implicit tokens
    (RESEARCH A2). deleteAccount/updateProfile are thin rpc callers; profileStats is the lazy stats fetcher. Keep ALL logic
    here so the screens in Tasks 4–5 stay thin.
  </action>
  <acceptance_criteria>
    - `cd app && npm test -- features/auth/oauth.test.ts features/profile/updateProfile.test.ts features/profile/deleteAccount.test.ts features/profile/profileStats.test.ts` exits 0.
    - `cd app && npm run test:coverage` shows 100% for all four new files.
    - `grep -n 'exchangeCodeForSession' app/src/features/auth/oauth.ts`, `grep -n "rpc('update_profile'\|rpc(\"update_profile\"" app/src/features/profile/updateProfile.ts`, and `grep -n "rpc('delete_account'\|rpc(\"delete_account\"" app/src/features/profile/deleteAccount.ts` all match.
    - An oauth test asserts the 'cancel' result returns null without throwing (no crash on user-dismiss).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: OAuth/Apple buttons on Sign-In + deep-link callback route + post-signup updateProfile wiring</name>
  <files>app/src/app/(auth)/sign-in.tsx, app/src/app/auth/callback.tsx, app/src/app/(auth)/sign-up.tsx, and colocated tests</files>
  <read_first>
    - .planning/phases/02-auth-profiles/02-UI-SPEC.md (Screen 2 Sign-In OAuth/Apple rows; Platform divergence table; §20 below)
    - .planning/phases/02-auth-profiles/02-CONTEXT.md (§2 Google Android-only, iOS Apple stub copy)
    - app/src/app/(auth)/sign-in.tsx (the placeholder region left by 02-01)
    - app/src/app/(auth)/sign-up.tsx (signUp call to extend)
    - app/src/features/auth/oauth.ts, app/src/features/profile/updateProfile.ts (consumed here)
  </read_first>
  <action>
    sign-in.tsx: in the marked region below the divider, render with a `Platform.OS === 'android'` gate a Secondary
    "Continue with Google" button (48pt) calling signInWithGoogle (Task 3); on iOS render instead a DISABLED stub button
    "Sign in with Apple — coming soon" (textDisabled border+label, accessibilityState disabled) and NO Google button
    (Apple guideline 4.8 — T-02-02 redirect allow-list backs this server-side). callback.tsx: a thin route at
    app/src/app/auth/callback.tsx that on mount reads the incoming deep-link URL and calls handleAuthCallback (exchanges the
    PKCE code → session); on success let the 02-01 guard route to /(tabs)/index; on error route back to /(auth)/sign-in with
    the generic network-failure copy. sign-up.tsx: after a successful supabase.auth.signUp, call updateProfile(displayName)
    to persist display_name to public.users (handle_new_user only set id+email), then navigate to /gps-consent; map a
    23505/taken result to "That display name is already taken." Keep all screens thin (logic in features/**). Cite §20 below.
  </action>
  <acceptance_criteria>
    - `cd app && npm test -- app/(auth) app/auth/callback` exits 0 (Platform-gate render test: Android shows Google + no Apple stub; iOS shows disabled Apple stub + no Google).
    - `grep -n "Platform.OS === 'android'" app/src/app/(auth)/sign-in.tsx` and `grep -n 'signInWithGoogle' app/src/app/(auth)/sign-in.tsx` match; `grep -n 'coming soon' app/src/app/(auth)/sign-in.tsx` matches.
    - `grep -n 'handleAuthCallback\|exchangeCodeForSession' app/src/app/auth/callback.tsx` matches.
    - `grep -n 'updateProfile' app/src/app/(auth)/sign-up.tsx` matches and follows the signUp call.
    - `grep -rn '#[0-9A-Fa-f]\{6\}' app/src/app/(auth)/sign-in.tsx app/src/app/auth/callback.tsx | grep -v '^\s*//'` returns nothing.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 5: Profile + Settings stub, Delete Account modal, Auth-Required modal</name>
  <files>app/src/app/(tabs)/profile.tsx, app/src/app/(components)/DeleteAccountModal.tsx, app/src/app/(components)/AuthRequiredModal.tsx, and colocated tests</files>
  <read_first>
    - .planning/phases/02-auth-profiles/02-UI-SPEC.md (Screen 5 Profile signed-in, Screen 6 unauthenticated, Screen 7 Auth-Required modal, Screen 9 Settings stub; Delete confirmation modal; Copywriting Contract; §20 below)
    - .planning/phases/02-auth-profiles/02-CONTEXT.md (§6 deletion UX; §4 sign-out)
    - app/src/app/(tabs)/profile.tsx (current stub)
    - app/src/features/profile/deleteAccount.ts, profileStats.ts, app/src/features/auth/SessionProvider.tsx (signOut), app/src/constants/legal.ts (LEGAL_URLS)
  </read_first>
  <action>
    profile.tsx: render conditionally on useSession — signed-in shows avatar placeholder, display_name, masked email, a Stats
    section lazily loaded via profileStats (TanStack Query; skeletonBase/skeletonHighlight while loading, "—" on error), and a
    Settings section with rows: Account (stub "Coming soon"), Privacy Policy → Linking.openURL(LEGAL_URLS.privacyPolicy),
    Terms of Service → Linking.openURL(LEGAL_URLS.termsOfService) (REQ-2-6), Delete Account → opens DeleteAccountModal, Sign Out
    → SessionProvider signOut() (→ SIGNED_OUT → Welcome), and a Location Permissions row explaining current consent state with
    an "Open Settings" button → Linking.openSettings() (ROADMAP SC-10). Unauthenticated shows the "Sign in to contribute" CTA
    (Primary "Sign In" + Secondary "Create Account") + muted stats preview — no router.replace redirect (public per Pattern 2).
    DeleteAccountModal.tsx: title "Delete Account", body copy verbatim, a "Type DELETE to confirm" input, a Destructive
    "Delete Account" button DISABLED until the input equals exactly "DELETE" (case-sensitive — T-02-03), calling deleteAccount();
    on failure show "Couldn't delete your account. Check your connection and try again."; swipe-to-dismiss FORBIDDEN, explicit
    Cancel only. AuthRequiredModal.tsx: ERR-10 inline slide-up (not a navigation redirect) with action-specific heading "Sign in
    to [verify/rate/report/submit/see access code]", Primary "Sign In" + Secondary "Create Account" + Ghost "Cancel"; reduced-motion
    instant appear. Cite §20 below. Use tokens only; touch targets ≥44pt, Primary ≥56pt.
  </action>
  <acceptance_criteria>
    - `cd app && npm test -- app/(tabs)/profile (components)/DeleteAccountModal (components)/AuthRequiredModal` exits 0.
    - DeleteAccountModal test asserts the Destructive button is disabled until input === "DELETE" and that tapping it calls deleteAccount (T-02-03 proof).
    - `grep -n 'openSettings' app/src/app/(tabs)/profile.tsx`, `grep -n 'LEGAL_URLS' app/src/app/(tabs)/profile.tsx`, and `grep -n 'signOut' app/src/app/(tabs)/profile.tsx` all match.
    - `grep -n 'Couldn.t delete your account' app/src/app/(components)/DeleteAccountModal.tsx` matches; AuthRequiredModal is a modal (no router.replace) — `grep -n 'router.replace' app/src/app/(components)/AuthRequiredModal.tsx` returns nothing.
    - `grep -rn '#[0-9A-Fa-f]\{6\}' app/src/app/(tabs)/profile.tsx app/src/app/(components)/DeleteAccountModal.tsx app/src/app/(components)/AuthRequiredModal.tsx | grep -v '^\s*//'` returns nothing.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 6: Profile-trigger provisioning verification test</name>
  <files>app/src/features/profile/__tests__/profileTrigger.test.ts</files>
  <behavior>
    - Asserts that the signup path leads to a public.users row keyed by the new auth user id with email populated and display_name initially unset by the trigger (set later by update_profile).
    - Asserts the client never performs a direct INSERT into users (provisioning is trigger-only; no INSERT RLS policy).
  </behavior>
  <read_first>
    - .planning/phases/02-auth-profiles/02-CONTEXT.md (§10 handle_new_user body; §7 existing assets)
    - .planning/phases/02-auth-profiles/02-RESEARCH.md (§Pitfall 4 trigger/INSERT policy; §Validation Architecture)
    - app/src/lib/__tests__/supabase.test.ts (mock structure)
    - supabase/migrations/20260627000000_handle_new_user_trigger.sql (from 02-01)
  </read_first>
  <action>
    Add a covered test (TDD) that mocks supabase.auth.signUp + a users select and asserts the provisioning contract: after
    signUp the users row exists with id+email and no client-side INSERT was issued (the only write to users is via the SECURITY
    DEFINER trigger / update_profile RPC). This closes ROADMAP SC-3 ("User row is auto-created on signup, trigger confirmed").
    If a live integration check is preferred over a mocked unit test, additionally use Supabase MCP to confirm the trigger fired
    for a throwaway test account and record the result in the SUMMARY (clean up the test account afterward).
  </action>
  <acceptance_criteria>
    - `cd app && npm test -- features/profile/profileTrigger.test.ts` exits 0.
    - The test asserts NO direct `from('users').insert(` is called by the client signup path.
    - SUMMARY records the trigger-confirmation evidence (mocked assertion and/or live MCP check output) satisfying SC-3.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OAuth provider + browser → app | Redirect/deep-link returns tokens/code into the app; manipulable redirect URIs |
| client → Postgres RPC | delete_account/update_profile cross into elevated SECURITY DEFINER functions |
| client UI → account deletion | Irreversible destructive action triggered from the device |
| npm registry → build | expo-auth-session third-party code enters the trusted build |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02 | Tampering/Elevation | OAuth redirect URI | mitigate | Strict Supabase redirect allow-list `gotta-go://auth/callback` (+`gotta-go://**`); redirect built via makeRedirectUri (never a literal); PKCE exchangeCodeForSession (Tasks 2,3,4). |
| T-02-03 | Repudiation/Destruction | Delete Account modal + delete_account RPC | mitigate | Type-`DELETE` (case-sensitive) gate disables the button until exact match; RPC re-checks auth.uid() server-side; deletion anonymizes FK children then deletes auth.users (Tasks 1,5). |
| T-02-DEL-FK | Denial of Service | account deletion vs NOT NULL/RESTRICT FKs | mitigate | 7 FK columns altered nullable ON DELETE SET NULL so deletion cannot be blocked or orphan identity data (Task 1; GDPR). |
| T-02-PII | Information Disclosure | Profile stats / logs | mitigate | No email/display_name/GPS in console.log or telemetry; masked email in UI; stats fetched lazily, never logged (Task 5, §20 Security checklist). |
| T-02-SC | Tampering | npx expo install expo-auth-session | mitigate | [ASSUMED] package gated behind blocking-human checkpoint (Task 2) verifying Expo publisher/repo/no-postinstall; Expo-resolved pin only. |
</threat_model>

## Component Acceptance Checklist (from docs/design/design-system.md §20)

### Visual Tokens
- [ ] All color values reference `Colors[colorScheme].tokenName` — no raw hex strings in component StyleSheet
- [ ] All text sizes reference the typography scale (`typography.ts` constants) — no inline `fontSize` raw numbers
- [ ] All spacing values reference the spacing scale (`spacing.ts` constants) — no magic numbers
- [ ] Dark mode tested: toggle OS dark mode setting, verify no color inversion artifacts or invisible elements
- [ ] Policy tag badge colors match Section 8 token table — `code_required` uses `textPrimary` text (not white)
- [ ] Confidence badge colors match Section 9 — `confidenceMedium` uses `textPrimary` text (not white)

### Accessibility
- [ ] All tappable elements have `accessibilityLabel` (non-empty string)
- [ ] All tappable elements have `accessibilityRole` (see Section 18.3 role table)
- [ ] `accessibilityHint` added on any element whose action outcome is not obvious from the label
- [ ] `accessibilityState` reflects current state: `{ disabled, selected, checked, expanded }` as applicable
- [ ] Rating inputs have `accessibilityValue={{ min: 1, max: 5, now: N, text: 'N out of 5 — Label' }}`
- [ ] No status uses color as its only indicator — every status has icon + text alongside color (Section 18.4)
- [ ] All touch targets ≥44pt; emergency-mode elements ≥56pt; FAB 64×64pt (Section 18.2)
- [ ] No `numberOfLines` on critical labels (location name, CTA text, error copy, distance)
- [ ] Tested at iOS maximum Larger Text size (+5 steps) — no content clipping, no layout breakage

### Error States
- [ ] All error conditions applicable to this screen are handled (reference Section 15 Error-State Copy Matrix)
- [ ] No screen state is a dead end — every error has a recovery action with a tappable affordance
- [ ] Auth-required state (ERR-10) triggers inline slide-up modal, not a full navigation redirect
- [ ] Failed verification copy (ERR-09) is generic — does not reveal detection method or rejection reason

### Emergency Mode
- [ ] Emergency-mode elements use `colors.emergency` / `colors.emergencyOrange` tokens — not `colors.primary`
- [ ] Any screen reachable during emergency mode has ≥56pt primary action height
- [ ] Emergency mode dismiss is explicit (Dismiss button or FAB re-tap) — no auto-dismiss
- [ ] Emergency mode is reachable from this tab in ≤2 taps (verify against Section 16 reachability matrix)

### Loading States
- [ ] Skeleton placeholders (not spinners) used for content area loading (Section 19 skeleton spec)
- [ ] Skeleton uses `colors.skeletonBase` / `colors.skeletonHighlight` tokens
- [ ] Reduced-motion: skeleton renders without animation when `AccessibilityInfo.isReduceMotionEnabled()` is true

### Map Pins (Map-screen components only)
- [ ] Pin colors driven by Mapbox `match` expression on `policy_tag` — not React state or client-side conditional
- [ ] Pending pin visible only when `submissions JOIN` returns submitter match — not a client-side filter
- [ ] Overlay icons (wheelchair, changing table) use separate SymbolLayer with `iconOffset: [8, -8]` starting point

### Bottom Sheet (components using bottom sheet)
- [ ] Snap points configured as exactly 30% / 55% / 90% — no intermediate or computed snap states
- [ ] Drag handle has `accessibilityRole='adjustable'` and `accessibilityValue={{ text: 'Peek'|'Half'|'Full' }}`
- [ ] Emergency mode sheet opens directly to 55% — no Peek state in emergency context

### GPS Consent (sign-up / first-launch components only)
- [ ] `gps_consent = true` and `gps_consent_at = now()` written ONLY after OS dialog resolves to `granted`
- [ ] Pre-prompt button triggers OS dialog only — does not itself write consent

### Security & Server Enforcement
- [ ] Access code field is ABSENT (not masked) for unauthenticated users — no client-side masking as the sole gate (T-1.5-05)
- [ ] `access_sensitivity` and `family_mode` filtering enforced at the RPC layer — client code must not apply these as a client-side filter (T-1.5-04)
- [ ] Public search results exclude deleted, shadowbanned, and suppressed locations at the server RPC — client-side filtering of these properties is forbidden
- [ ] No PII (email, display name) and no precise GPS coordinates written to `console.log`, analytics events, or crash/error reports
- [ ] No client-side trust score, shadowban status, or suppression logic — client receives pre-filtered results only; enforcement is server-side

<verification>
- `cd app && npm run test:coverage` — 100% over src/features/**; all suites green.
- `supabase db push` applied migrations 3+4; Supabase MCP confirms 7 nullable ON DELETE SET NULL columns + update_profile/delete_account.
- Android dev-client: "Continue with Google" → consent → returns via gotta-go://auth/callback → signed in; iOS shows disabled Apple stub, no Google.
- Settings: Privacy/ToS open Termly URLs; Sign Out → Welcome; Location Permissions "Open Settings" opens OS settings.
- Delete Account: type DELETE → button enables → RPC → SIGNED_OUT → Welcome; verify community rows remain with NULL user/submitter id and the auth.users row is gone.
- §20 Component Acceptance Checklist reviewed for Profile, Settings, Sign-In (OAuth rows), Delete + Auth-Required modals before /review-gate.
</verification>

<success_criteria>
- ROADMAP SC-2 (Google OAuth Android deep link; iOS Apple-stub-no-Google), SC-3 (trigger confirmed), SC-6 (Apple route shows coming soon), SC-7 (account deletion → profile anonymization + session revocation), SC-8 (Termly privacy/ToS links), SC-10 (Settings stub incl. location-permission OS deep link), SC-11 (§20 checklist) demonstrable.
- REQ-2-2, REQ-2-3, REQ-2-5 fully covered; REQ-2-6 completed (Settings links + onboarding).
- All new src/features/** modules at 100% coverage; both migrations applied; no raw hex / magic numbers in screen StyleSheets.
</success_criteria>

<output>
Create `.planning/phases/02-auth-profiles/02-02-SUMMARY.md` when done (record db push output, OAuth dashboard config status, and the live trigger-confirmation evidence).
</output>
