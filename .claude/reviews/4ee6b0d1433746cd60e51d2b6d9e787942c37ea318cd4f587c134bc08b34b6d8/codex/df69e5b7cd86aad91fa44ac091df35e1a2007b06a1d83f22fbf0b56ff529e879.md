## Codex Review - Calibration Exit Gate documentation fix

**VERDICT: APPROVE**

scope_hash: sha256:4ee6b0d1433746cd60e51d2b6d9e787942c37ea318cd4f587c134bc08b34b6d8
review_id: rg-2026-07-31-calibration-doc-fix-01
risk_level: low
runtime_required: false
blind_review: true
prior_reviewer_outputs_read: false
evidence_level: 2
runtime_evidence: not_applicable

### Reviewed Queue

- `ANTIGRAVITY.md`

### Skills Applied

- Global `artifact-qa-gate` skill.
- Repository-local Artifact QA Gate shared core (`.claude/skills/artifact_qa_gate.md`).
- Repository-local Codex Overlay for implementation and enforcement accuracy.

### Findings

- None.

### Open Questions

- None affecting merge safety.

### Verification

- Recomputed the staged fingerprint from the Git index entry for `ANTIGRAVITY.md`; it matched the packet exactly: `sha256:4ee6b0d1433746cd60e51d2b6d9e787942c37ea318cd4f587c134bc08b34b6d8`.
- `node --check .claude/hooks/check-review-artifacts.js` exited 0.
- `node --test --test-name-pattern "rejects a passing calibration row whose archived verdict is not an accepted passing verdict|fails closed when the calibration contract omits passingVerdicts" .claude/hooks/check-review-artifacts.test.js` exited 0: 2 passed, 0 failed, 0 skipped.
- `git diff --check HEAD -- ANTIGRAVITY.md` exited 0.
- Inspected the complete staged `ANTIGRAVITY.md` diff: 13 insertions and 5 deletions, confined to `### Calibration Exit Gate`.

### Evidence Receipts

- `.claude/antigravity-calibration-contract.json:13` declares `"passingVerdicts": ["ADVISORY"]`.
- `.claude/hooks/check-review-artifacts.js:208-218` normalizes the contract list and fails closed when it is absent or empty.
- `.claude/hooks/check-review-artifacts.js:295-310` requires exactly one parseable archived verdict and rejects a token not contained in `passingVerdicts`.
- `.claude/hooks/check-review-artifacts.test.js:911-996` exercises both the rejected `VERDICT: BLOCK` case and the missing-`passingVerdicts` case; both focused tests passed in this review.
- `ANTIGRAVITY.md:110-123` accurately documents those enforced behaviors and retains the existing round-4 rationale.

### Adversarial Disproof

- Attempted the documented counterexample through the existing focused test: a receipt claiming `passed: true` while its archive declares `VERDICT: BLOCK`; the hook rejected it.
- Attempted omission of `passingVerdicts` through the existing focused test; the hook failed closed.
- Checked the tightened “exactly one parseable `VERDICT:` line” statement against the actual zero- and multiple-verdict rejection branches; it is accurate.

### Unverified Boundaries

- The full review-gate test suite was not rerun because this scope changes documentation only; the two directly relevant behavioral tests and hook syntax check were run.
- The live `.claude/review-queue.txt` and canonical `*-latest.md` artifacts remain assigned to the separate Probity batch. This verdict covers only the packet-defined `ANTIGRAVITY.md` bytes and does not claim that the canonical commit gate is currently configured for this scope.

### Runtime Boundary Check

- No runtime, hook, application, database, Supabase, transport, permission, or mock boundary changed. The documentation now matches already-present hook behavior; focused execution confirmed the two claims introduced by the paragraph.

### Approved

- The new `passingVerdicts` paragraph is accurate, complete for the behavior it describes, and consistent with the live contract and `validateCalibration()` implementation.
- The tightened “exactly one parseable `VERDICT:` line” wording accurately reflects `artifactVerdicts()` plus the validator's duplicate-rejection branch.
