## Codex Review - Agent Harness Known Limitations, Round 4

**VERDICT: APPROVE**

scope_hash: sha256:09e3550afcaa3400c47315fb5f044dff62822a71413ca6e558fd14d06386f6c9
review_id: rg-2026-07-31-agent-harness-known-limitations-04
risk_level: low
runtime_required: false
blind_review: true
prior_reviewer_outputs_read: false
evidence_level: 2
runtime_evidence: executed

### Reviewed Queue

- `docs/agent-harness.md` (staged `## Known Limitations` addition; Round 4 changes only limitation 1)

### Skills Applied

- Global `artifact-qa-gate` skill and its Gotta Go QA profile.
- Repository-local shared Artifact QA Gate (`.claude/skills/artifact_qa_gate.md`).
- Repository-local Codex Overlay for implementation, test-quality, and enforcement-boundary review.
- Node review-gate implementation reading and documentation-accuracy review required by the packet.

### Findings

- None.

### Open Questions

- None.

### Verification

- `node .claude/hooks/check-review-artifacts.js --print-staged-scope-hash` exited 0 with `sha256:09e3550afcaa3400c47315fb5f044dff62822a71413ca6e558fd14d06386f6c9`, exactly matching the Round 4 packet.
- `.claude/review-queue.txt` contains only `docs/agent-harness.md`; the staged document has no unstaged overlay.
- Read the complete queued document and compared its staged diff with the live hook implementation at `.claude/hooks/check-review-artifacts.js:359-382,547-553,617-630,694,755-757`.
- Focused isolated-Git run for `allows active Antigravity authority only with complete calibration receipts|requires the verdict archive to be present in the git index` exited 0: 2 passed, 0 failed.
- Direct real-repository OID/content check on the saved Round 3 Codex verdict found `exactTextMatch=true`, `archiveIndexOidMatchesWorktree=true`, and `canonicalIndexOidMatchesWorktree=false`.
- `git diff --cached --check` exited 0; output was limited to the environment's global-ignore permission warning.
- The full 55-test hook suite was not repeated because Round 4 changes only documentation; the two boundary-defining fixtures were rerun directly.

### Evidence Receipts

- `docs/agent-harness.md:188-204`: the text now states that the gate/caller reads canonical verdict content, passes it to `hasArchivedCopy()`, and binds the derived archive path rather than the canonical pointer path.
- `.claude/hooks/check-review-artifacts.js:359-382`: `hasArchivedCopy(content, scopeHash, reviewer)` hashes the passed content, derives `archivePath`, requires byte equality, and OID-compares that archive path between the index and working tree.
- `.claude/hooks/check-review-artifacts.js:694,755-757`: the caller reads the canonical verdict with `readFileSafe(req.file)` and passes that content to `hasArchivedCopy()`.
- The prior Round 3 Codex canonical file and `.claude/reviews/b663966c116f9ee20eb5600b807faa23d16548ecff763863d6d4c5d0d824ff72/codex/2746faad56e35db4513ebace5c80417fef6787251231a2083279d882a4071df2.md` were exact-text matches; only the archive path's index OID matched its working-tree OID. This directly confirms the corrected pointer-versus-content distinction.
- `.claude/hooks/check-review-artifacts.test.js:546-566,1022-1040`: focused fixtures retain the calibration-receipt versus canonical-verdict archive distinction and require the canonical-verdict archive in the Git index.

### Adversarial Disproof

- Tried to falsify the corrected exception by using the prior verdict state where the canonical pointer's index blob differed from its working-tree blob. The content-addressed archive still matched the canonical working-tree text exactly and had a matching index OID, so the reviewed verdict content was recorded even though the pointer path differed.
- Searched for a canonical-path read inside `hasArchivedCopy()`; none exists. The canonical read remains at the caller, while the helper reads and checks only the derived archive path, matching the Round 4 wording.
- Rechecked for the Round 3 universal “any listed input is invisible” claim. It is gone; canonical verdict content is explicitly carved out before the remaining unbound inputs are described.
- Recomputed the staged scope after focused execution; it remained identical to the packet hash.

### Unverified Boundaries

- No current Antigravity verdict or current-scope reviewer output was read before saving this independent verdict. Staged archive path names were observed only through Git metadata.
- No application, database, Supabase, device, network, or production runtime is implicated by this documentation-only scope.
- Superpowers skills were not exposed in this Codex harness, so none are claimed as applied.

### Runtime Boundary Check

- The relevant path is caller `readFileSafe(req.file)` -> canonical working-tree content -> `hasArchivedCopy(content, scopeHash, reviewer)` -> derived archive path -> byte comparison -> archive index/working-tree OID comparison. The live source, Git index, prior saved artifact, and focused real-Git fixtures were checked.
- No application mocks or production services are involved. The temporary repositories in focused tests are scaffolding around the real hook and Git commands.

### Approved

- Round 3's contradictory universal conclusion and helper call-path attribution are fully corrected.
- Round 1's queue and calibration-receipt distinctions remain accurate.
- Round 2's canonical-pointer versus archive-copy distinction remains accurate and is now stated without contradiction.
- Limitation 2 and the historical provenance remain accurate.
- The staged Round 4 documentation is ready to merge for this scope.
