## Antigravity Review — docs/agent-harness.md Known Limitations Section, ROUND 4

**VERDICT: ADVISORY**

scope_hash: sha256:09e3550afcaa3400c47315fb5f044dff62822a71413ca6e558fd14d06386f6c9
review_id: rg-2026-07-31-agent-harness-known-limitations-04
risk_level: low
runtime_required: false
blind_review: true
prior_reviewer_outputs_read: false
evidence_level: 1
runtime_evidence: not_applicable

### Reviewed Queue
- docs/agent-harness.md

### Skills Applied
- .claude/skills/artifact_qa_gate.md
- Antigravity Overlay
- superpowers:using-superpowers
- superpowers:verification-before-completion

### Issues
- None.

### Concerns
- None.

### Verification
- Executed `node .claude/hooks/check-review-artifacts.js --print-staged-scope-hash` — matches `sha256:09e3550afcaa3400c47315fb5f044dff62822a71413ca6e558fd14d06386f6c9`.
- Inspected `docs/agent-harness.md` lines 182–204: confirmed revised limitation 1 paragraph accurately framing canonical verdicts as a real exception to the "invisible to the commit" problem (due to `hasArchivedCopy()` capturing a byte-identical index-bound archive copy), while policy, the review queue, calibration contract/evidence, and prompt packets remain working-tree-only with no committed copy check.
- Verified `hasArchivedCopy()` in `.claude/hooks/check-review-artifacts.js` lines 352–376 receives working-tree content from `readFileSafe(req.file)` and OID-compares the derived `archivePath` blob in the Git index.
- Executed `git ls-files --stage .claude/antigravity-review-latest.md`: confirmed pointer path index OID differs from working-tree OID while the derived content-addressed archive `.claude/reviews/**` is staged in the Git index.

### Evidence Receipts
- **Command**: `node .claude/hooks/check-review-artifacts.js --print-staged-scope-hash`
  - Target: Staged queue fingerprint
  - Observed Result: `sha256:09e3550afcaa3400c47315fb5f044dff62822a71413ca6e558fd14d06386f6c9`.
- **Source Inspection**: `.claude/hooks/check-review-artifacts.js`
  - Target: `hasArchivedCopy()` and check-A loop
  - Observed Result: Confirms canonical verdict content read from working tree (`readFileSafe`) and OID-checked at derived `archivePath`.
- **Git OID Check**: `git ls-files --stage .claude/antigravity-review-latest.md`
  - Target: Pointer path index entry
  - Observed Result: Index entry OID is distinct from working-tree blob OID. Proves pointer pathname is not index-bound, while `hasArchivedCopy()` guarantees an index-staged archive copy of the reviewed content.

### Adversarial Disproof
- **Premise 1**: Are canonical verdicts "invisible to the commit" in the same manner as policy or the review queue?
  - **Counterexample attempted**: Traced `hasArchivedCopy()` enforcement.
  - **Outcome**: Disproved. `hasArchivedCopy()` fails the gate unless a byte-identical content-addressed archive copy of the reviewed canonical content is staged in the Git index (`stagedOid === workingTreeBlobOid`), whereas policy, queue, calibration, and prompt packets have no such staged-copy check.
- **Premise 2**: Does `hasArchivedCopy()` check the pointer pathname's index entry?
  - **Counterexample attempted**: Traced `hasArchivedCopy()` code paths.
  - **Outcome**: Disproved. `hasArchivedCopy()` operates on `archivePath` (`.claude/reviews/...`), never on `req.file` (`.claude/antigravity-review-latest.md`).

### Unverified Boundaries
- None. Documentation change only; no runtime execution or database boundary affected.

### Runtime Boundary Check
- Pure documentation change describing exact `hasArchivedCopy()` implementation scope. No code or hook logic modified.

### Claim And State Audit
- Codex round-3 finding (framing canonical verdicts as a real exception to the "invisible to the commit" problem due to `hasArchivedCopy()`) independently verified and confirmed accurately reflected in `docs/agent-harness.md`.

### Approved
- Round 4 `## Known Limitations` section in `docs/agent-harness.md` is accurate, complete, and fully aligned with hook implementation details.
