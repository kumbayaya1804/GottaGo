## Codex Review - Review-gate hardening round 7

**VERDICT: REQUEST CHANGES**

scope_hash: sha256:fac2d32eec7a452b4dae9da4f5d2b78b211ffe7445acc3cfc84c5e789a4dc521
review_id: rg-2026-07-30-antigravity-probation-07
risk_level: high
runtime_required: true
blind_review: true
prior_reviewer_outputs_read: false
evidence_level: 3
runtime_evidence: executed

### Reviewed Queue

- `.claude/commands/antigravity-review.md`
- `.claude/commands/codex-prompt.md`
- `.claude/commands/review-gate.md`
- `.claude/hooks/check-review-artifacts.js`
- `.claude/hooks/check-review-artifacts.test.js`
- `.claude/skills/artifact_qa_gate.md`
- `.claude/skills/review_packet_generator.md`
- `AGENTS.md`
- `ANTIGRAVITY.md`
- `CLAUDE.md`
- `CODEX.md`
- `docs/agent-harness.md`
- `docs/review-severity.md`
- `docs/verification.md`
- `.claude/antigravity-calibration-contract.json`
- `.claude/antigravity-review-policy.json`
- `.claude/hooks/archive-review-artifact.js`
- `.beads/hooks/pre-commit`
- `.beads/context/execution-state.md`
- `.planning/stale-info-scan-latest.md`

### Skills Applied

- `.claude/skills/artifact_qa_gate.md` shared core
- Codex Overlay
- Global `artifact-qa-gate` with its Gotta Go workflow
- Node enforcement, Git-index, HTML-entity, mutation-test, and regression-suite review

### Findings

- [MAJOR] `.claude/hooks/check-review-artifacts.js:115`, `.claude/hooks/check-review-artifacts.js:152`, `.claude/hooks/check-review-artifacts.js:256`, `.claude/hooks/check-review-artifacts.js:268`, `.claude/hooks/check-review-artifacts.js:687` - Decision-bearing policy, calibration contract/evidence, calibration prompts/archives, packets, and canonical verdicts are still read from the working tree with `fs`. The archive check separately proves only that a byte-identical archive enters the index. The mismatch remains observable in this review: the current Codex packet's indexed blob is `418ccbe...` while its reviewed working-tree blob is `8dd93b8...`; the hook evaluates the latter. An unstaged permissive policy or fresh-looking packet/verdict can therefore decide whether a commit passes while the commit records different trust-input bytes. Disclosure does not reduce this approval-integrity defect. Required fix: evaluate every decision-bearing input from the Git index (or fail on any worktree/index mismatch), bind all dependent paths and receipts to indexed blobs, and add isolated regressions for a safe staged policy plus permissive unstaged policy and for stale staged packet/verdict bytes.

- [MAJOR] `.claude/hooks/check-review-artifacts.js:201`, `.claude/hooks/check-review-artifacts.js:204`, `.claude/hooks/check-review-artifacts.js:237`, `.claude/hooks/check-review-artifacts.js:304`, `.claude/hooks/check-review-artifacts.js:329`, `.claude/antigravity-calibration-contract.json:1` - Calibration truth is still supplied by the evidence author. The contract has required case IDs and accepted verdict tokens but no trusted expected outcome per case. `falseApprovals` is accepted from the evidence header; rows not labeled `passed: true` are skipped; required-case completion is then derived only from those same author-labeled passing rows. Receipt, uniqueness, hash, and `ADVISORY` checks prove that reviews occurred, not that the reviewer correctly handled a hidden failing case. Required fix: introduce a trusted/versioned case manifest or equivalent oracle containing expected outcomes, derive pass/fail and false-approval totals from archived verdicts against that oracle, and regression-test a forged all-passing evidence file containing an expected-negative case.

- [MINOR] `.claude/hooks/check-review-artifacts.test.js:1213`, `.claude/hooks/check-review-artifacts.test.js:1220`, `.claude/hooks/check-review-artifacts.test.js:1227`, `.beads/context/execution-state.md:18`, `.planning/stale-info-scan-latest.md:26` - The new test and state records claim to pin the exact 41 `key -> value` pairs and catch a future value typo, but the test observes each value only after full path canonicalization, then compares only the raw key names. A controlled in-memory mutant changing the authoritative `bsol: '\\'` mapping to `bsol: '/'` produced zero loop failures, identical keys, and `wouldCurrentCompletenessAssertionsPass: true`, because line 1220 deliberately collapses either raw value to the same final `/`. The shipped table itself is currently exact, and this mutant is behaviorally equivalent for path detection, so this is a MINOR test-quality/documentation defect rather than a present bypass. Required fix: extract/evaluate the raw `NAMED_CHARACTER_REFERENCES` object and deep-compare it with `EXPECTED` before testing end-to-end canonicalization (or narrow every affected claim to key completeness plus canonicalized behavior).

### Open Questions

- Will the user explicitly document an override accepting the two MAJOR approval-integrity gaps, or should this batch remain held until they are fixed? Under the current contract, a reviewer cannot convert either unresolved finding into approval merely because it is disclosed.

### Verification

- `node --test .claude/hooks/check-review-artifacts.test.js` - passed: 52 tests, 0 failed, 0 skipped; real temporary Git repositories and spawned hook/wrapper.
- `node --check .claude/hooks/check-review-artifacts.js` - passed.
- `node --check .claude/hooks/archive-review-artifact.js` - passed.
- `C:\Program Files\Git\bin\sh.exe -n .beads/hooks/pre-commit` - passed against the actual Git-for-Windows shell.
- `node .claude/hooks/check-review-artifacts.js --print-staged-scope-hash` - reproduced the packet fingerprint exactly.
- `git diff --cached --check -- <20 queued paths>` - passed.
- Queue/index comparison - 20 queue entries, all 20 present in the staged queued diff, none missing.
- Byte scan of all 20 queued files - 0 NUL bytes.
- Fresh authoritative entity-table comparison using `https://html.spec.whatwg.org/entities.json` - `whatwgCount: 41`, `stagedCount: 41`, `missing: []`, `extra: []`.
- Controlled in-memory `bsol` mutant against the new completeness assertions - zero assertion-loop failures, keys match, and the incorrect raw `/` mapping would pass.
- Direct read of `.beads/context/execution-state.md`, `.planning/stale-info-scan-latest.md`, and `.planning/STATE.md` against current Git state - the prior status contradictions remain reconciled; the only new state inaccuracy is the overstated exact-value-test claim reported above.

### Evidence Receipts

- Source: staged queue and `git diff --cached`; target: all 20 manifest paths; observed: all queued files are staged and the staged-scope hash is `sha256:fac2d32eec7a452b4dae9da4f5d2b78b211ffe7445acc3cfc84c5e789a4dc521`; inference: this verdict is bound to the updated packet's current bytes.
- Source: fresh WHATWG HTML entity data, `https://html.spec.whatwg.org/entities.json`; target: semicolon-terminated names whose value is one ASCII non-alphanumeric code point, normalized like the staged decoder; observed: 41 expected pairs, 41 staged pairs, no missing or extra mapping; inference: the shipped round-7 table is correct against the current authority.
- Source: in-memory source mutation plus the staged test's own extraction/comparison logic; target: `bsol`; observed: changing the raw mapping from backslash to slash leaves every current completeness assertion satisfied; inference: the test does not enforce every exact raw value it claims to pin.
- Source: `git ls-files --stage` and `git hash-object`; target: `.claude/codex-prompt-latest.md`; observed: indexed and working-tree blob OIDs differ while `readFileSafe()` consumes the working tree; inference: the trust-boundary mismatch remains present in the normal workflow.
- Source: staged source inspection; target: `validateCalibration()` and `.claude/antigravity-calibration-contract.json`; observed: no independent expected-outcome field or oracle is consumed; inference: receipt authenticity does not establish calibration correctness.

### Adversarial Disproof

- Repeated the full live-spec comparison rather than trusting the packet or the pinned oracle: the round-6 `underbar` omission is closed and the current table is an exact 41/41 match.
- Exercised the real extracted `canonicalizePathText()` through the new `UnderBar` tests: both case variants and `%5f` equivalence pass.
- Mutated `bsol` to the wrong raw value without changing keys: the new test's special-case normalization concealed the mutation, disproving its exact-value guarantee while retaining end-to-end path behavior.
- Re-ran the real-hook `&percnt;2e`/`&percnt;2f` regression within the complete suite: both variants remain blocked.
- Tried to disprove the worktree/index concern using current blobs: the current Codex packet again has unequal index/worktree OIDs while the gate reads the working tree. The premise is retained.
- Traced calibration from contract through receipts to verdict parsing: archived `ADVISORY` tokens are verified, but expected case outcomes and false approvals are never independently derived. The premise is retained.

### Unverified Boundaries

- No live Antigravity calibration run exists; calibration behavior is covered only by synthetic fixtures.
- No commit was attempted because this verdict is `REQUEST CHANGES`; the wrapper was exercised by its real-process test suite and syntax-checked directly.
- Filesystem-level archive immutability outside Git history remains unenforced, as the queued stale-info scan documents.
- The fresh entity comparison establishes the WHATWG set at review time; the pinned local oracle cannot detect future upstream additions without a fresh authoritative diff.

### Runtime Boundary Check

- Commit path inspected: `git commit` -> configured `.beads/hooks/pre-commit` -> `node` -> `.claude/hooks/check-review-artifacts.js` -> real filesystem and Git subprocesses -> exit code. The suite exercises the real hook in isolated repositories and includes the real wrapper's missing-Node failure path; no mocks replace Git or the filesystem.
- The new extraction helper evaluates the shipped constant and canonicalization functions from the real hook source. Its start/end markers currently resolve correctly and would fail loudly if the expected declarations disappeared; the material gap is raw-value observability after later normalization, not stale-source extraction.
- The archive helper writes a content-addressed file and stages it with real `git add`. The hook checks the archive's working-tree content and clean-filtered blob OID, but other decision inputs remain on the working-tree side of the boundary.
- No database, Supabase, auth, or application runtime lies on this commit-hook path. The only network operation in this review was the independent authoritative entity-table fetch.

### Approved

- The round-6 `underbar` omission is fixed: the current staged table matches all 41 authoritative pairs, and `&UnderBar;` canonicalizes to `_`.
- The extraction boundary currently runs the real shipped table/functions, and the two new tests execute successfully.
- The earlier `&percnt;` composition bypass and other separator/dot/percent/entity canonicalization regressions remain closed.
- The state documents retain the round-6 status reconciliation and avoid a self-referential current scope hash.
- All 52 focused runtime tests pass with zero skips; syntax, shell, staged-scope, queued-diff, and NUL-byte checks are clean.
