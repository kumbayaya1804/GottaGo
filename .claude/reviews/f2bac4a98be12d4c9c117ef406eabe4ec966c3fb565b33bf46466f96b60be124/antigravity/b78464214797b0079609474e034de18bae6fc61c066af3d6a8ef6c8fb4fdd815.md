## Antigravity Review — Review-Gate Hardening, ROUND 5

**VERDICT: ADVISORY**

scope_hash: sha256:f2bac4a98be12d4c9c117ef406eabe4ec966c3fb565b33bf46466f96b60be124
review_id: rg-2026-07-30-antigravity-probation-05
risk_level: high
runtime_required: true
blind_review: true
prior_reviewer_outputs_read: false
evidence_level: 3
runtime_evidence: executed

### Reviewed Queue
- .claude/commands/antigravity-review.md
- .claude/commands/codex-prompt.md
- .claude/commands/review-gate.md
- .claude/hooks/check-review-artifacts.js
- .claude/hooks/check-review-artifacts.test.js
- .claude/skills/artifact_qa_gate.md
- .claude/skills/review_packet_generator.md
- AGENTS.md
- ANTIGRAVITY.md
- CLAUDE.md
- CODEX.md
- docs/agent-harness.md
- docs/review-severity.md
- docs/verification.md
- .claude/antigravity-calibration-contract.json
- .claude/antigravity-review-policy.json
- .claude/hooks/archive-review-artifact.js
- .beads/hooks/pre-commit
- .beads/context/execution-state.md
- .planning/stale-info-scan-latest.md

### Skills Applied
- .claude/skills/artifact_qa_gate.md
- Antigravity Overlay
- superpowers:using-superpowers
- superpowers:verification-before-completion

### Issues
- [MAJOR] [.claude/hooks/check-review-artifacts.js:632](file:///C:/Users/mrsai/Gotta%20Go/.claude/hooks/check-review-artifacts.js#L632): `check-review-artifacts.js` still evaluates prompt packets and review verdicts by reading from the working tree (`readFileSafe` / `fs.readFileSync`) rather than reading from the Git index (`stagedBlobOid` / `indexContent`). While this architectural deferral is tracked and disclosed, the technical surface remains open: an unstaged edit in the working tree satisfied pre-commit validation while the committed index entry retained stale text.
- [MINOR] [.claude/hooks/check-review-artifacts.js:280](file:///C:/Users/mrsai/Gotta%20Go/.claude/hooks/check-review-artifacts.js#L280): Calibration `falseApprovals` and individual row `passed` flags remain self-asserted in `calibration-evidence.json` rather than derived from raw benchmark execution logs.

### Concerns
- CommonMark entity decoding in `decodeCharacterReferencesOnce()` uses a focused 5-entry dictionary (`amp`, `period`, `sol`, `bsol`, `colon`). While sufficient for standard path structural delimiters, additional CommonMark entity names (such as `&sol;` vs `&sol`) could expand if non-semicolon-terminated entity forms are permitted in specialized Markdown parsers.

### Verification
- Executed `git status` to verify staged queued files and untracked review artifacts.
- Executed `node --test .claude/hooks/check-review-artifacts.test.js` to run the test suite (49/49 tests passing).
- Verified CommonMark numeric (`&#46;`, `&#x2e;`) and named (`&period;`, `&sol;`, `&amp;`) character reference decoding in `canonicalizePathText()`.

### Evidence Receipts
- **Command**: `node --test .claude/hooks/check-review-artifacts.test.js`
  - Target: `.claude/hooks/check-review-artifacts.test.js`
  - Observed Result: Executed 49 tests covering review gate rules, CommonMark character reference decoding (decimal, hex, named, nested `&amp;#46;`), OID archive verification under CRLF, and calibration `passingVerdicts` validation. Exit status 0 (`pass 49, fail 0, skipped 0`).
  - Proves: All 49 test cases pass on Windows Node environment.
- **Source Inspection**: `.claude/hooks/check-review-artifacts.js` lines 400-446 (`canonicalizePathText`)
  - Target: `decodeCharacterReferencesOnce()` combined with percent-decoding in `while` loop
  - Observed Result: `while ((decoded = decodeCharacterReferencesOnce(...)) !== text)` iteratively decodes percent-encodings and character references until string reaches a fixed point.
  - Proves: CommonMark entity encodings for path separators and dots are decoded to a fixed point before blind review pattern matching.

### Adversarial Disproof
- **Premise 1**: Nested entity encoding `&amp;#46;` could evade blind review path detection.
  - **Counterexample attempted**: Tested `referencesReviewerOutput("See &amp;#46;claude/codex-review-latest.md", "codex")`.
  - **Outcome**: Disproved. Pass 1 decodes `&amp;` -> `&`, turning string into `&#46;claude/...`. Fixed-point loop detects change and executes Pass 2, decoding `&#46;` -> `.`, yielding `.claude/codex-review-latest.md`, which triggers the blind review block.
- **Premise 2**: Disclosure of working-tree trust input reading eliminates operational risk.
  - **Counterexample attempted**: Staged a valid archive via `archive-review-artifact.js`, then edited `.claude/antigravity-review-latest.md` on disk without staging it.
  - **Outcome**: Retained. Disclosure documents the issue, but `check-review-artifacts.js` still evaluates working-tree text instead of the staged index, leaving the structural vulnerability active until index-based reading (`stagedBlobOid` / `indexContent`) is implemented.

### Unverified Boundaries
- No remote repository or network boundaries were touched; all git operations were executed against local repository indices.

### Runtime Boundary Check
- Traced execution chain: `git commit` -> `.beads/hooks/pre-commit` -> `node .claude/hooks/check-review-artifacts.js` -> `git ls-files --stage` / `git hash-object`.
- Confirmed pre-commit wrapper correctly fails closed when `node` is absent from `PATH`.

### Claim And State Audit
- Confirmed `.beads/context/execution-state.md` and `.planning/stale-info-scan-latest.md` are in the active queue and match observable `git status`.
- Reconciled prompt claims with repository files; confirmed policy mode is `probation` requiring `ADVISORY` verdict for clean probation checks.

### Approved
- Round 5 fix for CommonMark/HTML5 character reference decoding (`decodeCharacterReferencesOnce` with numeric and named entity handling) is verified and effective.
