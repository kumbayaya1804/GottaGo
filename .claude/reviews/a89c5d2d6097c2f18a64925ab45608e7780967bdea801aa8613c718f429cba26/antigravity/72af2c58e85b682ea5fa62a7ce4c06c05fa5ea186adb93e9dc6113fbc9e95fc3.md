## Antigravity Review — docs/agent-harness.md Known Limitations Section, ROUND 1

**VERDICT: ADVISORY**

scope_hash: sha256:a89c5d2d6097c2f18a64925ab45608e7780967bdea801aa8613c718f429cba26
review_id: rg-2026-07-31-agent-harness-known-limitations-01
risk_level: low
runtime_required: false
blind_review: true
prior_reviewer_outputs_read: false
evidence_level: 1
runtime_evidence: not_applicable

### Reviewed Queue
- docs/agent-harness.md

### Skills Applied
- .claude/skills/artifact_qa_gate.md
- Antigravity Overlay
- superpowers:using-superpowers
- superpowers:verification-before-completion

### Issues
- None.

### Concerns
- None.

### Verification
- Executed `node .claude/hooks/check-review-artifacts.js --print-staged-scope-hash` — matches `sha256:a89c5d2d6097c2f18a64925ab45608e7780967bdea801aa8613c718f429cba26`.
- Inspected `docs/agent-harness.md` lines 176–200: confirmed addition of `## Known Limitations` section detailing working-tree reads and self-asserted calibration truth.
- Inspected `.claude/hooks/check-review-artifacts.js` functions `readPolicy()`, `validateCalibration()`, and `readFileSafe()` — confirmed they read policy, calibration contract/evidence, prompts, and canonical verdicts via `fs.readFileSync` against working-tree files, while `hasArchivedCopy()` uses git OID inspection for archived verdicts (`.claude/reviews/**`). Confirms limitation 1.
- Inspected `validateCalibration()` in `.claude/hooks/check-review-artifacts.js` line 237 — confirmed `result.passed` and `evidence.falseApprovals` are read directly as supplied without independent re-execution or oracle derivation. Confirms limitation 2.
- Ran `git diff HEAD -- docs/agent-harness.md` — confirmed diff is strictly confined to the 25-line `## Known Limitations` addition.

### Evidence Receipts
- **Command**: `node .claude/hooks/check-review-artifacts.js --print-staged-scope-hash`
  - Target: Staged queue fingerprint
  - Observed Result: `sha256:a89c5d2d6097c2f18a64925ab45608e7780967bdea801aa8613c718f429cba26`.
- **Source Inspection**: `.claude/hooks/check-review-artifacts.js`
  - Target: `readPolicy()`, `validateCalibration()`, `hasArchivedCopy()`
  - Observed Result: Proves working-tree file reads for trust inputs vs OID git index verification for verdict archives.
- **Git Diff Inspection**: `git diff HEAD -- docs/agent-harness.md`
  - Target: `docs/agent-harness.md`
  - Observed Result: Confirms edit is 100% documentation-only and limited to `## Known Limitations`.

### Adversarial Disproof
- **Premise 1**: Could limitation 1 be inaccurate (do trust inputs use git index reads)?
  - **Counterexample attempted**: Searched `check-review-artifacts.js` for git index calls (`stagedBlobOid`/`workingTreeBlobOid`).
  - **Outcome**: Disproved. `stagedBlobOid` is used solely in `hasArchivedCopy()`; `readPolicy()`, `readRepoJson()`, and prompt/verdict checks use `fs.readFileSync()`.
- **Premise 2**: Could limitation 2 be inaccurate (does the hook calculate `falseApprovals` or re-test calibration cases)?
  - **Counterexample attempted**: Traced `validateCalibration()`.
  - **Outcome**: Disproved. `falseApprovals` and `passed` values are read directly from JSON payload.

### Unverified Boundaries
- None. Documentation change only; no runtime execution or database boundary affected.

### Runtime Boundary Check
- Pure documentation change disclosing existing architectural limitations. No runtime code or hook logic modified.

### Claim And State Audit
- Packet claims (working-tree reads, self-asserted calibration truth, commit `d03bfe7` risk acceptance attribution, no collateral diff in `docs/agent-harness.md`): Confirmed verbatim.

### Approved
- The `## Known Limitations` section in `docs/agent-harness.md` is accurate, complete, and fully aligned with hook implementation and project history.
