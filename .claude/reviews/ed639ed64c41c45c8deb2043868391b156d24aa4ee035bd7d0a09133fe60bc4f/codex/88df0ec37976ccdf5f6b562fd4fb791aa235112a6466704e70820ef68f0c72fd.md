## Codex Review - database.types.ts regeneration

**VERDICT: APPROVE**

scope_hash: sha256:ed639ed64c41c45c8deb2043868391b156d24aa4ee035bd7d0a09133fe60bc4f
review_id: rg-2026-07-30-database-types-regen-02
risk_level: medium
runtime_required: false
blind_review: true
prior_reviewer_outputs_read: false
evidence_level: 4
runtime_evidence: executed

### Reviewed Queue

- `app/src/lib/database.types.ts` - inspected the complete 1,018-line generated file, its staged 65-insertion/3-deletion diff, all generated helpers, and its nearest typed Supabase consumers.

### Skills Applied

- `.claude/skills/artifact_qa_gate.md` shared core.
- Codex Overlay.
- Global `artifact-qa-gate`.
- Supabase skill for current live-project type regeneration and official generator-contract verification.

### Findings

- None.

### Open Questions

- None affecting merge safety.

### Verification

- `node .claude/hooks/check-review-artifacts.js --print-staged-scope-hash` - reproduced `sha256:ed639ed64c41c45c8deb2043868391b156d24aa4ee035bd7d0a09133fe60bc4f` before and after verification.
- `git diff --cached --check -- app/src/lib/database.types.ts` - clean; the queued file is staged and `git diff -- app/src/lib/database.types.ts` is empty. The packet narrative saying it is unstaged is stale, but its manifest, queue, and fingerprint bind the actual staged bytes correctly.
- Independent Supabase MCP type generation against live project `ebmzhjmmtmldhrojkdqw` (`ACTIVE_HEALTHY`, PostgreSQL 17.6.1.104) - captured 30,671 bytes and compared them with the queued file: byte-identical. Both SHA-256 values are `218582f863d6d8a3b18e495000439de9e5313b20b0844e82758ec04190790837`.
- `npm.cmd run typecheck` in `app` - passed (`tsc --noEmit`).
- `npx.cmd eslint src/lib/database.types.ts` in `app` - passed with no output.
- `npm.cmd test -- --runInBand src/features/submit/__tests__/submitLocation.test.ts --forceExit` in `app` - 1/1 suite and 11/11 tests passed.
- `npm.cmd test -- --runInBand src/features/locations/__tests__/useCurrentPosition.test.ts --forceExit` in `app` - 1/1 suite and 9/9 tests passed.
- `npm.cmd test -- --forceExit` in `app` - 46/46 suites and 389/389 tests passed. Jest emitted the pre-existing MapScreen `act(...)` warning and its open-handle/force-exit advisory, neither tied to this declaration-only change.
- An initial serial full-suite attempt timed out after an anomalously long `useCurrentPosition` test. That same suite passed in 2.261 seconds when isolated, and the subsequent complete parallel run passed in 19.046 seconds; the timeout was not retained as a reproducible product or type-generation failure.

### Evidence Receipts

- Live Supabase project `ebmzhjmmtmldhrojkdqw`, `_generate_typescript_types`, observed 2026-07-30 - the returned TypeScript payload is byte-for-byte identical to `app/src/lib/database.types.ts`; inference: the staged file is a current, unmodified representation of the live public schema rather than a hand-transcription approximation.
- `app/src/lib/database.types.ts:369`, `:597`, and `:846` - generated `submission_tags`, evolved `verification_events`, and `search_pending_submissions_nearby` declarations match `supabase/migrations/20260717120000_phase5_event_model.sql:29`, `:57`, `:95` and `supabase/migrations/20260717120100_phase5_discovery_rpc.sql:20`.
- `app/src/lib/database.types.ts:901`, `:930`, `:955`, `:980`, and `:997` - `Tables`, `TablesInsert`, `TablesUpdate`, `Enums`, and `CompositeTypes` use the standard generated conditional-generic structure. In particular, `CompositeTypes` selects `PublicCompositeTypeNameOrOptions` in its default-schema branch at line 1011, not `CompositeTypeName`.
- `app/src/lib/database.types.ts:12` - the generated internal metadata reports PostgREST `14.5`, consistent with the current live generator payload.
- Supabase's official [Generating TypeScript Types](https://supabase.com/docs/guides/api/rest/generating-types) guide documents generation from the project schema; inference: an independently authenticated live generation is the authoritative comparison for this artifact.

### Adversarial Disproof

- Attempted to disprove freshness by regenerating from the live project instead of trusting the packet or prior review. No byte, length, or hash difference was found.
- Attempted to disprove scope binding by recomputing the staged fingerprint after all checks and checking for an unstaged queued-file delta. The fingerprint remained exact and no queued-file worktree delta exists.
- Attempted to disprove migration fidelity by checking each changed declaration against the producing SQL: nullable `location_id`, nullable optional `submission_id`, the three audit columns, both new foreign-key relationships, the submission-tag shape, and the discovery RPC's optional `result_limit` plus six-column return row all match.
- Attempted to disprove helper integrity by tracing every schema/default-schema conditional branch in all five exported helpers and comparing the entire file with a fresh live generator result. The previously reported `CompositeTypes` wrong-parameter mutation is absent.
- Attempted to disprove consumer compatibility through the real compiler and the nearest typed RPC tests. TypeScript accepted `createClient<Database>` and the `submit_location` argument projection, and all focused and full-suite tests passed.

### Unverified Boundaries

- No device UI or production app session was exercised; this file contains compile-time declarations only and `runtime_required` is false.
- Database writes, RLS behavior, and RPC execution semantics were not re-tested in this review because the queued artifact does not alter them. The live boundary was exercised read-only through authenticated schema type generation.
- `superpowers:using-superpowers` and `superpowers:verification-before-completion` were named by the packet but are not installed in this Codex environment; they were not claimed as applied. The repository and global artifact QA contracts supplied the verification-before-completion discipline used here.

### Runtime Boundary Check

- The queued file has no runtime side effects. Its production boundary is consumption by `createClient<Database>` in `app/src/lib/supabase.ts:10`; the only direct function-argument projection is `submit_location` in `app/src/features/submit/submitLocation.ts:5`.
- Jest mocks the Supabase singleton for `submitLocation`, so those tests establish client mapping and error propagation, not live RPC behavior. This limitation is immaterial to the schema-declaration question because the independent live generator comparison directly validates the declaration source.
- The TypeScript compiler, ESLint, staged-scope hook, Git diff checks, live Supabase schema read, and byte comparison all ran against actual artifacts rather than packet assertions or mocks.

### Approved

- The staged generated declaration is current with the live Supabase schema and ready to merge for this exact scope.
- The Phase 5 table, event-model, relationship, and discovery-RPC additions are represented accurately.
- All generic helper types, including the corrected `CompositeTypes` default-schema branch, match the authoritative live generator output.
