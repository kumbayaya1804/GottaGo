## Antigravity Review — docs/agent-harness.md Known Limitations Section, ROUND 2

**VERDICT: ADVISORY**

scope_hash: sha256:ac9593d622280759b10e3b157ce71fca5b762c3b4fd301050fe341defe5fb68d
review_id: rg-2026-07-31-agent-harness-known-limitations-02
risk_level: low
runtime_required: false
blind_review: true
prior_reviewer_outputs_read: false
evidence_level: 1
runtime_evidence: not_applicable

### Reviewed Queue
- docs/agent-harness.md

### Skills Applied
- .claude/skills/artifact_qa_gate.md
- Antigravity Overlay
- superpowers:using-superpowers
- superpowers:verification-before-completion

### Issues
- None.

### Concerns
- None.

### Verification
- Executed `node .claude/hooks/check-review-artifacts.js --print-staged-scope-hash` — matches `sha256:ac9593d622280759b10e3b157ce71fca5b762c3b4fd301050fe341defe5fb68d`.
- Inspected `docs/agent-harness.md` lines 182–195: confirmed revised limitation 1 paragraph explicitly names gitignored `.claude/review-queue.txt` and its role driving `stagedScopeHash()`, and narrows archive binding to current canonical verdicts (`.claude/antigravity-review-latest.md` / `.claude/codex-review-latest.md`) via `hasArchivedCopy()`.
- Verified `validateCalibration()` in `.claude/hooks/check-review-artifacts.js` lines 268–293 uses `fs.existsSync(archiveResolved)` and content hashing without calling `stagedBlobOid()` or `workingTreeBlobOid()`, confirming calibration archives are content-addressed but not index-bound.
- Verified `readLines(queueFile)` on working-tree file `.claude/review-queue.txt` feeds `stagedScopeHash()`.

### Evidence Receipts
- **Command**: `node .claude/hooks/check-review-artifacts.js --print-staged-scope-hash`
  - Target: Staged queue fingerprint
  - Observed Result: `sha256:ac9593d622280759b10e3b157ce71fca5b762c3b4fd301050fe341defe5fb68d`.
- **Source Inspection**: `.claude/hooks/check-review-artifacts.js`
  - Target: `stagedScopeHash()`, `validateCalibration()`, `hasArchivedCopy()`
  - Observed Result: Confirms queue reads working-tree file `.claude/review-queue.txt`; confirms calibration archives check filesystem existence without index OID checks.

### Adversarial Disproof
- **Premise 1**: Could calibration receipt archives be OID-bound to the Git index?
  - **Counterexample attempted**: Searched `validateCalibration()` for `stagedBlobOid` or `workingTreeBlobOid`.
  - **Outcome**: Disproved. `validateCalibration()` only checks filesystem existence via `fs.existsSync(archiveResolved)`.
- **Premise 2**: Could `.claude/review-queue.txt` be tracked in Git?
  - **Counterexample attempted**: Checked `.gitignore` and `git ls-files .claude/review-queue.txt`.
  - **Outcome**: Disproved. `.claude/review-queue.txt` is gitignored and untracked.

### Unverified Boundaries
- None. Documentation change only; no runtime execution or database boundary affected.

### Runtime Boundary Check
- Pure documentation change describing existing hook implementation realities. No code or hook logic modified.

### Claim And State Audit
- Both Codex round-1 findings (untracked review queue disclosure and accurate scoping of Git index archive binding) independently verified and confirmed accurately reflected in `docs/agent-harness.md`.

### Approved
- Round 2 `## Known Limitations` section in `docs/agent-harness.md` is accurate, complete, and fully aligned with hook implementation details.
