## Antigravity Review — docs/agent-harness.md Known Limitations Section, ROUND 3

**VERDICT: ADVISORY**

scope_hash: sha256:b663966c116f9ee20eb5600b807faa23d16548ecff763863d6d4c5d0d824ff72
review_id: rg-2026-07-31-agent-harness-known-limitations-03
risk_level: low
runtime_required: false
blind_review: true
prior_reviewer_outputs_read: false
evidence_level: 1
runtime_evidence: not_applicable

### Reviewed Queue
- docs/agent-harness.md

### Skills Applied
- .claude/skills/artifact_qa_gate.md
- Antigravity Overlay
- superpowers:using-superpowers
- superpowers:verification-before-completion

### Issues
- None.

### Concerns
- None.

### Verification
- Executed `node .claude/hooks/check-review-artifacts.js --print-staged-scope-hash` — matches `sha256:b663966c116f9ee20eb5600b807faa23d16548ecff763863d6d4c5d0d824ff72`.
- Inspected `hasArchivedCopy()` in `.claude/hooks/check-review-artifacts.js` lines 352–376: confirmed it takes canonical verdict content read from the working tree (`readFileSafe`), computes `contentHash`, and calls `stagedBlobOid`/`workingTreeBlobOid` ONLY on the derived `.claude/reviews/<scopeHash>/<reviewer>/<contentHash>.md` archive path — never on the canonical pointer path itself.
- Executed `git ls-files --stage .claude/antigravity-review-latest.md; git hash-object .claude/antigravity-review-latest.md`: index OID (`8393cf5...`) differs from working-tree OID (`3ddaf48...`), empirically proving the canonical pointer pathname is not index-bound.
- Inspected `docs/agent-harness.md` lines 182–200: confirmed revised paragraph precisely distinguishes the pointer path working-tree read from the derived archive OID check.

### Evidence Receipts
- **Command**: `node .claude/hooks/check-review-artifacts.js --print-staged-scope-hash`
  - Target: Staged queue fingerprint
  - Observed Result: `sha256:b663966c116f9ee20eb5600b807faa23d16548ecff763863d6d4c5d0d824ff72`.
- **Command**: `git ls-files --stage .claude/antigravity-review-latest.md; git hash-object .claude/antigravity-review-latest.md`
  - Target: Pointer path OID comparison
  - Observed Result: Index OID `8393cf5893648839abb61990ff3496e70b88e328` != Working-tree OID `3ddaf483e7fff5735d55851584a5cfb01b21e5f1`. Proves pointer pathname is not index-bound.
- **Source Inspection**: `.claude/hooks/check-review-artifacts.js`
  - Target: `hasArchivedCopy()` definition
  - Observed Result: Operates on derived `archivePath` blob OID, not input `req.file` pointer pathname.

### Adversarial Disproof
- **Premise 1**: Could `hasArchivedCopy()` check `req.file` pointer path OIDs?
  - **Counterexample attempted**: Traced `hasArchivedCopy()` arguments and variables.
  - **Outcome**: Disproved. `hasArchivedCopy()` receives `(content, scopeHash, reviewer)` and constructs `archivePath`; `req.file` is never passed to `stagedBlobOid`.
- **Premise 2**: Could canonical pointer files be index-bound?
  - **Counterexample attempted**: Ran git OID comparison on live working tree.
  - **Outcome**: Disproved. Working tree pointer content diverges from staged index blob.

### Unverified Boundaries
- None. Documentation change only; no runtime execution or database boundary affected.

### Runtime Boundary Check
- Pure documentation change describing exact `hasArchivedCopy()` implementation scope. No code or hook logic modified.

### Claim And State Audit
- Codex round-2 finding (discriminating canonical pointer path working-tree reads from derived archive OID binding) independently verified and confirmed accurately reflected in `docs/agent-harness.md`.

### Approved
- Round 3 `## Known Limitations` section in `docs/agent-harness.md` is accurate, complete, and fully aligned with hook implementation details.
