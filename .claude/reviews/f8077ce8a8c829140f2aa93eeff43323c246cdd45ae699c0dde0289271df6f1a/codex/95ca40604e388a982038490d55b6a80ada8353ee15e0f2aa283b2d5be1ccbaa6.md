## Codex Review - Review-gate hardening round 8

**VERDICT: REQUEST CHANGES**

scope_hash: sha256:f8077ce8a8c829140f2aa93eeff43323c246cdd45ae699c0dde0289271df6f1a
review_id: rg-2026-07-30-antigravity-probation-08
risk_level: high
runtime_required: true
blind_review: true
prior_reviewer_outputs_read: false
evidence_level: 3
runtime_evidence: executed

### Reviewed Queue

- `.claude/commands/antigravity-review.md`
- `.claude/commands/codex-prompt.md`
- `.claude/commands/review-gate.md`
- `.claude/hooks/check-review-artifacts.js`
- `.claude/hooks/check-review-artifacts.test.js`
- `.claude/skills/artifact_qa_gate.md`
- `.claude/skills/review_packet_generator.md`
- `AGENTS.md`
- `ANTIGRAVITY.md`
- `CLAUDE.md`
- `CODEX.md`
- `docs/agent-harness.md`
- `docs/review-severity.md`
- `docs/verification.md`
- `.claude/antigravity-calibration-contract.json`
- `.claude/antigravity-review-policy.json`
- `.claude/hooks/archive-review-artifact.js`
- `.beads/hooks/pre-commit`
- `.beads/context/execution-state.md`
- `.planning/stale-info-scan-latest.md`

### Skills Applied

- `.claude/skills/artifact_qa_gate.md` shared core
- Codex Overlay
- Global `artifact-qa-gate` with its Gotta Go workflow
- Node enforcement, Git-index, mutation-test, extraction-boundary, and regression-suite review

### Findings

- [MAJOR] `.claude/hooks/check-review-artifacts.js:115`, `.claude/hooks/check-review-artifacts.js:152`, `.claude/hooks/check-review-artifacts.js:256`, `.claude/hooks/check-review-artifacts.js:268`, `.claude/hooks/check-review-artifacts.js:687` - Decision-bearing policy, calibration contract/evidence, calibration prompts/archives, packets, and canonical verdicts are still read from the working tree with `fs`. The archive check separately proves only that a byte-identical archive enters the index. The mismatch remains observable in this review: the current Codex packet's indexed blob is `418ccbe...` while its reviewed working-tree blob is `74a0e12...`; the hook evaluates the latter. An unstaged permissive policy or fresh-looking packet/verdict can therefore decide whether a commit passes while the commit records different trust-input bytes. Disclosure does not reduce this approval-integrity defect. Required fix: evaluate every decision-bearing input from the Git index (or fail on any worktree/index mismatch), bind all dependent paths and receipts to indexed blobs, and add isolated regressions for a safe staged policy plus permissive unstaged policy and for stale staged packet/verdict bytes.

- [MAJOR] `.claude/hooks/check-review-artifacts.js:201`, `.claude/hooks/check-review-artifacts.js:204`, `.claude/hooks/check-review-artifacts.js:237`, `.claude/hooks/check-review-artifacts.js:304`, `.claude/hooks/check-review-artifacts.js:329`, `.claude/antigravity-calibration-contract.json:1` - Calibration truth is still supplied by the evidence author. The contract has required case IDs and accepted verdict tokens but no trusted expected outcome per case. `falseApprovals` is accepted from the evidence header; rows not labeled `passed: true` are skipped; required-case completion is then derived only from those same author-labeled passing rows. Receipt, uniqueness, hash, and `ADVISORY` checks prove that reviews occurred, not that the reviewer correctly handled a hidden failing case. Required fix: introduce a trusted/versioned case manifest or equivalent oracle containing expected outcomes, derive pass/fail and false-approval totals from archived verdicts against that oracle, and regression-test a forged all-passing evidence file containing an expected-negative case.

### Open Questions

- Will the user explicitly document an override accepting the two MAJOR approval-integrity gaps, or should this batch remain held until they are fixed? Round 8 is clean beyond these two unchanged architectural findings, but under the current contract a reviewer cannot convert either into approval merely because it is disclosed.

### Verification

- `node --test .claude/hooks/check-review-artifacts.test.js` - passed: 53 tests, 0 failed, 0 skipped; real temporary Git repositories and spawned hook/wrapper.
- `node --check .claude/hooks/check-review-artifacts.test.js` - passed.
- `node --check .claude/hooks/check-review-artifacts.js` - passed.
- `node --check .claude/hooks/archive-review-artifact.js` - passed.
- `C:\Program Files\Git\bin\sh.exe -n .beads/hooks/pre-commit` - passed against the actual Git-for-Windows shell.
- `node .claude/hooks/check-review-artifacts.js --print-staged-scope-hash` - reproduced the packet fingerprint exactly.
- `git diff --cached --check -- <20 queued paths>` - passed.
- Queue/index comparison - 20 queue entries, all 20 present in the staged queued diff, none missing.
- Byte scan of all 20 queued files - 0 NUL bytes.
- Current hook hash comparison - `.claude/hooks/check-review-artifacts.js` remains `6063d85f...`, byte-identical to the reviewed round-7 production hook; round 8 changes only the test and state evidence.
- Authoritative entity-table comparison using the freshly fetched WHATWG data from the immediately preceding round - `whatwgCount: 41`, `stagedCount: 41`, `missing: []`, `extra: []`.
- Controlled mutations using the test file's actual `node:assert/strict` import: the prior `bsol: '\\'` -> `'/'` mutation, a `period`/`sol` value swap, and a wrong-type `tab: 0` mutation all fail the new raw-object comparison.
- Direct read of `.beads/context/execution-state.md`, `.planning/stale-info-scan-latest.md`, and `.planning/STATE.md` against current Git state - the descriptions now accurately separate raw-object completeness from end-to-end canonicalization.

### Evidence Receipts

- Source: staged queue and `git diff --cached`; target: all 20 manifest paths; observed: all queued files are staged and the staged-scope hash is `sha256:f8077ce8a8c829140f2aa93eeff43323c246cdd45ae699c0dde0289271df6f1a`; inference: this verdict is bound to the updated packet's current bytes.
- Source: `.claude/hooks/check-review-artifacts.test.js:1208-1239`; target: raw named-reference extraction; observed: the helper evaluates the real shipped object literal and the `node:assert/strict` deep comparison rejects the prior `bsol` mutant and additional wrong-key/value/type mutants; inference: the round-7 test-quality finding is fixed.
- Source: `.claude/hooks/check-review-artifacts.test.js:1241-1256`; target: full canonicalization pipeline; observed: all 41 expected raw mappings are separately exercised through the shipped canonicalizer; inference: raw completeness and end-to-end behavior are now distinct, correctly scoped properties.
- Source: `git ls-files --stage` and `git hash-object`; target: `.claude/codex-prompt-latest.md`; observed: indexed and working-tree blob OIDs differ while `readFileSafe()` consumes the working tree; inference: the trust-boundary mismatch remains present in the normal workflow.
- Source: staged source inspection; target: `validateCalibration()` and `.claude/antigravity-calibration-contract.json`; observed: no independent expected-outcome field or oracle is consumed; inference: receipt authenticity does not establish calibration correctness.

### Adversarial Disproof

- Re-ran the exact round-7 `bsol` mutant against the new raw-object assertion: it fails, closing the prior observation gap.
- Swapped the distinct `period` and `sol` values: the raw-object assertion fails, so meaningful key/value swaps are distinguished.
- Changed `tab` from the authoritative string `'\t'` to numeric `0`: using the suite's actual `node:assert/strict` import, the assertion fails. An initial standalone check using legacy `node:assert` passed through loose equality, but that was not faithful to the test's import and was discarded after reproducing the real boundary.
- Checked extraction markers: the current regex captures the single live constant declaration, and deletion/renaming makes the explicit `assert.ok` fail rather than silently testing a stale copy.
- Re-ran the complete hook suite: the `UnderBar`, `percnt`, numeric/named entity, separator, dot-segment, archive, calibration-receipt, and missing-Node regressions remain green.
- Tried to disprove the worktree/index concern using current blobs: the current Codex packet again has unequal index/worktree OIDs while the gate reads the working tree. The premise is retained.
- Traced calibration from contract through receipts to verdict parsing: archived `ADVISORY` tokens are verified, but expected case outcomes and false approvals are never independently derived. The premise is retained.

### Unverified Boundaries

- No live Antigravity calibration run exists; calibration behavior is covered only by synthetic fixtures.
- No commit was attempted because this verdict is `REQUEST CHANGES`; the wrapper was exercised by its real-process test suite and syntax-checked directly.
- Filesystem-level archive immutability outside Git history remains unenforced, as the queued stale-info scan documents.
- The pinned entity oracle cannot detect future WHATWG additions without another authoritative live-spec comparison.

### Runtime Boundary Check

- No production hook bytes changed in round 8. The commit path remains `git commit` -> configured `.beads/hooks/pre-commit` -> `node` -> `.claude/hooks/check-review-artifacts.js` -> real filesystem and Git subprocesses -> exit code.
- The suite exercises the real hook in isolated repositories and includes the real wrapper's missing-Node failure path; no mocks replace Git or the filesystem.
- The new raw extractor evaluates the single shipped object declaration in isolation, while the separate canonicalization test evaluates the shipped functions through their complete decode/path-normalization pipeline.
- The archive helper writes a content-addressed file and stages it with real `git add`. The hook checks the archive's working-tree content and clean-filtered blob OID, but other decision inputs remain on the working-tree side of the boundary.
- No network, database, Supabase, auth, or application runtime lies on the round-8 test/doc-only execution path.

### Approved

- The round-7 completeness-test blind spot is fixed: the exact `bsol` mutant now fails, and additional distinct key/value/type mutants are rejected.
- The raw 41-pair guarantee and full-pipeline canonicalization behavior are now tested separately and accurately described in both queued state files.
- The production entity table remains an exact 41/41 match to the authoritative data, including `underbar`.
- No new finding survives beyond the two unchanged architectural approval-integrity defects.
- All 53 focused runtime tests pass with zero skips; syntax, shell, staged-scope, queued-diff, and NUL-byte checks are clean.
