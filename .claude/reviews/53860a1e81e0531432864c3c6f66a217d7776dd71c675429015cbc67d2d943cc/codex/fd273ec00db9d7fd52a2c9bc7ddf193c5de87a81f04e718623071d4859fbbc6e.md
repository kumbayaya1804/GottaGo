## Codex Review - Review-gate hardening round 6

**VERDICT: REQUEST CHANGES**

scope_hash: sha256:53860a1e81e0531432864c3c6f66a217d7776dd71c675429015cbc67d2d943cc
review_id: rg-2026-07-30-antigravity-probation-06
risk_level: high
runtime_required: true
blind_review: true
prior_reviewer_outputs_read: false
evidence_level: 3
runtime_evidence: executed

### Reviewed Queue

- `.claude/commands/antigravity-review.md`
- `.claude/commands/codex-prompt.md`
- `.claude/commands/review-gate.md`
- `.claude/hooks/check-review-artifacts.js`
- `.claude/hooks/check-review-artifacts.test.js`
- `.claude/skills/artifact_qa_gate.md`
- `.claude/skills/review_packet_generator.md`
- `AGENTS.md`
- `ANTIGRAVITY.md`
- `CLAUDE.md`
- `CODEX.md`
- `docs/agent-harness.md`
- `docs/review-severity.md`
- `docs/verification.md`
- `.claude/antigravity-calibration-contract.json`
- `.claude/antigravity-review-policy.json`
- `.claude/hooks/archive-review-artifact.js`
- `.beads/hooks/pre-commit`
- `.beads/context/execution-state.md`
- `.planning/stale-info-scan-latest.md`

### Skills Applied

- `.claude/skills/artifact_qa_gate.md` shared core
- Codex Overlay
- Global `artifact-qa-gate` with its Gotta Go workflow
- Node enforcement, Git-index, HTML-entity, and regression-test review

### Findings

- [MAJOR] `.claude/hooks/check-review-artifacts.js:115`, `.claude/hooks/check-review-artifacts.js:152`, `.claude/hooks/check-review-artifacts.js:256`, `.claude/hooks/check-review-artifacts.js:268`, `.claude/hooks/check-review-artifacts.js:682` - Decision-bearing policy, calibration contract/evidence, calibration prompts/archives, packets, and canonical verdicts are still read from the working tree with `fs`. The archive check separately proves only that a byte-identical archive enters the index. The mismatch is observable in this review: the current Codex packet's index blob is `418ccbe...` while the reviewed working-tree blob is `ba7924...`; the hook deliberately evaluates the latter. An unstaged permissive policy or fresh-looking packet/verdict can therefore decide whether a commit passes while the commit records different trust-input bytes. Disclosure does not reduce this approval-integrity defect. Required fix: evaluate every decision-bearing input from the Git index (or fail on any worktree/index mismatch), bind all dependent paths and receipts to indexed blobs, and add isolated regressions for a safe staged policy plus permissive unstaged policy and for stale staged packet/verdict bytes.

- [MAJOR] `.claude/hooks/check-review-artifacts.js:201`, `.claude/hooks/check-review-artifacts.js:204`, `.claude/hooks/check-review-artifacts.js:237`, `.claude/hooks/check-review-artifacts.js:304`, `.claude/hooks/check-review-artifacts.js:329`, `.claude/antigravity-calibration-contract.json:1` - Calibration truth is still supplied by the evidence author. The contract has required case IDs and accepted verdict tokens but no trusted expected outcome per case. `falseApprovals` is accepted from the evidence header; rows not labeled `passed: true` are skipped; required-case completion is then derived only from those same author-labeled passing rows. Receipt, uniqueness, hash, and `ADVISORY` checks prove that reviews occurred, not that the reviewer correctly handled a hidden failing case. Required fix: introduce a trusted/versioned case manifest or equivalent oracle containing expected outcomes, derive pass/fail and false-approval totals from archived verdicts against that oracle, and regression-test a forged all-passing evidence file containing an expected-negative case.

- [MINOR] `.claude/hooks/check-review-artifacts.js:393`, `.claude/hooks/check-review-artifacts.js:407`, `.claude/hooks/check-review-artifacts.test.js:1137`, `.planning/stale-info-scan-latest.md:20` - The new constant and state record claim a complete WHATWG category, but an exact comparison with the authoritative `entities.json` produces 41 eligible lowercased semicolon-terminated names versus 40 staged entries. The missing alias is `&UnderBar;` -> `_` (`underbar` after this decoder's lowercasing); there are no extras. The new regression covers `percnt`, not table completeness. No current cross-reviewer path bypass was reproduced from `_` because the protected output paths contain no underscore, so this is MINOR rather than another current exploit. Required fix: add `underbar: '_'` and a deterministic completeness assertion or checked generation step, or narrow the source comment and stale-info claim so they do not promise a closed set.

### Open Questions

- Will the user explicitly document an override accepting the two MAJOR approval-integrity gaps, or should this batch remain held until they are fixed? Under the current contract, a reviewer cannot convert either unresolved finding into approval merely because it is disclosed.

### Verification

- `node --test .claude/hooks/check-review-artifacts.test.js` - passed: 50 tests, 0 failed, 0 skipped; real temporary Git repositories and spawned hook/wrapper.
- `node --check .claude/hooks/check-review-artifacts.js` - passed.
- `node --check .claude/hooks/archive-review-artifact.js` - passed.
- `C:\Program Files\Git\bin\sh.exe -n .beads/hooks/pre-commit` - passed against the actual Git-for-Windows shell.
- `node .claude/hooks/check-review-artifacts.js --print-staged-scope-hash` - reproduced the packet fingerprint exactly.
- `git diff --cached --check -- <20 queued paths>` - passed.
- Queue/index comparison - 20 queue entries, all 20 present in the staged queued diff, none missing.
- Byte scan of all 20 queued files - 0 NUL bytes.
- Authoritative entity-table comparison using a fresh fetch of `https://html.spec.whatwg.org/entities.json` - `whatwgCount: 41`, `stagedCount: 40`, `missing: [["underbar","_"]]`, `extra: []`.
- Direct read of `.beads/context/execution-state.md`, `.planning/stale-info-scan-latest.md`, and `.planning/STATE.md` against current Git state - the round-5 `database.types.ts`, calibration-status, and self-referential current-scope contradictions are reconciled; no third state contradiction found.

### Evidence Receipts

- Source: staged queue and `git diff --cached`; target: all 20 manifest paths; observed: all queued files are staged and the staged-scope hash is `sha256:53860a1e81e0531432864c3c6f66a217d7776dd71c675429015cbc67d2d943cc`; inference: this verdict is bound to the packet's current bytes.
- Source: WHATWG HTML entity data, `https://html.spec.whatwg.org/entities.json`; target: semicolon-terminated names whose `characters` value is one ASCII non-alphanumeric code point, normalized the same way as the staged decoder; observed: 41 expected keys, 40 implemented keys, only `underbar` missing; inference: the staged "COMPLETE closed set" claim is false.
- Source: `git ls-files --stage` and `git hash-object`; target: `.claude/codex-prompt-latest.md`; observed: indexed and working-tree blob OIDs differ while `readFileSafe()` consumes the working tree; inference: the trust-boundary mismatch is present in the normal workflow, not merely hypothetical.
- Source: staged source inspection; target: `validateCalibration()` and `.claude/antigravity-calibration-contract.json`; observed: no independent expected-outcome field or oracle is consumed; inference: receipt authenticity does not establish calibration correctness.

### Adversarial Disproof

- Re-ran the real-hook `&percnt;2e`/`&percnt;2f` regression: both variants are blocked, so the specific round-5 bypass is closed.
- Compared the entire claimed entity category rather than spot-checking the newly added names: this disproved completeness with the omitted `UnderBar` alias.
- Tried to reduce the `UnderBar` omission to a current blind-review exploit: retained no such exploit because `_` is absent from both protected reviewer-output path forms; the incomplete guarantee and missing completeness test remain.
- Tried to disprove the worktree/index concern using current blobs: the current Codex packet itself has unequal index/worktree OIDs, while the gate reads the worktree. The premise is retained.
- Traced calibration from contract through receipts to verdict parsing: archived `ADVISORY` tokens are verified, but expected case outcomes and false approvals are never independently derived. The premise is retained.

### Unverified Boundaries

- No live Antigravity calibration run exists; calibration behavior is covered only by synthetic fixtures.
- No commit was attempted because this verdict is `REQUEST CHANGES`; the wrapper was exercised by its real-process test suite and syntax-checked directly.
- Filesystem-level archive immutability outside Git history remains unenforced, as the queued stale-info scan documents.
- The authoritative entity comparison verified the staged constant against the current WHATWG data; it did not test every entity spelling through a rendered Markdown implementation.

### Runtime Boundary Check

- Commit path inspected: `git commit` -> configured `.beads/hooks/pre-commit` -> `node` -> `.claude/hooks/check-review-artifacts.js` -> real filesystem and Git subprocesses -> exit code. The suite exercises the real hook in isolated repositories and includes the real wrapper's missing-Node failure path; no mocks replace Git or the filesystem.
- The archive helper writes a content-addressed file and stages it with real `git add`. The hook checks the archive's working-tree content and clean-filtered blob OID, but other decision inputs remain on the working-tree side of the boundary.
- No network, database, Supabase, auth, or application runtime lies on this commit-hook path. The only network operation in this review was the independent authoritative entity-table fetch.

### Approved

- The round-5 `&percnt;` composition bypass is closed for the tested real paths, and the fixed-point decoder regression passes.
- The round-5 state-document contradictions are reconciled without embedding a self-referential live scope hash.
- All 50 focused runtime tests pass with zero skips; syntax, shell, staged-scope, queued-diff, and NUL-byte checks are clean.
- The earlier fail-closed policy load, content-addressed/indexed archive enforcement, separator/dot/percent/entity canonicalization, CRLF-safe blob comparison, verdict-token validation, and missing-Node wrapper behavior remain covered by passing regressions.
