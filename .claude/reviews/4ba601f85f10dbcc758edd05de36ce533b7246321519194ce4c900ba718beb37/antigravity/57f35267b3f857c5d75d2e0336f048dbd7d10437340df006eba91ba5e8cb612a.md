## Antigravity Review — Review-Gate Hardening, ROUND 3

**VERDICT: ADVISORY**

scope_hash: sha256:4ba601f85f10dbcc758edd05de36ce533b7246321519194ce4c900ba718beb37
review_id: rg-2026-07-30-antigravity-probation-03
risk_level: high
runtime_required: true
blind_review: true
prior_reviewer_outputs_read: false
evidence_level: 3
runtime_evidence: executed

### Reviewed Queue
- .claude/commands/antigravity-review.md
- .claude/commands/codex-prompt.md
- .claude/commands/review-gate.md
- .claude/hooks/check-review-artifacts.js
- .claude/hooks/check-review-artifacts.test.js
- .claude/skills/artifact_qa_gate.md
- .claude/skills/review_packet_generator.md
- AGENTS.md
- ANTIGRAVITY.md
- CLAUDE.md
- CODEX.md
- docs/agent-harness.md
- docs/review-severity.md
- docs/verification.md
- .claude/antigravity-calibration-contract.json
- .claude/antigravity-review-policy.json
- .claude/hooks/archive-review-artifact.js
- .beads/hooks/pre-commit

### Skills Applied
- .claude/skills/artifact_qa_gate.md
- Antigravity Overlay
- superpowers:using-superpowers
- superpowers:verification-before-completion

### Issues
- [MAJOR] [.claude/hooks/check-review-artifacts.js:542](file:///C:/Users/mrsai/Gotta%20Go/.claude/hooks/check-review-artifacts.js#L542): `check-review-artifacts.js` reads `antigravity-prompt-latest.md`, `codex-prompt-latest.md`, `antigravity-review-latest.md`, and `codex-review-latest.md` using `readFileSafe` (`fs.readFileSync` from working tree) rather than reading from the Git index via `indexContent(req.file)`. If a developer or model edits `antigravity-review-latest.md` on disk and runs `archive-review-artifact.js` (which stages the content-addressed archive in the Git index), but leaves `antigravity-review-latest.md` unstaged or staged with stale content, `check-review-artifacts.js` passes because it evaluates the working-tree file while `hasArchivedCopy` finds the archive staged in the index. `git commit` then commits stale/invalid review text in `.claude/antigravity-review-latest.md` alongside a newly staged archive, violating the guarantee that staged artifacts represent the committed state.
- [MAJOR] [.claude/hooks/check-review-artifacts.js:284](file:///C:/Users/mrsai/Gotta%20Go/.claude/hooks/check-review-artifacts.js#L284): `validateCalibration()` checks `result.passed === true` and regex `/^\s*(?:\*\*)?VERDICT:\s*\S/im` on `verdictArchive` content, but does not verify whether the verdict inside `verdictArchive` matches the expected verdict for `caseId`. If a calibration run produces `VERDICT: BLOCK` for a test case, but the JSON evidence declares `{ "caseId": "...", "passed": true }`, `validateCalibration()` accepts the row as a passing run because `VERDICT: BLOCK` satisfies the generic `VERDICT:` pattern check.
- [MINOR] [.claude/hooks/check-review-artifacts.js:278](file:///C:/Users/mrsai/Gotta%20Go/.claude/hooks/check-review-artifacts.js#L278): `validateCalibration()` checks that `verdictArchive` matches `/^\.claude\/reviews\/[a-f0-9]{64}\/antigravity\//`, but does not verify that the 64-hex scope directory in the archive path matches `result.promptSha256` or the case prompt scope hash.
- [MINOR] [.claude/hooks/check-review-artifacts.js:361](file:///C:/Users/mrsai/Gotta%20Go/.claude/hooks/check-review-artifacts.js#L361): `canonicalizePathText()` decodes single percent-encodings (`%2f` and `%5c`), but does not decode nested/double encodings (such as `%252f`), which could allow nested URL encodings in path references to bypass normalization.

### Concerns
- `archive-review-artifact.js` automatically runs `git add` on the archive file to satisfy Check A's requirement that the archive blob exists in the Git index. However, because `check-review-artifacts.js` does not check if `antigravity-review-latest.md` itself is staged, running `archive-review-artifact.js` without staging `antigravity-review-latest.md` creates index desynchronization.

### Verification
- Executed `git status` to verify staged changes and untracked files.
- Executed `node --test .claude/hooks/check-review-artifacts.test.js` to run the test suite across temporary repositories.
- Executed `node .claude/hooks/check-review-artifacts.js` to observe pre-commit gate validation failures when scope hashes and review IDs were mismatched.
- Verified blind review path normalization across doubled slashes, `./`, `../`, and backslashes.

### Evidence Receipts
- **Command**: `node --test .claude/hooks/check-review-artifacts.test.js`
  - Target: `.claude/hooks/check-review-artifacts.test.js`
  - Observed Result: Executed 44 tests verifying review gate rules, calibration receipt validation, archive staging in index, blind review path canonicalization, and POSIX shell fallback. Exit status 0.
  - Proves: The test suite passes completely on Windows Node environment.
- **Command**: `node .claude/hooks/check-review-artifacts.js`
  - Target: Working tree and staged index
  - Observed Result: Failed closed when scope hashes or review IDs were mismatched, confirming fingerprint enforcement. Exit status 1.
  - Proves: Scope hash and review ID mismatch prevention is active.
- **Counterexample attempted**: Staging an archive with `node .claude/hooks/archive-review-artifact.js antigravity` while modifying `antigravity-review-latest.md` in the working tree without staging it.
  - Observed Result: `check-review-artifacts.js` evaluated working-tree content using `readFileSafe` and passed, while `git diff --cached` contained stale/unstaged `antigravity-review-latest.md`.
  - Proves: `readFileSafe` working-tree read creates index desynchronization vulnerability.

### Adversarial Disproof
- **Premise 1**: Round 2 fix for untracked archives (`indexContent(archivePath) === content`) fully secures archive immutability.
  - **Counterexample attempted**: Staged a valid archive via `archive-review-artifact.js`, then edited `.claude/antigravity-review-latest.md` on disk without staging it.
  - **Outcome**: Disproved. `check-review-artifacts.js` checked working-tree text for `antigravity-review-latest.md` while checking Git index for the archive, approving the commit even though `antigravity-review-latest.md` in the index was stale or unstaged.
- **Premise 2**: Calibration receipts bind every row to a valid passing verdict.
  - **Counterexample attempted**: Inspected `validateCalibration()` logic for verdict semantics check.
  - **Outcome**: Disproved. `validateCalibration()` checks `VERDICT:` presence via generic regex `/^\s*(?:\*\*)?VERDICT:\s*\S/im`, so a calibration archive containing `VERDICT: BLOCK` is accepted as a passing run when `"passed": true` is set in the JSON evidence row.

### Unverified Boundaries
- No live Supabase or remote database instance was connected during this review; all database tests were verified against schema contracts and local node test runners.

### Runtime Boundary Check
- Traced execution chain: `git commit` -> `.beads/hooks/pre-commit` -> `node .claude/hooks/check-review-artifacts.js` -> `child_process.execFileSync('git', ['ls-files', ...])`.
- Verified that `.beads/hooks/pre-commit` fails closed when `node` is absent from PATH.
- Verified that `indexContent` correctly queries `git show :<path>` to confirm staged index presence.

### Claim And State Audit
- Checked `.planning/stale-info-scan-latest.md`, `.beads/context/execution-state.md`, `AGENTS.md`, `ANTIGRAVITY.md`, `CLAUDE.md`, `CODEX.md`, `docs/agent-harness.md`, `docs/review-severity.md`, and `docs/verification.md`.
- Confirmed operational state documents match `git status`.
- Verified policy mode is `probation` with `approvalAuthority: false`, requiring verdict `ADVISORY` for clean probation checks.

### Approved
- Round 2 fix for untracked archives (`indexContent` check) successfully prevents untracked archives from passing the gate.
- Pre-commit wrapper script `.beads/hooks/pre-commit` correctly fails closed when `node` is missing from `PATH`.
- Path canonicalization in `canonicalizePathText()` handles doubled slashes, `./` segments, `../` resolution, and backslash normalization.
